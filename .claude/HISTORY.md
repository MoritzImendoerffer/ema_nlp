# Project Work History

Chronological log of all interactions that produced code or config changes.
Do not load this file at session start — read it only when the user asks.

---

> ⚠️ **Corrections (2026-05-30 retrieval refactor).** Some earlier rows describe work later superseded — or **never actually shipped**:
> - The **2026-05-28 "MIGR-011/013/015/018–025 link-graph" row** claims `corpus/extractors/link_graph.py`, a Mongo `link_graph` collection, and `parsed_documents`/`link_graph` backfills (80k/22k docs) shipped with operational metrics. They were **never committed or run** — verified in work units `2026-05-30_19` & `_20` (the collections don't exist in live Mongo). Treat that row as aspirational, not factual.
> - The **pgvector / `EMA_RETRIEVER` / three-layer-PG** rows (MIGR-001..017, NARR-*) are **superseded** by the LlamaIndex-first Neo4j refactor — see `DECISIONS.md` and [`docs/RETRIEVAL.md`](../docs/RETRIEVAL.md).

| Date | Summary | Changed | Phase | Work unit |
|------|---------|---------|-------|-----------|
| 2026-05-26 | NARR closeout — propagate "pgvector is the runtime default" into the canonical docs. `README.md` Stack table now lists Postgres+pgvector as the runtime vector store with FAISS as the legacy opt-out, Status section adds the narrative-corpus milestone, Data sources adds the Postgres tables row, Quick links links `docs/RETRIEVAL_PG.md`. `DECISIONS.md` adds a "Postgres + pgvector as the narrative-corpus retrieval backend" entry with switch contract + rationale + non-chosen alternatives; the prior "FAISS as the vector store backend (v1)" entry is marked superseded for the runtime path (query-cache FAISS is unchanged). `project_roadmap/ROADMAP.md` gets a new "Phase 1.7 — Narrative corpus on Postgres + pgvector" section (schema / ingest / retrieval surface / switch contract / test coverage). New `DELIVERY.md` inside the NARR work unit folder consolidates the final ship summary (architecture, metrics, gotchas, follow-ups). Doc-only commit; no code changes. | `README.md`, `DECISIONS.md`, `project_roadmap/ROADMAP.md`, `.claude/work/2026-05-25_16_pgvector-narrative-corpus/DELIVERY.md` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/DELIVERY.md) |
| 2026-05-26 | NARR-028 — cutover: default `EMA_RETRIEVER` flipped from `faiss` → `pgvector` in `config.py`, `app.py`, and `harness/run_eval.py`. `harness/retrieve.py` module docstring + `build_retrieve_fn` docstring marked legacy with a forward pointer to `harness.retrieve_pg`. `docs/RETRIEVAL_PG.md` env-var table re-ordered so `pgvector` is documented as the default and `faiss` as the legacy opt-out. Closes the pgvector-narrative-corpus work unit (28/28 NARR tasks complete). Full suite 253 green under the new default. | `config.py`, `app.py`, `harness/run_eval.py`, `harness/retrieve.py`, `docs/RETRIEVAL_PG.md` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-26 | NARR-011 — timing + scaling notes on marvin-gpu: PDF `--limit 100` runs at 207 docs/min (1,021 chunks, ~10/doc); HTML `--limit 100` runs at 311 docs/min (227 chunks, ~2.3/doc — trafilatura strips boilerplate aggressively); bottleneck is BGE encode on the 3090, not chunking or PG upsert. Index footprint: HNSW ~7.7 KB/chunk, BM25 GIN ~2 KB/chunk, heap ~17 KB/chunk; extrapolated full ingest (~38k PDFs + ~60k HTML) ≈ 6 h wall and ~14–18 GB DB total — well within the host envelope. GPU sat at ~1.9 GiB / 24 GiB throughout. | `.claude/work/2026-05-25_16_pgvector-narrative-corpus/timing_notes.md` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-26 | NARR-027 — new `docs/RETRIEVAL_PG.md` (provisioning → env vars → schema → ingest CLI → retrieval/traversal modes → sub-corpus filters → test DB setup → troubleshooting). `CLAUDE.md` §Data sources now declares the pg `chunks` table as the runtime retrieval target with `corpus.jsonl` benchmark-only; §V1 scope locks states the narrative corpus is in scope. `docs/SETUP.md` gets a top-of-file cross-link to the new guide. | `docs/RETRIEVAL_PG.md`, `CLAUDE.md`, `docs/SETUP.md` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-26 | NARR-026 — `tests/test_retrieve_pg.py` against a dedicated `ema_nlp_test` PG database (DSN via `PG_DSN_TEST`), populated by a deterministic fixture: 10 chunks across 3 docs (CHMP PDF / PRAC PDF / HTML) + 2 resolved links. The fixture monkey-patches `_query_embedding` so tests don't load the BGE model; embeddings are seeded with `np.random.default_rng(seed)`. 9 tests cover seeded counts, dense top-k self-recall, BM25 keyword hit, hybrid RRF combination, prefilter (committee + date_range), auto-traversal expansion via the resolved link graph, `max_hops=0` no-op, and dispatcher mode routing. Skip-clean when `PG_DSN_TEST` is unset. Test DB setup is documented in `docs/RETRIEVAL_PG.md` §10. | `tests/test_retrieve_pg.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-26 | NARR-025 — unit test suite for the corpus.ingestion modules: new `test_chunker.py`, `test_pdf_normaliser.py`, `test_html_normaliser.py` against the existing tests/fixtures (`ema_pdf_sample.md`, `ema_html_sample.html`, `ema_nav_landing.html`); the existing `test_link_extractor.py` (NARR-012) was retained as-is. Coverage on the four ingestion modules = 91% (chunker 98% / link_extractor 91% / pdf_normaliser 93% / html_normaliser 83%); 53 tests run in 4 s. | `tests/test_chunker.py`, `tests/test_pdf_normaliser.py`, `tests/test_html_normaliser.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-26 | NARR-024 — YAML exposure of `retrieval.prefilter` + `retrieval.traversal` was already wired in `RetrievalConfigPG.from_yaml_section` (NARR-015); shipped `harness/configs/example_chmp_only.yaml` as the reference config and verified end-to-end that `committee=['CHMP']` restricts results to CHMP-only chunks via direct SQL on the live PG (10/10 returned chunks come from `documents.committee='CHMP'`). | `harness/configs/example_chmp_only.yaml` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | NARR-023 — end-to-end smoke of `simple_rag` on pgvector: 5/5 questions returned non-empty answers; retrieval surfaced 46/50 chunks from URLs outside `corpus.jsonl` (narrative coverage confirmed). Also fixed a latent bug from NARR-021: `WorkflowRunner._stamp_span` was stamping onto `get_current_span()` BEFORE `_wf.run()` started, so the no-op span swallowed every `ema.*` attribute — zero `ema.*` attrs were reaching Phoenix in practice. `ainvoke()` now opens an explicit `tracer.start_as_current_span("<Workflow>.invoke")` and stamps into that, and `run_eval.py` registers Phoenix at module top (mirroring `app.py`). Live Phoenix now shows `ema.retrieval.backend='pgvector'` plus all `ema.orchestration/retrieval/run` attributes on every workflow invocation. Added `test_live_otel_sdk_records_attributes_on_wrapper_span` (real OTel SDK + in-memory exporter, not just mocks) so this won't regress; fixed `_run` helper to use a fresh event loop (closes a pre-existing test-order flake). Suite: 212 green. | `harness/run_eval.py`, `harness/workflows/utils.py`, `tests/test_span_attributes.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | NARR-022 — `EMA_RETRIEVER` env-var dispatch in `harness/run_eval.py` mirroring the `app.py` pattern (pgvector branch skips `build_index`, builds `RetrievalConfigPG`-backed `retrieve_fn`); `WorkflowRunner._stamp_span` now also emits `ema.retrieval.backend = 'faiss'\|'pgvector'` on every workflow span, sourced from the env var. Smoke-tested retrieval-only on a 5-question slice under both backends — both produced complete `results/<run_id>/` outputs; span-attribute hook verified via mock OTel span; 27 workflow/span tests green. | `harness/run_eval.py`, `harness/workflows/utils.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | NARR-021 — `EMA_RETRIEVER` env-var dispatch in `app.py`: `pgvector` branch skips `build_index`, configures the BGE embed model in-place, and builds a `RetrievalConfigPG`-backed `retrieve_fn`; `faiss` branch unchanged. `results_to_docs` now prefers `metadata["text"]` so pg results don't need a LlamaIndex docstore. Verified end-to-end against live PG (10 hybrid hits → simple_rag produces an answer with PDF source citations); 211 tests still green. | `app.py`, `harness/workflows/utils.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | NARR-016..020 — Phase E retrieval complete: dense kNN via HNSW (`<=>` with explicit `::vector` cast on params), BM25 via `ts_rank_cd`, hybrid RRF (K=60) + dispatcher + `build_retrieve_fn_pg`; auto-traversal via recursive CTE (initial seeds preserved; expansion appended with score=0); `follow_links` FunctionTool for ReAct agent_tool traversal. Verified live against 25-doc seed: dense hits 0.6–0.7 cosine, BM25 keyword matches work, hybrid fuses both, auto-traversal expands 2 seeds → 7 chunks via the legal-notice link cluster. 14 new pure-Python unit tests; full suite 211 green. | `harness/retrieve_pg.py`, `harness/pg/adapter.py`, `harness/pg/tools.py`, `harness/pg/queries.py`, `tests/test_retrieve_pg_pure.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | NARR-010..015: HTML source wired into ingest (10 html docs / 16 chunks); link_extractor module (markdown / HTML / EMA ref / see-Q&A); links populated via ingest (1012 hyperlinks, 94 reference_numbers on the 25-doc seed); scripts/resolve_links.py (3.9% / 4.3% resolution on seed; idempotent); RetrievalConfigPG + PrefilterConfig + TraversalConfig scaffolding + pg-side LlamaIndex adapter (`harness/pg/adapter.py`). 32 new unit tests; full suite 197 green. | `harness/embed_pg.py`, `harness/retrieve_pg.py`, `harness/pg/adapter.py`, `corpus/ingestion/link_extractor.py`, `scripts/resolve_links.py`, `tests/test_link_extractor.py`, `tests/test_retrieve_pg_config.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | Phase A+B+C of pgvector pivot: Postgres+pgvector via Docker, schema DDL + init_db, psycopg pool with vector type, LlamaIndex chunker, PDF + HTML normalisers, BGE embedder on CUDA, ingest pipeline (10 docs / 311 chunks in 10s), resume + --force semantics verified. NARR-001..009 done | `deploy/postgres/`, `corpus/pg_schema.sql`, `scripts/init_db.py`, `scripts/test_ingest_resume.py`, `harness/pg/`, `harness/embed_pg.py`, `harness/providers.py`, `corpus/ingestion/`, `pyproject.toml`, `config.py` | Phase 1 (corpus pivot) | [pgvector narrative](./work/2026-05-25_16_pgvector-narrative-corpus/SESSION_HANDOFF.md) |
| 2026-05-25 | Fixed "View traces" link 404 in chat UI by resolving Phoenix project node ID via /v1/projects instead of using raw name in URL | `app.py` | Phase 1 | — |
| 2026-05-22 | Created implementation plan for advanced retrieval strategies (recursive, hierarchical, adaptive); documented 4 architecture inconsistencies found during review | `.claude/work/2026-05-22_advanced-retrieval/` | Phase 3/4 | [advanced-retrieval](.claude/work/2026-05-22_advanced-retrieval/implementation-plan.md) |
| 2026-05-22 | Architecture assessment: LangGraph+LlamaIndex integration reviewed; created 11-task migration plan to drop LangChain entirely in favour of LlamaIndex Workflows | `.claude/work/2026-05-22_10_llamaindex-langgraph-assessment/` | Phase 2/3 | [WF migration](./work/2026-05-22_10_llamaindex-langgraph-assessment/implementation-plan.md) |
| 2026-05-22 | Updated documentation to reflect actual project state: README stack table, ARCHITECTURE.md (chains layer, retrieval strategies, LangSmith), RETRIEVAL_PIPELINE.md (strategies, EMARetriever), DECISIONS.md (LangChain/LangGraph, LangSmith decisions) | `README.md`, `docs/ARCHITECTURE.md`, `docs/RETRIEVAL_PIPELINE.md`, `DECISIONS.md` | Phase 3/4 | — |
| 2026-05-10 | Validated project plan against literature, researched IDMP-O ontology, surfaced roadmap gaps | — | Phase 0 | [exploration](.claude/work/2026-05-10_01_project-exploration/exploration.md) |
| 2026-05-10 | Generated 35-task implementation plan with acceptance criteria and GitHub issue creation script | `scripts/create_github_issues.py`, `scripts/create_github_issues.sh` | Phase 0 | [plan](.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md) |
| 2026-05-10 | Executed Phase 0 inventory: 165 sources, 1,506 est. pairs, 11 topic clusters, 43.6% chain completeness | `scripts/phase0_inventory.py`, `scripts/phase0_topic_report.py` | Phase 0 | [phase0-execution](.claude/work/2026-05-10_03_phase0-execution/decisions.md) |
| 2026-05-15 | Added HTML accordion extractor and PDF Q&A extractor with tests | `corpus/extraction/`, `tests/` | Phase 1 | — |
| 2026-05-15 | Added portable setup script, one-way MongoDB sync script, and dotenv-based config | `scripts/setup.sh`, `scripts/sync_mongo.sh`, `config.py`, `pyproject.toml` | Phase 0 | — |
| 2026-05-15 | Updated HISTORY.md schema (5-column) and CLAUDE.md work-history rule to trigger on all code changes | `CLAUDE.md`, `.claude/HISTORY.md` | Phase 0 | — |
| 2026-05-15 | Added docs/SETUP.md with full setup + sync guide; extended sync_mongo.sh with export/import subcommands via Nextcloud; updated README | `docs/SETUP.md`, `scripts/sync_mongo.sh`, `README.md` | Phase 0 | — |
| 2026-05-15 | Architecture exploration: agentic memory via LlamaIndex DocumentSummaryIndex, model-agnostic tracing via Arize Phoenix/OpenInference, ontology as node metadata | — | Phase 1 | [exploration](.claude/work/2026-05-15_04_agentic-memory-architecture/exploration.md) |
| 2026-05-15 | Adopted LlamaIndex as retrieval framework; updated TASK-016/017/020/027 in state.json; added TASK-016.5 (ontology metadata); added LlamaIndex+Phoenix deps to pyproject.toml | `pyproject.toml`, `state.json`, `implementation-plan.md` | Phase 1 | — |
| 2026-05-15 | Architecture exploration: RL-style feedback store, semantic cache with user confirmation, online few-shot learning from rated trajectories | — | Phase 1 | [exploration](.claude/work/2026-05-15_05_rl-feedback-cache/exploration.md) |
| 2026-05-15 | Revised RL/cache design after library research: Phoenix annotations replace SQLite, thin FAISS query cache stays custom, DSPy deferred; added TASK-027.5–027.9 to state.json | `.claude/work/2026-05-15_05_rl-feedback-cache/exploration.md`, `state.json` | Phase 1 | — |
| 2026-05-15 | Added DECISIONS.md and OPEN_QUESTIONS.md; rewrote README.md (removed stale Neo4j section); updated CLAUDE.md (phase, commands, decisions pointer) | `DECISIONS.md`, `OPEN_QUESTIONS.md`, `README.md`, `CLAUDE.md` | Phase 1 | — |
| 2026-05-15 | Adversarial plan review (24 issues); applied 7 critical + 3 significant fixes to state.json and implementation-plan.md: TASK-009 now gates benchmark construction; TASK-022/024 unblocked to TASK-008; TASK-016.5 wired to TASK-023; TASK-027.7 fixed to depend on TASK-027.8; TASK-030 handles TASK-029 skip path; TASK-015 pass/fail threshold added | `.claude/work/2026-05-10_02_implementation-plan/state.json`, `.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md` | Phase 1 | — |
| 2026-05-17 | Offline plan: split TASK-007 into TASK-007A (pure logic), TASK-007B (mini-corpus HTTP), TASK-007 (MongoDB adaptor); relaxed TASK-016 dependency to TASK-007B; updated state.json and implementation-plan.md | `state.json`, `implementation-plan.md` | Phase 1 | [plan](.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md) |
| 2026-05-17 | Updated create_github_issues.py with new/changed tasks and completed statuses; wrote sync_github_issues.py (create+update+close delta sync) | `scripts/create_github_issues.py`, `scripts/sync_github_issues.py` | Phase 1 | — |
| 2026-05-17 | Completed TASK-007A (build_corpus.py, 16 tests), TASK-007B (fetch_mini_corpus.py), TASK-016 (harness/embed.py, VectorStoreIndex+FAISS, 11 tests incl. top-1 retrieval); fixed FakeEmbedModel to subclass BaseEmbedding; fixed excluded_embed_metadata_keys bug | `corpus/build_corpus.py`, `scripts/fetch_mini_corpus.py`, `harness/embed.py`, `tests/test_build_corpus.py`, `tests/test_embed.py`, `state.json` | Phase 1/3 | [plan](.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md) |
| 2026-05-17 | Completed TASK-017: BM25Retriever + manual RRF hybrid fusion (harness/retrieve.py, 8 tests); QueryFusionRetriever avoided since it requires OpenAI install; RRF implemented directly; hybrid recall test proves dense misses exact-match token "26.5 ng/day" while hybrid finds it | `harness/retrieve.py`, `tests/test_retrieve.py`, `state.json` | Phase 3 | [plan](.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md) |
| 2026-05-17 | Completed TASK-018 (eval_retrieval.py: Recall@k, Precision@k, Citation Accuracy by T1–T4 + bar chart, 10 tests), TASK-016.5 (tag_concepts.py: IDMP RDF→concepts.yaml, filter_by_concept retriever), TASK-019 (judge.py: faithfulness+correctness LLM judges with prompt files), TASK-020 (run_eval.py: YAML-driven single entry point + baseline configs) | `harness/eval_retrieval.py`, `harness/judge.py`, `harness/run_eval.py`, `harness/judges/`, `harness/configs/`, `harness/ontology/concepts.yaml`, `scripts/tag_concepts.py`, `tests/test_eval_retrieval.py`, `state.json` | Phase 3 | [plan](.claude/work/2026-05-10_02_implementation-plan/implementation-plan.md) |
| 2026-05-17 | PDF-001: scripts/ingest_parsed_pdfs.py — bulk-upserts 65k parsed_pdf.pkl files into MongoDB parsed_pdfs collection (batch 500, tqdm, --dry-run/--limit flags); uses os.walk (Path.walk is 3.12+); requires ema_scraper repo on sys.path for pickle deserialization | `scripts/ingest_parsed_pdfs.py`, `.claude/work/2026-05-17_07_pdf-mongodb-linking/state.json` | Phase 1 | [pdf-mongodb-linking](.claude/work/2026-05-17_07_pdf-mongodb-linking/state.json) |
| 2026-05-17 | PDF-002: ran full ingestion (65,263/65,265 upserted, 2 oversized skipped); validated parsed_pdfs collection — 65,263 docs, 781 MB compressed; 10.5% parse failure rate (6,821 bad PDFs); 19,494 legacy null-error docs with markdown; documented in exploration.md | `.claude/work/2026-05-17_07_pdf-mongodb-linking/exploration.md`, `scripts/ingest_parsed_pdfs.py`, state.json | Phase 1 | [pdf-mongodb-linking](.claude/work/2026-05-17_07_pdf-mongodb-linking/state.json) |
| 2026-05-17 | PDF-003: corpus/sources/mongo_source.py — records_from_mongodb() adaptor; html_raw is stored as 1-element list (fixed); 7/7 integration tests pass | `corpus/sources/__init__.py`, `corpus/sources/mongo_source.py`, `tests/test_mongo_source.py`, `pyproject.toml` | Phase 1 | [pdf-mongodb-linking](.claude/work/2026-05-17_07_pdf-mongodb-linking/state.json) |
| 2026-05-17 | PDF-004: full corpus run — 26,251 QARecords written to corpus/corpus.jsonl (83,895 input, 57,644 deduped, 0 filtered; 17,505 html_accordion + 8,746 pdf) | `corpus/corpus.jsonl`, `corpus/corpus_dedup_log.jsonl`, `corpus/corpus_filter_log.jsonl`, state.json | Phase 1 | [pdf-mongodb-linking](.claude/work/2026-05-17_07_pdf-mongodb-linking/state.json) |
| 2026-05-17 | TASK-UI-001/002/003: app.py Chainlit 2.11 chat UI — hybrid RAG retrieval, streaming Claude synthesis via AsyncAnthropic, cl.Step for retrieval+synthesis, cl.Text sidebar source cards, Phoenix auto-instrumentation (LlamaIndex+Anthropic), OTel span capture, 👍/👎 cl.Action buttons → phoenix.client.Client().spans.add_span_annotation(); pyproject.toml [ui] extras; chainlit.md welcome page | `app.py`, `pyproject.toml`, `chainlit.md`, `.chainlit/config.toml`, `.claude/work/2026-05-17_06_chat-ui/state.json` | Phase 4B | [chat-ui](.claude/work/2026-05-17_06_chat-ui/state.json) |
| 2026-05-17 | run_ui.sh — starts Phoenix on :6006 (waits for readiness, logs to .phoenix.log) then Chainlit on :8000; cleanup trap kills Phoenix on exit; PHOENIX_DISABLED=1 skips Phoenix | `run_ui.sh` | Phase 4B | [chat-ui](.claude/work/2026-05-17_06_chat-ui/state.json) |
| 2026-05-17 | TASK-022/023/024/025/026: Full Ablation A retrieval pipeline — acronym_dict.yaml (42 EMA entries, context-guarded expansion for AI/MA), relevance_rubric_sme.md (v1), a1_query_expansion.py (bidirectional, no recursive expansion), a2_topic_filter.py (keyword post-filter + concept pre-filter modes), a3_reranker.py (SME rubric, Haiku), a4_reranker.py (generic prompt), 7 ablation configs (A1–A5+keyword/concept variants), run_eval.py wired; 14/14 tests pass | `ablations/A_evidence_filter/acronym_dict.yaml`, `harness/prompts/relevance_rubric_sme.md`, `harness/ablations/a1_query_expansion.py`, `harness/ablations/a2_topic_filter.py`, `harness/ablations/a3_reranker.py`, `harness/ablations/a4_reranker.py`, `harness/configs/ablation_a_*.yaml`, `harness/ontology/__init__.py`, `harness/run_eval.py`, `tests/test_ablations.py` | Phase 3 | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-17 | docs/SETUP.md: added LLM and embedding model settings section — ANTHROPIC_BASE_URL, EMA_LLM_MODEL, EMA_EMBED_MODEL, EMA_EMBED_PROVIDER with HuggingFace/OpenAI examples and laptop/gateway example configs | `docs/SETUP.md` | Phase 3 | [provider-config](.claude/work/2026-05-17_08_provider-config/state.json) |
| 2026-05-17 | COV-001/002: fixed retrieval coverage — DEFAULT_CORPUS changed from mini_corpus.jsonl (156 records) to corpus.jsonl (26,251 records) in embed.py and app.py; documented EMA_CORPUS_PATH and EMA_INDEX_PATH env vars in SETUP.md | `harness/embed.py`, `app.py`, `docs/SETUP.md` | Phase 1/3 | [retrieval-coverage](.claude/work/2026-05-17_09_retrieval-coverage/state.json) |
| 2026-05-17 | PROV-001/002/003: harness/providers.py — configure_embed_model() and get_llm_model() read EMA_LLM_MODEL/EMA_EMBED_MODEL/EMA_EMBED_PROVIDER lazily; wired into embed.py, judge.py, a3_reranker.py, a4_reranker.py, run_eval.py, app.py; 94/94 tests pass | `harness/providers.py`, `harness/embed.py`, `harness/judge.py`, `harness/ablations/a3_reranker.py`, `harness/ablations/a4_reranker.py`, `harness/run_eval.py`, `app.py` | Phase 3 | [provider-config](.claude/work/2026-05-17_08_provider-config/state.json) |
| 2026-05-17 | SEP: moved corpus.jsonl and FAISS index out of repo tree to ~/Nextcloud/Datasets/ema_nlp/; config.py exposes EMA_DATA_DIR/CORPUS_PATH/INDEX_DIR; embed.py imports from config; run_eval.py _resolve() supports tilde paths; 8 YAML configs updated; corpus.jsonl removed from git history via filter-repo | `config.py`, `harness/embed.py`, `harness/run_eval.py`, `harness/configs/*.yaml`, `app.py`, `.gitignore` | Phase 3 | — |
| 2026-05-17 | TASK-027.5: harness/query_cache.py — FAISS flat-IP (cosine) index over past query embeddings + JSON sidecar; add_entry/get_similar/update_rating; graceful empty-cache handling; 10/10 tests pass | `harness/query_cache.py`, `tests/test_query_cache.py`, `state.json` | Phase 4B | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-17 | TASK-032: harness/models.py + harness/configs/models.yaml — unified call_model() for Anthropic (Haiku/Opus) and Together AI (OLMo 3 32B); TOGETHER_API_KEY required for OLMo; 15/15 tests pass | `harness/models.py`, `harness/configs/models.yaml`, `tests/test_models.py`, `state.json` | Phase 4C | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-18 | TASK-009: contamination check — 35 sentences from 5 EMA source docs searched via Infini-gram; 31% match Dolma v1.7 on generic boilerplate (max count=7); 0/5 match OLMo-mix → OLMo 3 confirmed as clean reference; docs/training_data_verification.md + docs/contamination_sentences.tsv + scripts/contamination_check.py | `docs/training_data_verification.md`, `docs/contamination_sentences.tsv`, `scripts/contamination_check.py`, `state.json` | Phase 1 | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-18 | Index build complete: FAISS VectorStoreIndex persisted to ~/Nextcloud/Datasets/ema_nlp/index (103M faiss.index, 108M docstore.json, 25,626 nodes); build took ~21 min CPU; loads in 4 s | `~/Nextcloud/Datasets/ema_nlp/index/` | Phase 3 | — |
| 2026-05-17 | TASK-007 (completed retroactively — done in PDF-003/004): MongoDB adaptor records_from_mongodb() already covers both html + pdf branches; 7/7 integration tests pass | `corpus/sources/mongo_source.py`, `tests/test_mongo_source.py`, `state.json` | Phase 1 | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-17 | TASK-008: corpus/corpus_stats.md generated — 26,251 records, 3,345 topic paths, 66.7% HTML + 33.3% PDF, 56.7% high / 43.3% medium confidence, 0 schema validation errors; SCOPE-RISK not triggered | `corpus/corpus_stats.md`, `state.json` | Phase 1 | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-17 | TASK-027.6: semantic cache lookup in app.py — cl.AskActionMessage before each query; [a/b/c] use cached / use as few-shot context + run fresh / [d] skip; cached path returns stored answer with no LLM call; fresh path injects cached Q&A as few-shot example; cache.add_entry() called after synthesis; run_eval.py logs warning if cache:true (no-op for batch); cache:false added to all 8 YAML configs | `app.py`, `harness/run_eval.py`, `harness/configs/*.yaml`, `state.json` | Phase 4B | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-21 | TASK-027.8/027.9/027.7: CLI rating UI (harness/rating.py — prompt_for_rating, Phoenix annotation, query cache sync, skippable), JSONL trace export (harness/export_traces.py — export_rated_traces with min_rating/date/model/retriever filters, child-span trajectory reconstruction), runtime few-shot injection (harness/fewshot_inject.py — get_fewshot_context, top-k rated cache hits, Phoenix trajectory fetch); ReActRAGAgent wired with fewshot_context parameter + OTel attribute logging; run_b1_sanity.py --rate flag added; 116/116 tests pass | `harness/rating.py`, `harness/export_traces.py`, `harness/fewshot_inject.py`, `harness/agents/react_agent.py`, `ablations/B_process_rewards/run_b1_sanity.py`, `state.json` | Phase 4B | [implementation-plan](.claude/work/2026-05-10_02_implementation-plan/state.json) |
| 2026-05-22 | Code review fixes: double docstore lookup in hierarchical retrieval eliminated, q_arr norm hoisted from loop, RetrievalConfig cached at EMARetriever init (PrivateAttr), Judge() singleton in evaluators, dead AgentState.docs field removed, agentic strategy raises NotImplementedError, private chain symbols made public, _load_records deduplicated, grade condition simplified, LCEL key-picker lambda removed; 159/159 tests pass | `harness/retrieve.py`, `harness/chains/retriever.py`, `harness/chains/evaluators.py`, `harness/chains/agents/react.py`, `harness/chains/agents/crag.py`, `harness/chains/simple_rag.py`, `harness/embed_hierarchical.py`, `tests/` | Phase 3/4 | — |
| 2026-05-22 | Critical bug fix: 12 architecture bugs corrected — max_tokens kwarg in call_model() (all ablation_c runs were silently failing), non-answer judge guard, Haiku→Sonnet upgrade for judge (all configs) and reranker (A3/A4/A5), judge context size 3→10 passages, double retrieval eliminated via cache, error gate for >50% answer failure, lift metric script compute_lift.py, judge enabled in all ablation_a configs, per-type T1–T4 judge breakdown in run_summary.md, OLMo naming fixed in models.yaml, CoT empty-answer guard | `harness/models.py`, `harness/judge.py`, `harness/run_eval.py`, `harness/answer_gen.py`, `harness/compute_lift.py`, `harness/configs/` | Phase 2 | — |
| 2026-05-22 | LG-001–006: LangGraph pipeline layer — PipelineState TypedDict, node library (retrieval/generation/grade/rewrite/summarization/review), build_pipeline() factory with PipelineConfig, 4 new strategies (summarize_rag/crag_summarize/crag_review/react_review), per-node RLHF annotation in rating.py, node_name filter in fewshot_inject.py; 77/77 tests pass | `harness/chains/pipeline_state.py`, `harness/chains/pipeline.py`, `harness/chains/nodes/`, `harness/chains/registry.py`, `harness/chains/agents/crag.py`, `harness/prompts/system_summarize.md`, `harness/rating.py`, `harness/fewshot_inject.py`, `tests/test_pipeline_nodes.py`, `tests/test_langchain_chains.py` | Phase 3/4 | [langgraph-orchestration](.claude/work/2026-05-22_langgraph-orchestration/state.json) |
| 2026-05-22 | LG-007: MemorySaver session wiring — app.py switched from raw Anthropic streaming to build_pipeline() with MemorySaver + uuid4 session_id; pipeline.ainvoke() called with config={thread_id} per turn; 4 new MemorySaver unit tests; 81/81 tests pass | `app.py`, `tests/test_pipeline_nodes.py`, `.claude/work/2026-05-22_langgraph-orchestration/state.json` | Phase 4B | [langgraph-orchestration](.claude/work/2026-05-22_langgraph-orchestration/state.json) |
| 2026-05-22 | Completed LangChain→LlamaIndex documentation cleanup: removed stale LangGraph/LangSmith/EMARetriever references from ARCHITECTURE.md, RETRIEVAL_PIPELINE.md, SETUP.md, DECISIONS.md, app.py, harness/retrieve.py, harness/workflows/utils.py; deleted empty transition stubs | `docs/ARCHITECTURE.md`, `docs/RETRIEVAL_PIPELINE.md`, `docs/SETUP.md`, `DECISIONS.md`, `app.py`, `harness/retrieve.py`, `harness/workflows/utils.py` | Phase 3 | — |
| 2026-05-23 | REFACT-009–011: Built ReActNativeWorkflow (per-step Phoenix spans via @step events); events.py ThoughtEvent/ActionEvent/ObservationEvent/FinishEvent; registered react_native as default 'react' strategy, react_legacy for old FunctionAgent; documented Phoenix annotation schemas + hosting in SETUP.md | `harness/workflows/react_native.py`, `harness/workflows/events.py`, `harness/workflows/registry.py`, `harness/workflows/composites.py`, `docs/SETUP.md`, `DECISIONS.md`, `tests/test_workflows.py`, `tests/test_workflow_registry.py` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | REFACT-012–013: Created harness/hitl/ package with export_annotations.py — Phoenix REST API → Nextcloud JSONL export with --since/--strategy/--dry-run flags; 9 unit tests using mocked Phoenix client | `harness/hitl/__init__.py`, `harness/hitl/export_annotations.py`, `tests/test_hitl_export.py` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | REFACT-015–020: BM25 module-level cache; dropped Doc dataclass, TextNode end-to-end in all workflows + app.py + run_eval.py; A3/A4 BaseNodePostprocessor subclasses; acronym expansion in react_native ema_search tool; hardcoded AI hint removed from react/react_native prompts; RETRIEVAL_PIPELINE.md updated for new A3/A4 postprocessors and react_native; 157 tests pass | `harness/retrieve.py`, `harness/workflows/utils.py`, `harness/workflows/simple_rag.py`, `harness/workflows/crag.py`, `harness/workflows/summarize_rag.py`, `harness/workflows/composites.py`, `harness/workflows/react.py`, `harness/workflows/react_native.py`, `harness/ablations/a3_reranker.py`, `harness/ablations/a4_reranker.py`, `app.py`, `harness/run_eval.py`, `docs/RETRIEVAL_PIPELINE.md` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | REFACT-019: moved results/ to ~/Nextcloud/Datasets/ema_nlp/results/ and created symlink; all 18 YAML configs updated to base_dir: ~/Nextcloud/Datasets/ema_nlp/results; results/ removed from git tracking; SETUP.md §7 added (Nextcloud layout, new-machine setup); DECISIONS.md infrastructure entry added | `.gitignore`, `harness/configs/*.yaml`, `docs/SETUP.md`, `DECISIONS.md` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | REFACT-021: confirmed A0+ zero regression (Recall@10=0.748, CitationAcc=0.904 reproduced exactly); re-ran baseline_a0, a0plus, a1 with new code; all 8 ablation_a configs confirmed working; REFACT-022: created 4 workflow axis configs (workflow_simple_rag, workflow_crag, workflow_react, workflow_crag_review); started simple_rag workflow eval run | `harness/configs/workflow_simple_rag.yaml`, `harness/configs/workflow_crag.yaml`, `harness/configs/workflow_react.yaml`, `harness/configs/workflow_crag_review.yaml` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | Bug fix: A2 concept filter crashed with FAISS (MetadataFilters not supported); added try-except fallback in run_eval.py so a2_concept gracefully falls back to base retrieval with warning log | `harness/run_eval.py` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-23 | Bug fix: review.py used d.page_content (LangChain) instead of d.text (LlamaIndex TextNode); caused crag_review workflow to return 0.00/5 judge scores for all questions; restarted crag_review eval run | `harness/workflows/review.py` | Phase 3 | [harness-refactoring](.claude/work/2026-05-22_11_harness-refactoring/state.json) |
| 2026-05-24 | HITL-005: added configure_embed_model() to run_b1_sanity.py; re-ran B1 sanity with Opus react workflow — all 5 questions now have trajectory len ≥ 3 with ema_search tool calls | `ablations/B_process_rewards/run_b1_sanity.py`, `ablations/B_process_rewards/b1_trajectories.jsonl` | Phase 3 | [hitl-pipeline-gaps](.claude/work/2026-05-24_12_hitl-pipeline-gaps/state.json) |
| 2026-05-24 | HITL-006: built harness/label_session.py — interactive labeling CLI with stratified/uniform sampling, crash-safe JSONL checkpoint, session resume, and end-of-session summary | `harness/label_session.py` | Phase 3 | [hitl-pipeline-gaps](.claude/work/2026-05-24_12_hitl-pipeline-gaps/state.json) |
| 2026-05-24 | HITL-007: wired get_fewshot_context() in app.py (automatic injection, removed manual "context" action) and run_eval.py (cache_inject: bool config field); added cache_inject: false to 14 YAML configs; 4-case unit test | `app.py`, `harness/run_eval.py`, `harness/configs/*.yaml`, `tests/test_fewshot_inject.py` | Phase 3 | [hitl-pipeline-gaps](.claude/work/2026-05-24_12_hitl-pipeline-gaps/state.json) |
| 2026-05-25 | UI2-001–004: ChatProfile workflow dropdown (9 strategies), ChatSettings right sidebar (model/temperature/k/cache toggle), SQLite chat history with password auth, on_chat_resume thread restore; added get_llm_for_model() to harness/llms.py | `app.py`, `harness/llms.py`, `pyproject.toml`, `.gitignore`, `.chainlit/config.toml` | Phase 3 | [chat-ui-enhancements](.claude/work/2026-05-25_13_chat-ui-enhancements/state.json) |
| 2026-05-25 | Created implementation plan for HARNESS_REFACTORS.md: 12 tasks across 3 changes (registry collapse, Phoenix span stamping, shared retrieval factory) in work unit 14 | `.claude/work/2026-05-25_14_config-trace-refactoring/` | Phase 3 | [config-trace-refactoring](.claude/work/2026-05-25_14_config-trace-refactoring/state.json) |
| 2026-05-25 | TRACE-001–012: implemented all 3 HARNESS_REFACTORS changes — registry collapse (7 strategies, prompt_strategy YAML field), Phoenix span stamping via config_attributes(), shared build_retrieve_fn factory enabling CRAG+reranker compositions; 165 tests pass | `harness/workflows/*.py`, `harness/retrieve.py`, `harness/run_eval.py`, `app.py`, `harness/configs/*.yaml`, `tests/test_span_attributes.py`, `DECISIONS.md`, `docs/RETRIEVAL_PIPELINE.md` | Phase 3 | [config-trace-refactoring](.claude/work/2026-05-25_14_config-trace-refactoring/state.json) |
| 2026-05-25 | Enabled `setup@local` toolkit plugin in settings.json after switching from laptop to this machine; verified other toolkit plugins (development/memory/system/transition/workflow) align with `/home/moritz/github_repos/claude-code-toolkit/plugins/`. Left `web-development@local` and `reports@local` entries pending — they reference plugins not present on this machine or in the official Claude Code marketplace. | `.claude/settings.json` | Phase 3 | — |
| 2026-05-25 | Removed `web-development@local` and `reports@local` from settings.json — user confirmed both were "failed to load" from the official marketplace on this machine and opted to skip rather than reinstall. | `.claude/settings.json` | Phase 3 | — |
| 2026-05-26 | MIGR-001: added `ParsedDocument` dataclass + Mongo writer for `parsed_documents` collection (compound unique index on `(url, parser, parser_version)` + per-URL + per-parser-version secondary indexes); 12 new tests under mongomock; 265 tests pass | `corpus/parsers/__init__.py`, `corpus/parsers/base.py`, `corpus/sources/parsed_documents.py`, `tests/test_parsed_documents_writer.py`, `pyproject.toml` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-26 | MIGR-002–006: Phase A foundation + PG schema delta — `corpus/metadata/{url,text}_metadata.py` (regexes moved verbatim from pdf_normaliser; link_extractor + pdf_normaliser re-import from new home); `corpus/parsers/{pymupdf4llm,trafilatura}.py` implementing Parser protocol with CLIs; `corpus/pg_schema.sql` adds parser/parser_version/parsed_at/parsed_text/parsed_text_hash columns + `documents_parser_idx` (idempotent via `ALTER TABLE … ADD COLUMN IF NOT EXISTS`); 80 new tests, 345 pass | `corpus/metadata/*`, `corpus/parsers/*`, `corpus/ingestion/{pdf_normaliser,link_extractor}.py`, `corpus/pg_schema.sql`, `scripts/ingest_parsed_pdfs.py`, `tests/test_{url,text}_metadata.py`, `tests/test_parsers_*.py` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-26 | MIGR-007–010: parser-agnostic sync refactor — `harness/embed_pg.sync(parser_preference, ...)` reads from Mongo `parsed_documents`, hash-skips on `documents.parsed_text_hash`, writes parser identity columns on upsert; `corpus/sources/synthetic_legacy_reader.py` bridges legacy collections; `harness/configs/parser_preference.yaml` + `--parser-preference content_type=parser` CLI override; `UPSERT_DOCUMENT` extended with the 5 new columns + new `PARSED_HASH_BY_DOC_IDS` query; +40 tests including 7-case integration suite against `ema_nlp_test` PG; 385 pass | `harness/embed_pg.py`, `harness/pg/queries.py`, `harness/configs/parser_preference.yaml`, `corpus/sources/synthetic_legacy_reader.py`, `tests/test_embed_pg_sync*.py`, `tests/test_synthetic_legacy_reader.py`, `tests/test_parser_preference.py` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-26 | MIGR-012, 014, 016, 017: PR3 code + docs — `scripts/migrate_mongo_to_parsed_documents.py` idempotent backfill (9 tests via mongomock); `corpus/parsers/llamahub_pdf.py` behind `[parsers-llamahub]` extra (7 tests, 2 skip when extra absent); `docs/RETRIEVAL_PG.md` §13 "Three-layer data flow" + "Adding a parser" + "Re-sync after a parser change" + env-var table; `CLAUDE.md` §Data sources notes Mongo-sink/PG-canonical split; `DECISIONS.md` "Three-layer separation" entry; 400 pass, 1 skipped. **PAUSED on MIGR-011 (live PG smoke), MIGR-013 (production Mongo backfill), MIGR-015 (E2E parser-swap smoke).** | `scripts/migrate_mongo_to_parsed_documents.py`, `corpus/parsers/llamahub_pdf.py`, `tests/test_migrate_backfill.py`, `tests/test_parsers_llamahub_pdf.py`, `pyproject.toml`, `docs/RETRIEVAL_PG.md`, `CLAUDE.md`, `DECISIONS.md` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-28 | MIGR-011, 013, 015, 018–025: Link-graph repair end-to-end. `corpus/extractors/link_graph.py` (anchor extraction with extension classification, 26 unit tests, ≥90% coverage); `corpus/sources/link_graph.py` (Mongo writer keyed on `_id=url` + `read_link_graph` + bootstrap_indexes, 13 unit tests); `harness/embed_pg._prepare_from_parsed_doc` joins `link_graph` for HTML docs and emits classified `Link` rows; integration test `tests/test_embed_pg_sync_with_link_graph.py` (5 PG-touching cases); `scripts/backfill_link_graph.py` operator script. Schema comment + `_DEFAULT_LINK_TYPES`/`_DEFAULT_FOLLOW_LINK_TYPES` extended with `'file_link'` (MIGR-020); `scripts/resolve_links.py` + `RESOLVE_LINKS_BY_URL` updated for `file_link`/`page_link`. Synthetic legacy reader retired (MIGR-013): `corpus/sources/synthetic_legacy_reader.py` removed, `harness/embed_pg.sync(source='legacy')` branch dropped, `--legacy-source` CLI gone, `--source pdfs\|html` now filters `parsed_documents.content_type` and `ingest_source()` deleted; `scripts/migrate_mongo_to_parsed_documents.py` rewritten to walk Mongo directly. Operational outcomes: `link_graph` backfill over 22,743 `web_items` HTML rows → 2,279,311 anchors (298,451 file_link + 1,834,301 page_link + 146,559 external) in ~6 min; `parsed_documents` backfill → 58,121 PDF rows + 22,743 HTML rows; targeted re-sync of the 96 HTML PG seed → `file_link=786` + `page_link=9,452` + `external=704` (recovers 94% of the 836 PDF anchors the audit measured as lost); recursive-CTE traversal smoke from an HTML seed expands to a linked PDF (cornerstone proven); MIGR-011 dry-run on all 948 PG docs hash-skips with zero writes; MIGR-015 parser-swap smoke flips 5 PDFs to `llamahub_pdf_PDFReader` and back to `pymupdf4llm` (re_synced=5 each direction). docs/RETRIEVAL_PG.md §14 + DECISIONS.md "Link graph as retrieval cornerstone" entry + CLAUDE.md updates. 449 tests pass. | `corpus/extractors/link_graph.py`, `corpus/sources/link_graph.py`, `harness/embed_pg.py`, `harness/retrieve_pg.py`, `harness/pg/tools.py`, `harness/pg/queries.py`, `corpus/ingestion/link_extractor.py`, `corpus/pg_schema.sql`, `scripts/backfill_link_graph.py`, `scripts/resolve_links.py`, `scripts/migrate_mongo_to_parsed_documents.py`, `tests/test_extractors_link_graph.py`, `tests/test_sources_link_graph.py`, `tests/test_embed_pg_link_graph_unit.py`, `tests/test_embed_pg_sync_with_link_graph.py`, `tests/test_retrieve_pg_config.py`, `tests/test_embed_pg_sync.py` (removed test_5), `docs/RETRIEVAL_PG.md`, `DECISIONS.md`, `CLAUDE.md`; removed: `corpus/sources/synthetic_legacy_reader.py`, `tests/test_synthetic_legacy_reader.py`, `scripts/test_ingest_resume.py` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json), [scraper-link-extraction-audit](.claude/work/2026-05-27_18_scraper-link-extraction-audit/) |
| 2026-05-29 | Recovered the marvin-gpu environment after an OS upgrade (kernel 7.0 + system Python 3.13→3.14) broke the pending full-corpus embedding run. Rebuilt `.venv` on uv-managed Python 3.13 (torch 2.10.0+cu128, CUDA OK on the 3090; sidestepped a root-owned `ema_nlp.egg-info` left by an old `sudo pip install`). Diagnosed MongoDB as unrunnable on kernel ≥6.19 (SERVER-121912): 8.0.23 refuses to start, 8.0.4 SIGSEGVs ~1 min in, Docker can't help (shared kernel) — so the run is pending a reboot into the installed 6.17/6.8 kernel. Added a GPU-pacing knob to the pgvector sync: `embed_pg.sync(cooldown_seconds=...)` sleeps after each embedding batch (skipped when a batch is empty / all hash-skipped / dry-run, via `_flush()` now returning the embedded-chunk count), plus `--cooldown` and `--embed-batch-size` CLI flags; 4-case unit test with PG/Mongo/model fully stubbed. Run not yet executed (blocked on the reboot). | `harness/embed_pg.py`, `tests/test_embed_pg_cooldown.py` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-29 | Unblocked the MongoDB-on-kernel-7.0 problem **without a reboot** (correcting the earlier "Docker can't help" finding): the `mongo:8.0.4` *container* runs fine on kernel ≥6.19 because it bundles its own older userspace/glibc, dodging the SIGSEGV the host's Ubuntu-26.04 glibc triggers — while newer 8.0.x images (`mongo:8.0`→8.0.23) hard-refuse to start. Verified it serves the live data dir (`/var/lib/mongodb`): parsed_documents=80,083, link_graph=22,743, parsed_pdfs=65,263, web_items=115,101. Created `deploy/mongo/docker-compose.yml` (pinned 8.0.4, `--user 109:118`, bind-mount, healthcheck) + `deploy/mongo/README.md`; wrote `scripts/start_services.sh` (brings up Postgres+Mongo, health-checks, guards against a live native mongod fighting the container) and validated it end-to-end (PG data intact: 8,852 docs/42,913 chunks; pgvector 0.8.2). Documented in CLAUDE.md (Data sources + Commands), DECISIONS.md (new Infrastructure entry), docs/RETRIEVAL_PG.md (§2.3 + troubleshooting). Embedding run (71,226 pending docs) staged but not yet launched — awaiting go-ahead. | `deploy/mongo/docker-compose.yml`, `deploy/mongo/README.md`, `scripts/start_services.sh`, `CLAUDE.md`, `DECISIONS.md`, `docs/RETRIEVAL_PG.md`, `.claude/HISTORY.md` | Phase 1 | [mongo-pg-data-architecture](.claude/work/2026-05-26_17_mongo-pg-data-architecture/state.json) |
| 2026-05-30 | Started the LlamaIndex-first retrieval refactor on branch `refactor/llamaindex-retrieval-pipeline` (archive `archive/pre-llamaindex-refactor` at checkpoint `5c3c8a8`). Dropped the cross-machine database-sync work (DBSYNC-001..010) — it targeted the Mongo+Postgres architecture being removed; committed form preserved on main/archive, uncommitted parallel delta in `git stash@{0}`. | removed `scripts/sync_mongo.sh`, `scripts/sync_pg.sh`, `tests/test_{artifact_store,manifest,sync_mongo_extensions,sync_pg}.py`, `.claude/work/2026-05-30_18_database-sync/` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-001: removed the benchmark/eval suite from the refactor branch (rebuilt later on the clean API; preserved on archive). Reverse-dep-verified keep set: `judge.py`/`judges/` (review workflow), `rating.py`, `query_cache.py`, `fewshot_inject.py`, `models.yaml`, `parser_preference.yaml`, and curated data (`benchmark/`, `corpus.jsonl`). Import-clean; remaining `run_eval` mentions are comments only (reconciled in LIR-013). | removed `harness/{run_eval,eval_retrieval,compute_lift,contamination_screen,label_session}.py`, `tests/test_eval_retrieval.py`, 24 `harness/configs/*.yaml` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-002: Neo4j infrastructure for the LlamaIndex-first refactor. Installed `llama-index-graph-stores-neo4j` 0.7.0 (+ `neo4j` 5.28.4 driver); added `deploy/neo4j/` (docker-compose `neo4j:5.26` + APOC + native vector index, env-overridable ports) and rewrote `scripts/start_services.sh` to bring up Mongo + Neo4j (Postgres dropped). Refactor-in-progress banner added to CLAUDE.md. Verified: container healthy + `Neo4jPropertyGraphStore` connects (`RETURN 1`→`[{'ok':1}]`). A native neo4j held :7474/:7687, so the project container runs on alt ports 7475/7688 (`bolt://localhost:7688`); consolidation deferred to the user. | `pyproject.toml`, `deploy/neo4j/{docker-compose.yml,README.md}`, `scripts/start_services.sh`, `CLAUDE.md` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-003/004/005: built the LlamaIndex-first retrieval **foundation** in `harness/indexing/`. (003) config-driven profiles + registries with `EMA_INDEX_PROFILE` selection and `build_index`/`build_retriever` dispatch; default profile `harness/configs/index/neo4j_hier.yaml`. (004) hierarchical chunker keeping all parent/child levels (vs old `chunker.py:92` leaf-discard) with deterministic ids. (005) link extractor producing classified `links_to` edges from raw HTML — the MIGR-018..025 extractor that was documented but never shipped. 29 unit tests (16+7+6) pass; ruff clean. Builders for the Neo4j index (LIR-007) + hierarchical retriever (LIR-008) are still stubs (NotImplementedError). | `harness/indexing/{__init__,profiles,registry,chunking,links}.py`, `harness/configs/index/neo4j_hier.yaml`, `tests/test_indexing_{profiles,chunking,links}.py` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-006 + LIR-007 data-prep. (006) `harness/indexing/ingest.py`: `ingest(profile) -> list[IngestedDoc]` (entity + hierarchical chunks + `links_to`), dedup/scope filters, metadata via url/text_metadata, links from `web_items.html_raw`; 5 mongomock tests; hardened `chunking.py` (Document excluded metadata keys). (007 data-prep) Found the canonical `parsed_documents`/`link_graph` collections were **never populated** on this host (only `web_items`=115k + `parsed_pdfs`=65k) — confirming work unit 19's finding in the live data (HISTORY's "80,083 parsed_documents" was never real). Backfilled a coherent 40-doc verify subset (`scripts/backfill_parsed_documents_subset.py`: 12 HTML via trafilatura + 28 linked PDFs from `parsed_pdfs`) so the PropertyGraphIndex build can be verified on CPU. Installed `mongomock` + `trafilatura` in conda base. | `harness/indexing/ingest.py`, `harness/indexing/chunking.py`, `scripts/backfill_parsed_documents_subset.py`, `tests/test_indexing_ingest.py` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-007 + LIR-008: hierarchical PropertyGraphIndex on Neo4j + chunk-centric retriever — **CPU-subset verify GREEN**. `property_graph.py`: `to_graph()` maps the IR to `EntityNode(Document)` + `ChunkNode` + `Relation(HAS_CHUNK/PARENT_OF/LINKS_TO)`; `build_property_graph_index` embeds chunks (bge-large) + upserts to Neo4j + creates a dedicated `Chunk` vector index; `from_existing` uses MockLLM. **Spike resolved**: the default `VectorContextRetriever` only searches the `__Entity__` index, so a custom `HierarchicalPGRetriever` queries the Chunk vector index + expands `HAS_CHUNK`/`PARENT_OF` in one Cypher (small-to-big = return the parent chunk). Verified live on Neo4j (8-doc/49-chunk slice): relevant hits + `source_url` provenance for 2 queries. 2 `to_graph` unit tests; full indexing suite 36 pass; ruff clean. Full 40-doc build deferred (CPU embed ~17 min for 1462 chunks). | `harness/indexing/property_graph.py`, `harness/indexing/__init__.py`, `tests/test_indexing_property_graph.py`, `tests/test_indexing_profiles.py` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-05-30 | LIR-013 (pulled forward): reconciled the whole documentation set to the post-refactor reality. **Deleted** `docs/RETRIEVAL_PG.md` + `docs/RETRIEVAL_PIPELINE.md`; **wrote** new `docs/RETRIEVAL.md` (mermaid data/process/node-model + retrieval-sequence diagrams). **Rewrote** `docs/ARCHITECTURE.md` (Neo4j data flow + mermaid). **Updated** `CLAUDE.md` (data sources/key decisions/commands/phase), `DECISIONS.md` (new Neo4j decision + SUPERSEDED banners on pgvector/FAISS/three-layer; **NEVER-BUILT** correction on the MIGR-018..025 link-graph), `README.md`, `docs/SETUP.md` (NEO4J_* env), `docs/ONBOARDING.md`, `HARNESS_REFACTORS.md`, `deploy/postgres/README.md` (obsolete), `project_roadmap/ROADMAP.md`, and `.claude/HISTORY.md` (Corrections callout). Fixed all broken links to the removed docs; docs flag refactor-in-progress throughout (LIR-009..012 pending). | `docs/RETRIEVAL.md` (new), `docs/{ARCHITECTURE,SETUP,ONBOARDING}.md`, `CLAUDE.md`, `DECISIONS.md`, `README.md`, `HARNESS_REFACTORS.md`, `deploy/postgres/README.md`, `project_roadmap/ROADMAP.md`; removed `docs/RETRIEVAL_PG.md`, `docs/RETRIEVAL_PIPELINE.md` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-06-02 | Full-corpus GPU embedding build launched (resumable + throttled). `parsed_documents` now genuinely holds **80,083 clean docs** in the `mongo:8.0.4` container (:27017) — the scale that earlier HISTORY rows flagged as "never real" has since been backfilled into the container volume (native mongod is down; container is the live source). Added `harness/indexing/build.py` CLI and rewrote `build_property_graph_index` into a **streamed, resumable** build (embed/upsert in `--flush-chunks` flushes; `resume` skips docs with ≥1 chunk) plus a **GPU throttle** (`--pause-every-docs`/`--pause-seconds`: flush-then-sleep, crash-safe). nvidia driver reinstalled + power capped to 250W; the 60s pause every 1000 docs mitigates a GSP-wedge-under-sustained-load failure (root cause unconfirmed and **not** kernel-specific — the same crash occurs on Ubuntu 24.04, so the earlier "kernel 7.0" attribution is dropped). Verified the throttle live (49-doc/166-chunk slice, pause fired) then launched `--full --embed-device cuda --pause-every-docs 1000 --pause-seconds 60` (skips the 300 docs already in Neo4j). 47 indexing tests pass; ruff clean. Log: `logs/build_full_*.log`. | `harness/indexing/build.py`, `harness/indexing/property_graph.py`, `.claude/work/2026-05-30_20_*/state.json` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-06-02 | Leaf-only embedding fix + EPAR scope decision (work unit 21). Investigated the "all 3 hierarchical levels embedded" finding: confirmed it's **our** bug (`property_graph._embed_pass.flush()` embedded every `chunk_node`), not a `HierarchicalNodeParser` behaviour — the splitter correctly emits all levels and the canonical pattern (NVIDIA GenerativeAIExamples nb 04) embeds **leaves only**. Fixed `flush()` to embed only `is_leaf` chunks; parents are still upserted (text + `PARENT_OF`) but unembedded, so the Neo4j vector index is leaf-only (auto-excludes null-embedding nodes). Stopped the buggy all-levels run (5,463 docs in) and **wiped** the graph (1.42M nodes). Live-verified the fix on a 30-doc slice: 137/137 leaves embedded, **0 parent embeddings**, retriever returns merged parent context. **Decision (user):** EPARs are now **in scope** for the narrative retrieval corpus (amended CLAUDE.md + DECISIONS.md); the ~18k EPAR PDFs stay, so the rebuild is the full ~80k. Added `tests/test_indexing_property_graph.py::test_embed_pass_embeds_leaves_only`; 48 indexing tests + ruff green. Full leaf-only GPU rebuild launched (`--full --reset`, throttled). | `harness/indexing/property_graph.py`, `tests/test_indexing_property_graph.py`, `CLAUDE.md`, `DECISIONS.md`, `.claude/work/2026-06-02_21_leaf-only-embedding/` | Phase 1 (refactor) | [leaf-only-embedding](.claude/work/2026-06-02_21_leaf-only-embedding/state.json) |
| 2026-06-02 | LIR-009 + LIR-010: re-seamed the runtime onto the Neo4j hierarchical retriever. **LIR-009** — all 7 workflow strategies (simple_rag, crag, react, summarize_rag, crag_summarize, crag_review, react_review) now consume a LlamaIndex `BaseRetriever` via `get_workflow(retriever=...)`; each retrieve step calls `retriever.aretrieve()` → new `utils.nodes_from_retrieval()`; dropped the dual `index`/`retrieve_fn`/`RetrievalConfig` path and the `ablation_config` span attrs (replaced by `utils.retriever_attributes()`); `format_docs` + citations key on node metadata (`source_url`/`doc_id`) not `qa_id`. ReAct toolset trimmed to `ema_search` + `filter_by_topic` (the FAISS-docstore-backed `follow_cross_refs`/`get_qa_by_id` removed). **LIR-010** — `app.py`: removed the `EMA_RETRIEVER` faiss/pgvector switch; `_load_index_sync` opens the Neo4j `PropertyGraphIndex` via `EMA_INDEX_PROFILE` + `configure_embed_model`; `_build_session_workflow` builds the retriever and passes it to `get_workflow`; source cards render narrative-chunk snippets (Q:/A: parsing removed); cache citations key on `source_url`. Offline-validated: 29 workflow tests rewritten to a fake retriever (green), full 427-test collection clean, ruff clean, app.py syntax+imports resolve. Live end-to-end (UI + tracing/feedback) is LIR-011, post-build. **Deferred LIR-012** (delete old PG/FAISS stack) until after LIR-011 verifies the new path end-to-end. | `harness/workflows/{utils,registry,simple_rag,summarize_rag,crag,composites,react_native}.py`, `app.py`, `tests/test_workflows.py`, `tests/test_workflow_registry.py` | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-06-03 | Hardened the index build against pathological documents + resumed the run. The full GPU build (LOE-005) crashed at **46,864 / 80,083 docs (~59%, 3.98M leaves)** after ~15h: a single 12.5 MB EPAR RMP-summary PDF (degenerate pymupdf4llm parse) blew Python's recursion limit inside nltk's punkt tokenizer (`HierarchicalNodeParser` → SentenceSplitter), a fatal `RecursionError`. Guarded `chunk_document` to catch per-document parser failures, log `skipping <url> (<n> chars) — <error>`, and return `[]` (caller treats it as a zero-chunk doc) — recursion limit left at default so the error stays a catchable `RecursionError` rather than a C-stack segfault. Added a monkeypatched unit test; 8 chunking tests + ruff green. Relaunched in **resume mode** (no `--reset`, skips the 46,864 built docs): verified live it reached the exact offending doc, skipped it, and continued embedding on GPU (100%, 250W). | `harness/indexing/chunking.py`, `tests/test_indexing_chunking.py` | Phase 1 (refactor) | [leaf-only-embedding](.claude/work/2026-06-02_21_leaf-only-embedding/state.json) |
| 2026-06-03 | Full embed pass COMPLETE; fixed + relaunched the LINKS_TO pass. The resumed build finished embedding all docs — Neo4j now holds **79,882 Documents, 7,435,393 chunks, 5,817,230 leaves, 0 parent-embeddings** (leaf-only fix verified at full scale). But the final `LINKS_TO` edge pass then died on `pymongo.errors.CursorNotFound` (code 43): `iter_source_rows`' `find({error:""}).sort("url",1)` cursor hit Mongo's 10-min idle timeout during the slower link-extraction iteration (48,724 of N edges had landed). Fixed `iter_source_rows` to open the cursor `no_cursor_timeout=True` + `cursor.close()` in `finally`, and added a per-doc `try/except` guard in `_links_pass` (skip a bad page, don't abort). 8 ingest/property_graph tests + ruff green. Relaunched `--links-only --embed-device cpu` (idempotent MERGE over the existing edges, no re-embed, no GPU). | `harness/indexing/ingest.py`, `harness/indexing/property_graph.py` | Phase 1 (refactor) | [leaf-only-embedding](.claude/work/2026-06-02_21_leaf-only-embedding/state.json) |
| 2026-06-03 | Root-caused + fixed the LINKS_TO pass hang (missing `:Document(id)` index). After the cursor fix the links pass still hung for ~50 min at 0.4% CPU; a `sudo py-spy dump` showed it blocked in `_merge_links_batch` → Neo4j `structured_query` (bolt recv), not Mongo. Cause: the MERGE matches `MATCH (a:Document {id}), (b:Document {id})`, but Neo4j only indexes the base `__Node__`/`__Entity__` labels — so every match was a full `:Document` label scan (≈3B comparisons per 20k-pair batch → effective hang, and the original `CursorNotFound` was the *same* root cause: slow batches starved the Mongo cursor >10 min). Added `ensure_document_id_index()` (`CREATE INDEX … FOR (d:Document) ON (d.id)` + `db.awaitIndexes`) called at the top of `_links_pass`, plus per-5k-doc progress logging. Re-ran `--links-only`: `LINKS_TO` went 48,724 → 865,785 in ~90s (was frozen 50 min). ruff green. | `harness/indexing/property_graph.py` | Phase 1 (refactor) | [leaf-only-embedding](.claude/work/2026-06-02_21_leaf-only-embedding/state.json) |
| 2026-06-03 | LIR-012 (+ LIR-011 headless verify): deleted the dead PG/FAISS stack now that the Neo4j path is built + proven. Removed 37 files/dirs — `harness/{embed,embed_pg,embed_hierarchical,retrieve,retrieve_pg}.py`, `harness/pg/`, `harness/ablations/{a2,a3,a4}`, `corpus/pg_schema.sql`, `corpus/sources/synthetic_legacy_reader.py`, `scripts/{init_db,resolve_links,test_ingest_resume,migrate_mongo_to_parsed_documents}.py`, `deploy/postgres/`, top-level `ablations/B_process_rewards/`, and 10 PG/embed test files. **Kept** `a1_query_expansion` (used by ReAct) + its `ablations/A_evidence_filter/acronym_dict.yaml` (over-deleted then restored). Fixed refs: `config.py` (dropped `PG_DSN`/`EMA_RETRIEVER`), `crag.py`/`corpus` docstrings, trimmed `test_ablations.py` to A1-only, `test_span_attributes.py` (dropped the ablation_config test; `EMA_RETRIEVER`→`EMA_INDEX_PROFILE`). Runtime path imports clean; **317 tests pass** (2 skipped). **LIR-011 (headless)**: end-to-end `get_workflow("simple_rag", retriever=…).invoke()` on the complete 79,882-doc graph → answer + 10 docs all with `source_url` citations, span stamped `ema.index.profile=neo4j_hier`; retriever smoke (3 queries, 0.90–0.93); span-stamping + query_cache + fewshot tests green. The live Chainlit click-through (Phoenix spans in-browser + 👍/👎 annotation) remains the user's manual step (chainlit not in `.venv`). | `harness/`, `corpus/`, `config.py`, `scripts/`, `deploy/postgres/`, `ablations/`, `tests/` (37 deletions + ref fixes) | Phase 1 (refactor) | [llamaindex-retrieval-refactor](.claude/work/2026-05-30_20_llamaindex-retrieval-refactor/state.json) |
| 2026-06-04 | Specced three new retrieval tracks + a comparison harness over the existing `harness/indexing` registry/profile seam — **spec only, no code** (`docs/RETRIEVAL_TRACKS.md`). **Track A** `vector_flat`: plain `VectorStoreIndex`/FAISS over the *same* leaf chunks the PG path embeds (the control arm), zero-re-embed by pulling `:Chunk.embedding` from Neo4j. **Track B** `hierarchical_links`: vector seed → `LINKS_TO` 1-hop expansion at `:Document`, finally consuming the dead `graph.max_hops`/`edge_types` config + an edge-hygiene filter. **Track C** `property_graph_native`: idiomatic native `PropertyGraphIndex` learning track (`SimplePropertyGraphStore`/Neo4j, `link_closure` subgraph scoping). **Harness** `retrieval_eval.py`: recall@k/MRR/linked-doc-recall vs `benchmark.jsonl`, per T1–T4, Phoenix-traced, no LLM judge. Produced via a 12-agent verify→design→adversarially-review workflow that read the *installed* llama-index/neo4j/faiss source. Adversarial pass caught load-bearing bugs folded into the spec: Track A's Neo4j embedding pull is silently nulled by `value_sanitize()` (lists ≥128 dropped) → needs a raw driver; Track C's "pure-native zero-Cypher" headline is unbuildable (FaissVectorStore returns positional ids, native sub-retrievers emit triplet nodes, `LINKS_TO` doc→doc needs `path_depth≥3`, `get_rel_map` has no edge-type filter); surfaced a shared P0 (hardcoded `open_index` is not registry-dispatched). Empirical input for Track B: top-100 `LINKS_TO` targets absorb 94.6% of 1.72M edges (global nav boilerplate). | `docs/RETRIEVAL_TRACKS.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-04 | `/workflow:explore` for the **link-extraction upgrade** prerequisite (work unit 24): investigated `docs/RETRIEVAL_TRACKS.md`'s LINKS_TO hygiene finding and the proven `../ema_scraper` extractor, then folded the prerequisite into the spec. **Quantified the chrome (live Neo4j):** 1,721,581 `LINKS_TO` over 21,957 source pages / 61,114 targets; **74 targets linked from >5% of pages absorb 94.4% of edges** (global header/footer/mega-menu nav); content remainder ≈96,837 edges; only 3.4% point to PDFs. **De-risked the port:** `web_items.html_raw`=22,743 pages; sampled 40/40 have `<main class="main-content-wrapper">`, 21/40 have `bcl-file` cards, main-content scoping keeps only 36.7% of anchors. **Read the proven extractor** (`ema_scraper/parsers/ema_parser.py` `EmaPageParser` + `data_classes.py`): main-content scoping, skips `bcl-inpage-navigation`/`breadcrumb`/`dropdown-menu`/`nav`, BCL-component-aware (`bcl-file` → `data-ema-document-type`/ref/format); its `Link` stores only text+href so context must be stamped at extraction. Created work-unit deliverables (metadata/requirements/exploration/state.json, 8 tasks LEU-001..008) and updated the spec: new **§0.8 Prerequisite P1** (port + edge model = single `LINKS_TO` label + `{kind,link_context,document_type,anchor}` properties + delete-and-re-MERGE edges only, no chunk/vector touch), reframed §0.4, **demoted Track B's `is_nav_hub` degree-cap to a secondary safety-net** (P1 fixes hygiene structurally) with `graph.edge_types` generalized to a `link_context` filter, Track C inherits via the IR, P1 inserted into the sequencing before B. Spec only — no code. | `docs/RETRIEVAL_TRACKS.md`, `.claude/work/2026-06-04_24_link-extraction-upgrade/`, `.claude/HISTORY.md` | Phase 1 (refactor) | [link-extraction-upgrade](.claude/work/2026-06-04_24_link-extraction-upgrade/state.json) |
| 2026-06-04 | `/workflow:plan` for the link-extraction upgrade (work unit 24): turned the explore output into a 7-task implementation plan (`implementation-plan.md` + `state.json`, planning_complete). Locked 7 decisions per user direction (no back-compat, embedding runtime irrelevant): hard-replace `extract_links`; strict main-only (no fallback); single `LINKS_TO` label + `{kind,link_context,document_type,anchor}` properties; **add explicit `link_contexts`/`document_types` profile fields** (keep `edge_types`); dedup richest-context-wins; **native `CALL{} IN TRANSACTIONS OF 50000` edge-only rebuild** (empirically verified `structured_query` is autocommit; `Relation.properties` round-trips; apoc 5.26.26 fallback). Ran an adversarial Plan-agent review against the real code that caught and folded in 8 amendments (A1–A8): port the **recursive `_parse_children` walk + processed-set + accordion descent** (a flat scan would drop links nested in divs/accordions), `bcl-file` anchor = file-title, use `html.parser` for scraper parity, `_merge_links_batch` param-shape + `_clean(None)`, a real main-presence diagnostic signal, validate `link_contexts` membership, and reconcile the DL4-vs-FR6 contradiction. Critical path 001→003→005→006→007 (~19h); TASK-004 parallelizable. Spec/plan only — no code. | `.claude/work/2026-06-04_24_link-extraction-upgrade/{implementation-plan.md,state.json,requirements.md}`, `.claude/HISTORY.md` | Phase 1 (refactor) | [link-extraction-upgrade](.claude/work/2026-06-04_24_link-extraction-upgrade/state.json) |
| 2026-06-04 | `/workflow:next` implemented + **shipped** the link-extraction upgrade (work unit 24, TASK-001..007). Ported `ema_scraper`'s `EmaPageParser` into `harness/indexing/links.py`: main-content scoping (`<main class="main-content-wrapper">`), recursive walk + `id()` processed-set, BCL-component awareness (`bcl-file`→`file_component`+`document_type`, listing/card→`card_or_listing`, inline/accordion→`inline`, standalone→`other`), `html.parser` for scraper parity; `ExtractedLink` gained `link_context`+`document_type` (kept `kind`/`anchor` + URL normalization). Stamped `{kind,link_context,document_type,anchor}` as `LINKS_TO` **properties** in both `to_graph` and the live `_links_pass`/`_merge_links_batch`; added `GraphRetrievalConfig.link_contexts`+`document_types` (validated; for Track B); added `reset_links` delete-and-re-MERGE (`build --links-only --reset-links`, native `CALL{} IN TRANSACTIONS`, no chunk/vector touch) + a no-main diagnostic. **Live rebuild executed** (~8.5 min, CPU): `LINKS_TO` **1,721,581→99,520** (−94.2%), chunks/HAS_CHUNK/PARENT_OF unchanged, **0** chrome targets (max in-degree 21,956→567), HTML→PDF card share **3.4%→58.3%**, 54,347 file_component edges carry `document_type`. Rewrote `tests/test_indexing_links.py` + fixed `test_indexing_ingest.py`/`test_indexing_property_graph.py`/`test_indexing_profiles.py`; **331 passed / 2 skipped**, `links.py` ruff+mypy clean. Docs: `DECISIONS.md` entry, `docs/RETRIEVAL.md` code map, `docs/RETRIEVAL_TRACKS.md` §0.8 result. | `harness/indexing/{links,property_graph,profiles,build}.py`, `tests/test_indexing_{links,ingest,property_graph,profiles}.py`, `DECISIONS.md`, `docs/RETRIEVAL.md`, `docs/RETRIEVAL_TRACKS.md`, `.claude/work/2026-06-04_24_link-extraction-upgrade/` | Phase 1 (refactor) | [link-extraction-upgrade](.claude/work/2026-06-04_24_link-extraction-upgrade/state.json) |
| 2026-06-04 | `/workflow:explore` for Chainlit strategy selection (work unit 25). Mapped the app's TWO config axes: orchestration **workflow** (7 registered: `simple_rag`, `react`, `crag`, `summarize_rag`, `crag_summarize`, `crag_review`, `react_review`; + prompt strategies `zero_shot`/`few_shot`/`cot_self`, which apply to all but `react`/`react_review`) vs **retrieval** (1 built: `hierarchical` over `property_graph`/`neo4j_hier`; `vector_flat`/`hierarchical_links`/`pg_native` are spec-only). Found the workflow is selected via `cl.ChatProfile` at chat-start (fixed per session), NOT the settings panel; `_build_session_workflow` already takes `strategy`+`prompt_strategy`, so the feature is app.py UI plumbing only. **Delivered `docs/WORKFLOWS.md`** (#3): two-axis model, implemented-strategies inventory, and step-by-step recipes to add custom orchestration workflows / prompt strategies / retrieval strategies + the `WorkflowRunner`/`config_attributes` contract. Wrote work-unit requirements/exploration + a 5-task plan outline for the settings-panel selector (#1). Explore/docs only — no app code changed. | `docs/WORKFLOWS.md`, `.claude/work/2026-06-04_25_chainlit-strategy-selector/`, `.claude/HISTORY.md` | Phase 1 (refactor) | [chainlit-strategy-selector](.claude/work/2026-06-04_25_chainlit-strategy-selector/state.json) |
| 2026-06-04 | `/workflow:explore` — extended work unit 25 to a **whole-documentation refresh** (per user). Ran an 11-agent parallel staleness audit of every doc against the current codebase; captured per-doc verdicts + concrete fixes in the work unit's `audit-findings.md`. Findings: **9 docs stale** from the retrieval refactor (LIR-001..012) + the 2026-06-04 link upgrade — recurring themes: pgvector/FAISS/`EMA_RETRIEVER`/`retrieve*.py` shown as current (all deleted), "re-seam pending / CPU subset" (done), `LINKS_TO` 1.72M (now 99,520), `run_eval`/eval-suite + "9 workflows" (archived; 7). Verdicts: major-rewrite = chainlit.md, ONBOARDING.md, SETUP.md, HARNESS_REFACTORS.md, ROADMAP.md (delete the pgvector Phase 1.7); minor-patch = CLAUDE.md, README.md, RETRIEVAL.md, ARCHITECTURE.md, OPEN_QUESTIONS.md; current/leave = WORKFLOWS.md, RETRIEVAL_TRACKS.md, corpus_stats.md, GLOSSARY/LEAKAGE. Added DOC-001..011 to `state.json` (~14h docs + the 5 chainlit-feature tasks). Explore/plan only — no doc edits executed yet. | `.claude/work/2026-06-04_25_chainlit-strategy-selector/{audit-findings.md,metadata.json,requirements.md,state.json}`, `.claude/HISTORY.md` | Phase 1 (refactor) | [chainlit-strategy-selector](.claude/work/2026-06-04_25_chainlit-strategy-selector/state.json) |
| 2026-06-04 | `/workflow:next` — executed the whole-documentation refresh (work unit 25, DOC-001..011) via an 11-agent parallel apply over the staleness-audit fix lists. Updated 12 docs to the post-refactor + post-link-upgrade reality: **README.md** + **CLAUDE.md** banners flipped to "refactor complete"; **chainlit.md** rewritten (BM25/Q&A → Neo4j PropertyGraphIndex hierarchical); **docs/ARCHITECTURE.md** ("pending" LIR annotations removed, live-graph stats added); **docs/ONBOARDING.md** major rewrite (new data flow, file map, 7 workflows, archived-suite callout, real commands); **docs/SETUP.md** (sync_mongo.sh gone, PHOENIX_COLLECTOR_ENDPOINT→PHOENIX_URL, app.py not providers.py); **HARNESS_REFACTORS.md** (Change 2 collapsed to [SUPERSEDED]); **project_roadmap/ROADMAP.md** (deleted the pgvector Phase 1.7 → archived note; fixed "No Neo4j" non-goal + v2+ deferrals); **docs/RETRIEVAL.md** status box + mermaid + data-note; **OPEN_QUESTIONS.md** (embedding-model Q resolved); **ABLATIONS.md**/**benchmark/SCHEMA.md** sweep. Then hand-fixed the stragglers my narrow task-scoping left (CLAUDE.md body lines 28/30/47/51/55/57; RETRIEVAL.md diagram + data note; ARCHITECTURE §5 LIR-011). Verified: docs-only (no .py touched), repo-wide stale-term grep clean (no live pgvector/CPU-subset/1.72M/run_eval), no broken doc links. WORKFLOWS.md + RETRIEVAL_TRACKS.md left as already-current. | `CLAUDE.md`, `README.md`, `chainlit.md`, `HARNESS_REFACTORS.md`, `OPEN_QUESTIONS.md`, `docs/{ARCHITECTURE,ONBOARDING,SETUP,RETRIEVAL}.md`, `project_roadmap/{ROADMAP,ABLATIONS}.md`, `benchmark/SCHEMA.md`, `.claude/work/2026-06-04_25_chainlit-strategy-selector/`, `.claude/HISTORY.md` | Phase 1 (refactor) | [chainlit-strategy-selector](.claude/work/2026-06-04_25_chainlit-strategy-selector/state.json) |
| 2026-06-04 | `/workflow:plan` — planned the **Chainlit dynamic strategy selector** (work unit 25, Thread B), superseding the earlier hardcoded outline (TASK-002..005) with CSEL-001..007. Core insight per the user's "pick up new strategies" ask: the settings panel is **registry-driven at render time** (`list_workflows()` / `list_prompt_strategies()` / `glob configs/index/*.yaml`) so new workflows/prompts/profiles auto-appear with no app.py edit. Two layers: L1 = dynamic workflow+prompt Selects (live-switchable); L2 = index_profile axis (needs the registry `open_index` dispatch / RETRIEVAL_TRACKS §0.7 P0). Verified the design against chainlit 2.11 source + the registries and **caught a latent bug** (passing `prompt_strategy` to `react`/`react_review` → TypeError, since their builders lack `**kwargs`). Ran a 2-agent adversarial review (plan-correctness=needs-revision, chainlit-runtime=solid) and folded 7 amendments (§3b A1-A7): `EMA_INDEX_PROFILE` is read at invoke-time in `utils.py:84/174` (tracing) → env-sync needed; do NOT rename `property_graph.open_index` (5 importers); auto-appear contract; guard BGE re-config on profile switch (GPU risk); retain `_PROFILE_STRATEGY`; mid-session re-render resets all widgets → seed every widget; ChatProfile switch = full session reset. Plan/state only — no code. | `.claude/work/2026-06-04_25_chainlit-strategy-selector/{implementation-plan.md,state.json}`, `.claude/HISTORY.md` | Phase 1 (refactor) | [chainlit-strategy-selector](.claude/work/2026-06-04_25_chainlit-strategy-selector/state.json) |
| 2026-06-04 | `/workflow:next` — implemented + shipped the **Chainlit dynamic strategy selector** (work unit 25, CSEL-001..007). The settings panel now selects **workflow + prompt strategy + retrieval profile**, all listed **dynamically from the registries** (`list_workflows()` / new `list_prompt_strategies()` / `glob configs/index/*.yaml`) so new strategies appear with no app.py edit. CSEL-001: `list_prompt_strategies()` + made `build_react_native`/`build_react_review` accept `**_` (fixes the confirmed `prompt_strategy` TypeError). CSEL-002: registry-level `open_index` dispatch (`OPEN_BUILDERS`/`register_open`/`open_index`; `@register_open("property_graph")`; exported) — the §0.7 P0, without renaming `property_graph.open_index`. CSEL-003/004/005/007 (app.py): `_chat_options()` + dynamic `_make_chat_settings(current)` seeding every widget; `_settings_to_pipeline_kwargs` maps the selectors; `on_settings_update` rebuilds the pipeline + reloads the index only when the profile changes; ChatProfile seeds, panel is live source of truth; `EMA_INDEX_PROFILE` env synced (after a successful open) so invoke-time tracing reports the live profile. Adversarial code review caught + fixed one bug (env was set before the load could fail). 11 new tests; **342 passed / 2 skipped**; ruff clean; mypy-neutral. Docs: app.py docstring, chainlit.md, WORKFLOWS.md. | `app.py`, `harness/workflows/{utils,react_native,composites}.py`, `harness/indexing/{registry,property_graph,__init__}.py`, `tests/test_{chat_settings,indexing_open}.py`, `chainlit.md`, `docs/WORKFLOWS.md`, `.claude/work/2026-06-04_25_chainlit-strategy-selector/` | Phase 1 (refactor) | [chainlit-strategy-selector](.claude/work/2026-06-04_25_chainlit-strategy-selector/state.json) |
| 2026-06-17 | Wrote a target-architecture design note for the agentic-RAG evolution (design only, no code): **MLflow** (judge-alignment as the reward signal; `log_model` intentionally unused) replacing Phoenix; **`FunctionAgent` + `FunctionTool`** orchestration retiring the hand-rolled per-step ReAct; **Pydantic** answer/citation output; **native-first** LlamaIndex retrieval (`kg_extractors`/`sub_retrievers`/`node_postprocessors`) with the chunk-seed `HierarchicalPGRetriever` kept as the one justified `CustomPGRetriever` (LIR-007 already proved `VectorContextRetriever` can't drive the `:Chunk` index); **two-layer graph** (structural `LINKS_TO` switched on now + deferred semantic ontology via an idempotent `enrich_ontology` button, `SchemaLLMPathExtractor` + a custom IDMP-linking `TransformComponent`); config-driven query expansion (agent optional) + dual reranker (cross-encoder + LLM); DSPy bootstrap loop (teacher exemplars, judge-filtered); and a no-silent-modes transparency rule (resolved config echoed + stamped on every trace). | `docs/TARGET_ARCHITECTURE.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Implemented the **Phase-1 foundation** of the target architecture on a new branch `claude/agentic-rag-foundation` — additive (no deletions; existing workflow/Phoenix stack untouched), fully unit-tested offline. **`harness/schemas/`**: Pydantic `RegulatoryAnswer`/`Claim`/`Citation` (+ duck-typed `citation_from_node`) and `Substance` → structured, cited output (aims 2 & 4), zero LlamaIndex dep. **`harness/tools/`**: a `register_tool`/`get_tool`/`build_tools` registry + `ema_search` (wraps any `BaseRetriever`) + `resolve_substance` (PubChem normalization behind an injectable fetcher, pure `parse_pubchem`) → the FunctionTool seam (aim 3). **`harness/agents/`**: config loader + `build_regulatory_agent` constructing a native LlamaIndex `FunctionAgent` with `output_cls=RegulatoryAnswer`, and a `build_agent` that wires tools/prompt/schema from `configs/agent/regulatory.yaml` (aims 2 & 3, config-driven). **`harness/obs/config_attrs.py`**: resolved-config → `ema.*` attrs + best-effort OTel stamping + echo = the no-silent-modes transparency rule (aim 4). **`harness/ontology/schema.py`** + `configs/ontology/ema.yaml`: typed entity/relation schema for a future `SchemaLLMPathExtractor` (extraction deferred). 5 test modules, **29 tests pass**, ruff + mypy clean (ad-hoc deps: llama-index-core 0.14, pydantic 2, pyyaml). Infra-gated phases (MLflow tracing/judge swap, Neo4j-native retrieval composition, ontology extraction, DSPy bootstrap) deliberately NOT built — they require Neo4j/Mongo/GPU/API keys/MLflow server absent from this container. | `harness/{schemas,tools,obs,agents}/`, `harness/ontology/schema.py`, `harness/configs/agent/regulatory.yaml`, `harness/configs/ontology/ema.yaml`, `harness/prompts/agent_regulatory.md`, `tests/test_{schemas,tools,obs_config_attrs,agents,ontology_schema}.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Phase-1 continued (branch `claude/agentic-rag-foundation`): added the **config-driven retrieval pipeline seam** `harness/retrieval/` — registries for query transforms (`none`/`acronym` deterministic + `llm_rewrite` lazy/runtime) and node-postprocessor rerankers (`cross_encoder`/`llm_sme` registered behind lazy imports so tests need no torch), a `RetrievalPipelineConfig` loaded from `configs/retrieval/native.yaml` with `resolved_attributes()` (→ `ema.retrieval.*`, explicit `graph_mode`, no silent defaults), and `run_retrieval()` (transform → multi-query dedupe → rerank, retriever-agnostic). Kept as a new top-level package (not under `harness/indexing/`) so it imports without dragging Neo4j. 14 new tests (43 total across the new suites), ruff + mypy clean. Real rerankers / LLM transforms are exercised at runtime, not in tests. | `harness/retrieval/`, `harness/configs/retrieval/native.yaml`, `tests/test_retrieval_pipeline.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Phase-1 integration (branch `claude/agentic-rag-foundation`): wired the seams together. `ema_search` now runs the config-driven retrieval pipeline (query transform → multi-query dedupe → rerank via `harness.retrieval.run_retrieval`) when a `transform`/`postprocessors` are supplied (forwarded transparently through `build_agent`'s tool kwargs), else a plain retrieve. Added `harness/agents/runner.py`: `coerce_answer` (robustly maps a FunctionAgent result — native `structured_response` model/dict, `.response` text, or a plain string — to `RegulatoryAnswer`, with citations from fallback nodes) + `arun_agent`/`run_agent` drivers that stamp the resolved retrieval config on the trace (transparency) and call `agent.run(user_msg=...)`. 11 new tests (**54 total** across the new suites), ruff + mypy clean. Live LLM / Neo4j paths remain runtime-only. | `harness/tools/search.py`, `harness/agents/runner.py`, `harness/agents/__init__.py`, `tests/test_search_pipeline.py`, `tests/test_agent_runner.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Phase-1 MLflow run-recording (branch `claude/agentic-rag-foundation`): added `harness/obs/runs.py` — **local, server-free** MLflow tracking (default `file:./mlruns`, opting out of the mlflow≥3 file-store maintenance exception via `MLFLOW_ALLOW_FILE_STORE`; switchable to sqlite/server). Provides `setup_mlflow`, a `record_run` context manager, `RunHandle` (param/metric/text logging), `answer_metrics`, and `record_answer_run` (logs resolved-config params + answer metrics + the answer-text artifact). Reproducibility **without `log_model`**; degrades to no-ops if mlflow is absent. Added `mlflow>=3.0` to deps; made `stamp_current_span` `getattr`-based (mlflow pulls in opentelemetry, which surfaced a mypy attr-defined error). 5 new tests against a temp file store, read back via `MlflowClient` (no pandas/server). **59 total** across the new suites; ruff + mypy clean. | `harness/obs/runs.py`, `harness/obs/{__init__,config_attrs}.py`, `pyproject.toml`, `tests/test_obs_runs.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Phase-2 infra-gated scaffolding (branch `claude/agentic-rag-foundation`, part 1) — written for live debugging on the GPU host per user direction; pure/seam logic unit-tested offline, heavy paths lazy. Added `harness/obs/tracing.py` (MLflow `setup_tracing` + LlamaIndex autolog guard + `traced` span ctx), `harness/retrieval/native_pg.py` (native `VectorContextRetriever`/`CypherTemplateRetriever` composition via `index.as_retriever`; signatures verified vs llama-index-core), `harness/ontology/enrich.py` + CLI (`OntologySchema`→`SchemaLLMPathExtractor` mapping + dry-run plan tested; runtime extraction lazy), `harness/agents/session.py` (`assemble_agent` pure wiring tested + `build_session`/`AgentSession` runtime onto the existing `open_index`/`build_retriever`, `ema_search` bound to the pipeline, optional MLflow recording), and `scripts/run_agent_demo.py`. 13 new tests (**70 total**); ruff clean; mypy clean on the new modules (10 pre-existing errors remain in untouched `harness/indexing/*` etc. from llama-index 0.14 version skew in the sandbox — out of scope). | `harness/obs/tracing.py`, `harness/retrieval/native_pg.py`, `harness/ontology/enrich.py`, `harness/agents/session.py`, `harness/{obs,retrieval,agents}/__init__.py`, `scripts/run_agent_demo.py`, `tests/test_{obs_tracing,retrieval_native_pg,ontology_enrich,agent_session}.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-17 | Phase-2 infra-gated scaffolding (branch `claude/agentic-rag-foundation`, part 2) — the aim-1 reward/optimizer layer `harness/eval/`. `predict.py` (`build_predict_fn`: AgentSession/callable → mlflow.genai predict_fn; tested), `judges.py` (`make_judge` wrappers from `harness/judges/*.md` + `ema_judges` + `align_judge`; lazy mlflow.genai), `evaluate.py` (`run_evaluation` wraps `mlflow.genai.evaluate`; lazy), `bootstrap.py` (teacher → **judge-filter** → DSPy `BootstrapFewShot`: `judge_filter` + `generate_exemplars` tested with a fake teacher/judge; `compile_fewshot` lazy dspy). 7 new tests (**77 total** across the new suites); ruff + mypy clean on the new modules. `mlflow.genai` API verified present in-sandbox; DSPy lazy (not installed). | `harness/eval/`, `tests/test_eval_{predict,bootstrap}.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-18 | Documentation audit + refresh across the live docs (3 parallel read-only audits vs the codebase). **Corrected genuine staleness:** `deploy/mongo/README.md` (described the deleted Postgres/`embed_pg` pipeline → Neo4j is the retrieval target; dropped a non-existent `link_graph` count), `CLAUDE.md` (Neo4j was listed as "deferred to v2+" though it's the live store; "no DSPy" → scaffolded; added an agentic-layer banner), `docs/ARCHITECTURE.md` ("never backfilled at scale" → 80,083 backfilled / 79,882 indexed; + new §4a agentic layer), `DECISIONS.md` (LIR-009/010/012 "pending" → shipped; `start_services` "Postgres" → Neo4j; removed a stale `link_graph 22,743` count), `docs/TARGET_ARCHITECTURE.md` banner ("not implemented" → foundation implemented), and `project_roadmap/{README_OUTLINE,CLAUDE_CODE_SETUP}.md` (broken `run_eval`/`black` commands → current). **Added** mermaid data-flow diagrams (README overview + WORKFLOWS two-axis) and agentic-layer cross-references (README/CLAUDE/ARCHITECTURE/WORKFLOWS → `docs/TARGET_ARCHITECTURE.md`). Reviewed and left as-is: plan/aspirational docs (ROADMAP/ABLATIONS/BLOG_OUTLINE/GLOSSARY/LEAKAGE), benchmark/corpus stats, and the prompt/judge content files; `.claude/{work,transitions}/` logs untouched (historical record). | `README.md`, `CLAUDE.md`, `DECISIONS.md`, `docs/{ARCHITECTURE,WORKFLOWS,TARGET_ARCHITECTURE}.md`, `deploy/mongo/README.md`, `project_roadmap/{README_OUTLINE,CLAUDE_CODE_SETUP}.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-18 | Clarified tracing-backend wording in `docs/WORKFLOWS.md` after a Phoenix-vs-MLflow confusion. Neutralized "Phoenix span per step" → "trace span per step" (the per-step ReAct design is tracer-agnostic) and added an explicit **Tracing backend** note: the live workflows + `app.py` use **Arize Phoenix** today; **MLflow is the agentic layer's additive *target*, not a completed migration**. No code change — Phoenix is still the live tracer by design. | `docs/WORKFLOWS.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-18 | Added `docs/RUNTIME_VERIFICATION.md` — a self-contained kickoff runbook for a **fresh Claude Code session on the GPU host** to catch up (CLAUDE.md → TARGET_ARCHITECTURE.md → HISTORY.md → PR #46) and verify the agentic layer on real infra in task order (T1 offline tests → T2 agent demo → T3 MLflow autolog → T4 ontology enrich → T5 judges/eval → T6 optional app wiring), with guardrails (additive, branch discipline, don't rebuild the Neo4j index). Doc only. | `docs/RUNTIME_VERIFICATION.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-21 | **T1 runtime verification on the GPU host** (`marvin-gpu`, branch `claude/agentic-rag-foundation`) — installed `mlflow==3.14.0` via `uv pip install -e ".[dev]"` (was absent; `mlflow>=3.0` is a core dep that hadn't been installed in this venv), llama-index-core unchanged at 0.14.22. Ran the full offline agentic suite: **77 passed**. Fixed one stale CI-only assumption — `test_obs_tracing.py::test_enable_llama_index_autolog_absent_returns_false` hardcoded `is False` ("not installed in CI"), but `mlflow.llama_index` ships in mlflow core and only needs `llama-index` present (already installed here), so autolog enables and the fn correctly returns `True`. Rewrote the test to assert the **contract** (returns True iff the integration is importable) — env-agnostic, passes in both CI and on a host with llama-index. Production `tracing.py` untouched (its behaviour was already correct). Good sign for T3: autolog is available without a separate `mlflow[llama-index]` install. | `tests/test_obs_tracing.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **T2 runtime verification — agent end-to-end works + fixed a latent MLflow-recording defect** (GPU host, branch `claude/agentic-rag-foundation`). Confirmed Neo4j graph intact (§4 smoke test: 9 hits on "nitrosamine acceptable intake limit", no rebuild). Ran the agent on "What is the Acceptable Intake for NDMA?" → correct `RegulatoryAnswer` ("EMA AI for NDMA = 96.0 ng/day", BMDL10/sartans-Article-31 derivation), confidence 0.9, 4–5 claims, 3 citations incl. the real EMA referral PDFs + a PubChem link → proves **both** tools fired (`ema_search` + `resolve_substance`), `output_cls` structured output populated, `agent.run(user_msg=…)` correct, LLM via gateway, `bge-reranker-large` downloaded fine. **Bug found+fixed:** `AgentSession.arun(record=True)` called `record_answer_run` without guaranteeing an MLflow backend; with `enable_tracing=False` (no prior `setup_tracing`) this hit mlflow≥3's default file store, which **raises** ("maintenance mode") unless `MLFLOW_ALLOW_FILE_STORE` is set — or silently misroutes runs to a stray `sqlite:///mlflow.db`/`Default` experiment. Fix: `arun` now calls `setup_mlflow(self.experiment or "ema_nlp")` before recording (sets the flag + pins to `file:./mlruns`, idempotent if tracing already set up). Re-run confirmed: run `t2_clean` lands in `file:./mlruns` experiment `ema_nlp` with params+metrics+`answer.txt`. Added a regression test (record=True configures a backend & doesn't crash, via `monkeypatch.chdir`) and gitignored the local `mlruns/`+`mlflow.db` store. Offline suite still green; ruff clean; mypy clean on `session.py` (the 11 `harness/indexing/*` errors are pre-existing llama-index version-skew, per runbook). | `harness/agents/session.py`, `tests/test_agent_session.py`, `.gitignore`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **T3 runtime verification — MLflow autolog traces COMPLETE** (no code change; GPU host). Ran the agent with `enable_tracing=True` (autolog on); `enable_llama_index_autolog()` returns True without a separate `mlflow[llama-index]` install. The autolog trace persisted to `file:./mlruns`/`ema_nlp` with **`state=TraceState.OK`** — the mlflow#13352 "stuck In-Progress" hang on Workflow-based `FunctionAgent` **did not manifest** (mlflow 3.14 + llama-index 0.14); program exited 0, no hang, so the explicit `traced()` fallback is unneeded. Two non-blocking warnings (both from the **live retriever path** → out of scope per guardrails, neither blocks trace completion, recorded for follow-up): (1) `Failed to end span … 'MockValSer' object is not an instance of 'SchemaSerializer'` — the `HierarchicalPGRetriever`'s `MockLLM` serialized into a sub-span; (2) `PydanticSerializationUnexpectedValue` on `NodeWithScore.score` (numpy float32 vs float). Decision: spike-1 (§8) resolved → autolog is usable as-is; keep `traced()` as the documented fallback. | `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **T4 runtime verification — ontology Layer-2 enrichment now works end-to-end** (GPU host); fixed 3 runtime defects in `harness/ontology/enrich.py`. Dry-run was already fine (7 entities / 20 relations / 10 validation triples). **(1) Construction** — `SchemaLLMPathExtractor` (llama-index 0.14) types `possible_entities`/`possible_relations` as `Literal` *types*, not lists; passing lists raised `PydanticSchemaGenerationError`. `build_schema_extractor` now adapts the readable lists from `schema_extractor_kwargs` to `Literal[...]` + `(s,r,o)` tuples (kept list-shaped in the dry-run plan/tests). **(2) Empty extraction** — `strict=True` silently dropped *every* triple: llama-index upper-cases extracted entity labels (`SUBSTANCE`) but the schema/validation triples were Title-Case (`Substance`) → no match. Fix: upper-case entity *types* in `possible_entities` + `kg_validation_schema` at the boundary (keeps the owner's strict-precision design; `configs/ontology/ema.yaml` stays Title-Case source of truth; Neo4j labels are `:SUBSTANCE`). Verified: 1 real nitrosamine chunk → 4 typed entities written to Neo4j (SUBSTANCE NDMA, LIMIT "AI 96.0 ng/day", GUIDELINE "ICH M7"), then **cleaned up** (surgical delete by `triplet_source_id`, graph restored). strict=False yields broader (16 ent/8 rel) if higher recall is wanted. **(3) Scope cap** — `_scoped_chunk_nodes` inherited the profile's ingest `scope.limit=50`, so `--scope nitrosamines` matched 0 docs; now drops that cap for keyword scopes (bounded by `--limit`), finding nitrosamine docs (6 chunks/doc in 13s). Added a `build_schema_extractor` construction test. Offline ontology tests pass; ruff clean; mypy only the pre-existing untyped-`client` error. Note: extraction quality is fine with the default `grader`/haiku given the fix, opus richer; full-scope enrichment deferred to owner (it's a long scan). | `harness/ontology/enrich.py`, `tests/test_ontology_enrich.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **T5 runtime verification — MLflow judges + `genai.evaluate` work end-to-end** (GPU host); fixed 2 eval-layer integration gaps in `harness/eval/judges.py`. **(1) Prompt variables** — `mlflow.genai.make_judge` only allows reserved vars (`inputs/outputs/expectations/trace/conversation`), but `harness/judges/*.md` use `{{question}}/{{context}}/{{answer}}/{{gold_answer}}` (rejected with `MlflowException: unsupported variables`). Those `.md` files are **shared with the legacy `harness.judge`** (string-templated), so instead of editing them, added `to_mlflow_instructions()` to map the custom vars onto the reserved set at build time (question/context→inputs, answer→outputs, gold_answer→expectations). **(2) Gateway routing** — judges hit "invalid x-api-key" (no base_url → real api.anthropic.com with the gateway key) or "Not found" (base_url=host → bare host). mlflow's judge gateway adapter uses `base_url` *verbatim* as the endpoint, so `_anthropic_judge_base_url()` now derives the full `…/v1/messages` URL from `ANTHROPIC_BASE_URL` (matching the agent's working route); falls back to the default endpoint when no gateway is set. Verified: `ema_judges` builds both judges; correctness→5 and faithfulness→5 with sensible rationales (and the correctness judge correctly read "AI" as Acceptable Intake); `mlflow.genai.evaluate` completed over a tiny dataset on the file store; `judge.align` API present (alignment deferred until ≥10 paired labels per §8). Added offline mapping/routing tests (`tests/test_eval_judges.py`, 3); full offline eval suite 10 passed; ruff + mypy clean on `judges.py`. | `harness/eval/judges.py`, `tests/test_eval_judges.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **T6 — wired the agentic FunctionAgent into `app.py` as a selectable mode** (additive; live workflow stack + Phoenix wiring untouched). Rather than edit app.py's option lists, registered the agent in the **workflow registry** (`harness/workflows/registry.py`: `"agent"` builder) so it auto-appears in the registry-driven settings panel — preserving `test_chat_options_mirror_registries`. New `harness/agents/workflow_adapter.py`: `AgentWorkflowAdapter` exposes an `AgentSession` through the WorkflowRunner `invoke`/`ainvoke` contract, mapping the structured `RegulatoryAnswer` → `{"answer_text","docs","answer"}` (citations → lightweight doc-like objects so the existing source sidebar renders cited URLs/quotes). Uses `pipeline_config=None` (plain retrieve, no extra reranker) to stay GPU-light in the live app; the full query-expansion+rerank pipeline stays on the demo/eval entrypoints. Phoenix's `auto_instrument` traces the FunctionAgent, so **no MLflow is introduced into the live path** (run-recording stays on `AgentSession.arun(record=True)`). Minimal app.py: 3 additive dict entries (`_PROFILE_STRATEGY`/`_PROFILE_DESCRIPTIONS`/`_WORKFLOW_LABELS`) add an "Agentic RAG (FunctionAgent)" ChatProfile + panel label. Verified: `app` imports, agent in `list_workflows()` + as a ChatProfile, real end-to-end `get_workflow("agent").ainvoke({...})` (the exact `_run_pipeline` call) → correct NDMA answer + 3 citations in the expected dict shape. 4 new adapter tests; full agentic+app suite **97 passed**; ruff + mypy clean on new files. | `harness/agents/workflow_adapter.py`, `harness/workflows/registry.py`, `app.py`, `tests/test_agent_workflow_adapter.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Documentation refresh after T1–T6** (docs-only). Flipped the agentic-layer status from "in progress / unit-tested offline / runtime-verification pending / not wired" → **"runtime-verified + wired into `app.py` as the selectable `agent` strategy"** across `CLAUDE.md` (banner), `README.md`, `docs/ARCHITECTURE.md` (§4a + added the `workflow_adapter` row), `docs/TARGET_ARCHITECTURE.md` (status banner + §7 build-order status note + §8 spikes: autolog **resolved**, span-feedback/judge-alignment still pending + §9: autolog-vs-manual **settled**, Phoenix/MLflow coexistence stated), `docs/RUNTIME_VERIFICATION.md` (added a ✅ T1–T6 results block + flipped the "NOT verified/NOT wired" state line), `docs/WORKFLOWS.md` (agentic callout → the `agent` strategy, registry-driven). Fixed the stale "**9 registered strategies**" count in `DECISIONS.md` → 8 (7 workflows + additive `agent`). Added **`docs/AGENTIC_GUIDE.md`** — a step-by-step how-to for the agentic tasks (prereqs/graph sanity, agent demo, MLflow runs+autolog, ontology enrichment, judges+eval, Chainlit agent mode, re-verify, config reference, troubleshooting); every command matches what was run on the GPU host. Cross-linked from README/CLAUDE/ARCHITECTURE/TARGET_ARCHITECTURE/RUNTIME_VERIFICATION/WORKFLOWS. Verified: no stale status phrases remain, all new doc links resolve. | `CLAUDE.md`, `README.md`, `DECISIONS.md`, `docs/{ARCHITECTURE,TARGET_ARCHITECTURE,RUNTIME_VERIFICATION,WORKFLOWS,AGENTIC_GUIDE}.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Repo-wide staleness sweep after the LlamaIndex/Neo4j + agentic refactors.** Fixed `run_ui.sh` — prepend the project `.venv/bin` to PATH so `phoenix`/`chainlit` resolve when the script is launched from a conda `base` shell (root cause of the reported `phoenix: command not found`; reproduced before/after). Resolved the **Phoenix-vs-MLflow current-vs-target contradiction** in `docs/TARGET_ARCHITECTURE.md`: §2 table, §4.1 header, §4.7 tracing+feedback, and §5 tree said "Phoenix dropped/removed" and "agents replace workflows / retires ReAct" as if done — now explicitly marked TARGET, with the current state stated (Phoenix is the live Chainlit tracer; MLflow is on the demo/eval entrypoints; agent + workflows coexist). Added a "Current state (2026-06-22)" phase-status table to `project_roadmap/ROADMAP.md` reconciling the benchmark-first plan with the additive agentic pipeline; fixed the Phase 5 release tree (`run_eval.py` → `harness/eval/`). `project_roadmap/CLAUDE_CODE_SETUP.md`: qdrant/FAISS → Neo4j+LlamaIndex, black → ruff, EPAR/ontology scope locks marked lifted/superseded. `project_roadmap/ABLATIONS.md`: noted Ablation B's agent already exists (`harness/agents/`). Removed the dead Postgres deps (psycopg/psycopg_pool/pgvector — nothing imports them) from `pyproject.toml`. Corrected `harness/configs/parser_preference.yaml` header (referenced the deleted `harness.embed_pg.sync`). Docs-and-config only; no runtime code paths changed. | `run_ui.sh`, `pyproject.toml`, `docs/TARGET_ARCHITECTURE.md`, `project_roadmap/{ROADMAP,CLAUDE_CODE_SETUP,ABLATIONS}.md`, `harness/configs/parser_preference.yaml`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Migrated the live app off Arize Phoenix onto MLflow (complete; no backward compat).** WHY IT WAS NOT DONE BEFORE: the agentic layer was scoped additive ("do not touch app.py Phoenix wiring"), and MLflow trace **assessments** (👍/👎) cannot be persisted by the local file store — verified `mlflow.log_feedback` 404s on the file store, works on **sqlite** — so wiring UI feedback to MLflow required moving to a sqlite-backed `mlflow server`, which nobody had done. WHAT: `harness/obs/tracing.py` gained a tracing-enabled flag (`traced()` no-ops until setup), `last_trace_id()`, `log_user_feedback()` (flushes async export, AssessmentSource HUMAN), `experiment_traces_url()`. `WorkflowRunner` (`harness/workflows/utils.py`) replaced its OpenTelemetry span with an MLflow span via `traced()` + a pure `_config_attributes()` (stamps `ema.*`). `app.py`: `phoenix.otel.register`→`setup_tracing(autolog=True)`; per-turn trace id captured via `last_trace_id()`; footer → MLflow experiment Traces URL; `on_rate` → `mlflow.log_feedback`; `PHOENIX_*`→`MLFLOW_TRACKING_URI`/`MLFLOW_UI_URL`/`EMA_TRACING_DISABLED`. `run_ui.sh` starts `mlflow server` (sqlite `mlflow.db`, :5000) instead of `phoenix serve`. `pyproject.toml`: dropped `arize-phoenix` + `openinference-*`. `harness/export_traces.py` rewritten to `mlflow.search_traces`; `harness/fewshot_inject.py` stripped of Phoenix trajectory-enrichment (cache-based Q+A only); deleted `harness/rating.py` + `harness/hitl/` (dead Phoenix CLIs) + `tests/test_hitl_export.py`. Docs swept Phoenix→MLflow across README/CLAUDE/DECISIONS (superseding decision entry)/TARGET_ARCHITECTURE (flipped target→done)/SETUP/ARCHITECTURE/ONBOARDING/AGENTIC_GUIDE/WORKFLOWS/RETRIEVAL/RUNTIME_VERIFICATION/chainlit + project_roadmap. VERIFIED end-to-end on a real `mlflow server`: importing the migrated `app`, a `WorkflowRunner` turn creates an MLflow trace with all `ema.*` attrs on the root span, `last_trace_id()` captures it, and a 👍 `log_feedback` round-trips as a trace assessment. Tests: rewrote `test_span_attributes.py` (OTel→MLflow), fixed `test_chat_settings.py` disable flag, fixed pre-existing `test_workflow_registry.py` (missing `agent` strategy); 103 offline pass, ruff+mypy clean on changed files. | `app.py`, `run_ui.sh`, `pyproject.toml`, `harness/obs/{tracing,__init__}.py`, `harness/workflows/{utils,registry,react_native,events}.py`, `harness/export_traces.py`, `harness/fewshot_inject.py`, `harness/agents/workflow_adapter.py`, `harness/obs/config_attrs.py`, deleted `harness/rating.py`+`harness/hitl/`, `tests/{test_span_attributes,test_chat_settings,test_workflow_registry}.py`, deleted `tests/test_hitl_export.py`, `README.md`, `CLAUDE.md`, `DECISIONS.md`, `docs/*`, `project_roadmap/*`, `chainlit.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Unified the MLflow store across live app + demo/eval (one UI).** Changed `harness/obs/runs.py setup_mlflow` default from the file store (`file:./mlruns`) to **sqlite `mlflow.db`** with precedence `explicit arg -> MLFLOW_TRACKING_URI env -> sqlite:///mlflow.db` — so `scripts/run_agent_demo.py` and `harness/eval/` now record into the same store the live-app `mlflow server` serves (a CLI run in a `run_ui.sh` shell logs through the server via the exported env var; standalone it writes sqlite directly). sqlite (not the file store) is also required for trace assessments. Per the owner, **no migration of old `./mlruns` runs** (backward compat not needed). Updated docstrings (`run_agent_demo.py`, `agents/session.py`), `.gitignore` (+`mlartifacts/`, `.mlflow.log`, sqlite sidecars), and `tests/test_agent_session.py` (asserts `mlflow.db` not `mlruns/`). Verified: default routes to sqlite + records a run; env override -> server URL; 49 obs/eval/agent/workflow tests pass; ruff + mypy clean. | `harness/obs/runs.py`, `harness/agents/session.py`, `scripts/run_agent_demo.py`, `.gitignore`, `tests/test_agent_session.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Unified response structure across RAG strategies + fixed chat-history persistence.** (1) RESPONSE SHAPE: every strategy now yields a structured `RegulatoryAnswer` recorded uniformly on the MLflow trace root span. `WorkflowRunner.ainvoke` wraps `{answer_text,docs}` via `RegulatoryAnswer.from_nodes` into `result["answer"]` (skipped for non-answer/echo results); `AgentWorkflowAdapter.ainvoke` now opens the same `traced()` root span with `ema.*` attrs. New `harness.obs.tracing.record_answer_on_span` sets span inputs/outputs. Verified on a live mlflow server: both simple_rag and agent produce identical response keys (`answer,caveats,citations,claims,confidence`) under a uniform `ema.*` root span (agent just has richer claims). (2) CHAT HISTORY (pre-existing bug, root-caused): threads had NULL `userId` so the sidebar (which filters by the logged-in user id) was always empty. Cause: `.chainlit/config.toml auto_tag_thread = true` makes Chainlit call `update_thread(..., user_id=<id>, tags=[profile])`, but the SQLite `SQLAlchemyDataLayer` cannot bind a Python list (`type 'list' is not supported`) so the entire write — including the user link — fails. Fixed by setting `auto_tag_thread = false`; verified that thread writes then persist `userId` and the thread appears in `list_threads(ThreadFilter(userId=...))`. Trade-off: resume no longer auto-restores the per-thread workflow (defaults; panel is source of truth). Old orphaned threads stay hidden; new chats persist. Tests: +2 in `test_span_attributes.py` (RegulatoryAnswer wrap + echo-skip); 83 offline pass, ruff clean. | `harness/workflows/utils.py`, `harness/agents/workflow_adapter.py`, `harness/obs/{tracing,__init__}.py`, `.chainlit/config.toml`, `app.py`, `tests/test_span_attributes.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Fixed two MLflow traces per prompt → one trace per turn.** Cause: `app._run_pipeline` embeds the query for the semantic cache (`_embed_query_sync`) BEFORE the workflow runs and outside any span, so `mlflow.llama_index.autolog()` recorded that standalone embedding as its own trace (question→vector), while the workflow (retrieval+generation) became a second trace. Fix: wrapped the whole turn in a single `traced("chat.turn")` root span (with `ema.run.id/source/index.profile`), so the cache embedding, retrieval, and generation all nest under it — verified the MLflow span context propagates through `asyncio.to_thread` (contextvar copy). The root span gets `set_inputs({"question": ...})` and `record_answer_on_span(answer=...)` (the structured `RegulatoryAnswer`, or the cached-answer dict on a cache hit), so the trace shows question in / answer out. Verified on a live mlflow server: one prompt now creates exactly 1 trace with root `chat.turn` → `huggingfaceEmbeddings_get_text_embedding` + `<workflow>.invoke` nested, inputs={question}, outputs=RegulatoryAnswer. 17 app/trace tests pass; ruff clean. | `app.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Fixed the Chainlit-app CUDA OOM (embed-model GPU leak) + simple_rag 120s timeout.** Root cause of `torch.OutOfMemoryError`: `configure_embed_model()` unconditionally did `Settings.embed_model = HuggingFaceEmbedding(...)` (a fresh ~1.3 GB bge-large on the 3090) on *every* call, and the app calls it twice per session (`open_index` + `build_hierarchical_retriever`) on each `on_chat_start`/`on_chat_resume`; each open thread keeps its retriever (and model) alive in `cl.user_session`, so copies accumulated (~14 = 18.7 GB) until OOM — the 22:48 three-thread resume storm tipped it. Fix: `configure_embed_model` now caches the embed model process-wide, keyed by `(provider, name, device, embed_batch_size)`, under a lock so a concurrent resume burst can't race-build several; all sessions share one GPU model. Verified GPU-free with a stubbed `HuggingFaceEmbedding`: 26 sequential + 16 concurrent calls → exactly 1 construction. Separately bumped `build_simple_rag` `timeout=120 → 300` to match crag/react_native/agentic composites (a T4 synthesis question + Opus generation legitimately exceeds 120s → `WorkflowTimeoutError … retrieve_and_generate`). Non-fatal items left as-is (reported, not patched): the `'MockValSer' … SchemaSerializer` autolog span warning (Pydantic serializer not built on a llama-index event; trace still records) and `create_element … No blob_storage_client` (expected without blob storage). The earlier `sqlite … type 'list' is not supported` was already fixed by `auto_tag_thread=false` (only in the 20:27 run). ruff clean. | `harness/providers.py`, `harness/workflows/simple_rag.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-22 | **Agentic RAG now emits real node-derived citations (was URL-only).** Root cause: the `FunctionAgent` self-authors `RegulatoryAnswer.citations` from what it sees, but `ema_search` returns only a formatted string (source URL + score in prose), so its citations had `source_url` populated and `doc_id`/`quote`/`score` empty/null — unlike `simple_rag`, whose `WorkflowRunner` builds citations from the actual retrieved nodes via `RegulatoryAnswer.from_nodes`/`citation_from_node`. Fix: added `harness.tools.search.capture_search_nodes` (a `contextvars`-based per-run sink; `ema_search` appends each retrieve's `NodeWithScore` list — verified the sink is visible across LlamaIndex's `sync_to_async` `copy_context` executor hop). `arun_agent` wraps `agent.run` in that scope and passes the captured nodes to `coerce_answer`, which now rebuilds the top-level `citations` from them (new `schemas.citations_from_nodes`: dedupe by node id, keep highest score, sort strongest-first) and enriches claim-level citations by `source_url` match (non-destructive). Renamed `coerce_answer`/`arun_agent`/`run_agent` param `fallback_nodes`→`evidence_nodes` (used in both structured + text paths now). `AgentWorkflowAdapter._CitationDoc` forwards the real score (was hardcoded 0.0) + doc_id/chunk_id so the source sidebar shows true scores. `chunk_id` stays "" (pre-existing: `citation_from_node` reads it from metadata, absent in live nodes — empty in simple_rag too, not part of this regression). Tests: +9 (runner override/enrich/passthrough + capture integration, tools capture, schemas dedupe/sort); full suite 429 passed/2 skipped, ruff + mypy clean on changed files. | `harness/tools/search.py`, `harness/schemas/{answer,__init__}.py`, `harness/agents/{runner,workflow_adapter}.py`, `tests/test_{agent_runner,tools,schemas}.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-24 | **Built the agent-centric, config-driven "recipe" engine + fixed audit findings (Phases 0–5).** Motivation: an audit found two parallel mode selectors (10 ChatProfiles vs orthogonal panel; 3 profiles wrongly `default=True`), split-brain retrieval (UI agent ran plain retrieve while eval ran the full pipeline), dead/misleading config (`native.yaml` `sub_retrievers`/`graph_mode` stamped on traces but never executed; orphaned `native_pg.py`; unconsumed `GraphRetrievalConfig`), a severed feedback loop (👍/👎 only hit MLflow, never the cache → few-shot injection permanently inert), and a hardcoded `ema.retrieval.strategy="hierarchical"`. **P0:** `retriever_attributes` derives the strategy from the retriever; one default ChatProfile; `on_rate` writes the rating back to the cache (👍=5/👎=1). **P1:** new `corrective_search` tool = CRAG's bounded grade→rewrite loop *inside a tool* (returns corrected context + honest grade note, feeds the citation `_NODE_SINK`); the rubric/parser/sufficiency/rewrite prompt are single-sourced in `harness/retrieval/corrective.py` and shared with the legacy `CRAGWorkflow` (no fork); new `docs/RAG_TECHNIQUES.md` (Naive/CRAG/ReAct, citations verified via web). **P2:** new `harness/recipes/` (Recipe dataclass + loader + registry + `build_recipe`) composing one agent-centric pipeline; `EMA_CONFIG_DIR` search path (`harness/config_paths.py`) so recipes/prompts live outside the source tree; `assemble_agent`/`build_agent` extended to accept an inline `AgentConfig` + forward the LLM to tools; 6 built-in recipes (`naive_rag` default, `crag_agentic`, `react_agentic`, `regulatory_agent`, `agentic_reranked`, `agentic_judged`) + per-recipe prompts; honest recipe-sourced `ema.*` stamping. **P3:** few-shot injection gated by `recipe.fewshot` (adapter prepends examples; runtime `ema.fewshot.injected`); retrieval pipeline opt-in via `recipe.retrieval.pipeline`; optional inline judge layer (`harness/eval/inline_judge.py` reuses the gold-free `Judge`, logged via new `obs.log_judge_feedback`, source LLM_JUDGE). **P4:** `app.py` rewritten to a single **recipe dropdown** + live model/temp/k/cache overrides (removed `_PROFILE_STRATEGY`/workflow+prompt+profile selectors); `_run_pipeline` gates few-shot + runs the judge; `scripts/run_agent_demo.py --recipe` goes through `build_recipe`. **P5:** `RetrievalPipelineConfig.resolved_attributes` now stamps only active stages (drops the misleading `sub_retrievers`/`graph_mode`/`k`); new `docs/RECIPES.md`; status banners on `RETRIEVAL_TRACKS.md` (unimplemented proposal; §4 Phoenix superseded) + recipe banners on `CLAUDE.md`/`AGENTIC_GUIDE.md`. Legacy `harness/workflows/*` kept as deterministic eval baselines. Tests: +~45 (recipes, corrective tool/primitives, inline judge, recipe chat-settings, adapter few-shot); rewrote `test_chat_settings.py`; full suite **456 passed / 2 skipped**, ruff clean on changed files, new modules mypy-clean. Live GPU-host verification (recipe traces, judge scores, cross-encoder) still pending. | `app.py`, `harness/recipes/*`, `harness/config_paths.py`, `harness/tools/{corrective_search,__init__}.py`, `harness/retrieval/{corrective,config}.py`, `harness/workflows/{crag,utils}.py`, `harness/agents/{session,registry,workflow_adapter,regulatory}.py`, `harness/obs/{tracing,__init__}.py`, `harness/eval/{inline_judge,__init__}.py`, `harness/configs/recipes/*.yaml`, `harness/prompts/agent_{naive,crag,react}.md`, `scripts/run_agent_demo.py`, `docs/{RAG_TECHNIQUES,RECIPES,RETRIEVAL_TRACKS,AGENTIC_GUIDE}.md`, `CLAUDE.md`, `tests/*`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-25 | **Retired the legacy Workflow engine + removed the `pattern` taxonomy (true single-engine agentic RAG).** WHY: the owner specified agentic-as-first-class with naive RAG = a tool call and CRAG = a config of the agent; the prior "hybrid" kept the whole `harness/workflows/*` Workflow engine alongside the agent ("two engines"), and added an inert `pattern: forced_once|agentic` field that `build_recipe` never read. WHAT: **deleted** `harness/workflows/` (10 modules: simple_rag/crag/summarize_rag/react_native/composites/review/registry/utils/events/__init__), the 4 workflow-only prompts (`system_{zero_shot,few_shot_sme,cot_self,summarize}.md`), and the workflow tests (`test_workflows.py`, `test_workflow_registry.py`, `test_span_attributes.py`); removed the dead `build_agent_workflow` registry bridge from `agents/workflow_adapter.py` (kept `AgentWorkflowAdapter`, used by `build_recipe`) and its 3 tests. The CRAG primitives already live in `harness/retrieval/corrective.py` (single source for the `corrective_search` tool), so nothing of value was lost. **Removed `pattern`** from `recipes/config.py` (field + `VALID_PATTERNS` + validation + trace stamping), all 6 recipe YAMLs, `test_recipes.py`, and the `agent_*.md` prompt headings; a recipe is now purely `{tools, system_prompt, output_schema, retrieval, generation, judge}` — the toolset+prompt define the technique. Verified pre-deletion that NO production code outside the package imported it (only tests + docstrings). Docs: rewrote the `CLAUDE.md` recipe banner (single-engine; notes the retirement + which docs are now stale), superseded-banner on `docs/WORKFLOWS.md` (was 100% about the deleted engine), de-`pattern`'d `RECIPES.md`/`RAG_TECHNIQUES.md`, fixed stale `CRAGWorkflow`/`workflows/registry` docstring refs. Smoke: all 6 recipes build a FunctionAgent with the right tools (`naive_rag`→[ema_search]; `crag_agentic`→[corrective_search, ema_search]). Full suite **417 passed / 2 skipped** (−39 deleted workflow tests), ruff clean on changed files. STILL STALE (flagged, not yet swept): `docs/{ONBOARDING,ARCHITECTURE,RETRIEVAL,TARGET_ARCHITECTURE}.md`, `DECISIONS.md`, `README.md` still reference the retired engine. | deleted `harness/workflows/`, `harness/prompts/system_*.md`, `tests/test_{workflows,workflow_registry,span_attributes}.py`; `harness/agents/workflow_adapter.py`, `harness/recipes/{config,__init__}.py`, `harness/configs/recipes/*.yaml`, `harness/prompts/agent_{naive,crag,react}.md`, `harness/retrieval/corrective.py`, `harness/indexing/registry.py`, `tests/test_{recipes,agent_workflow_adapter}.py`, `CLAUDE.md`, `docs/{WORKFLOWS,RECIPES,RAG_TECHNIQUES}.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-25 | **Fixed 7 bugs/inconsistencies found in a self-review of the recipe engine.** (1) **Trace honesty:** `build_recipe` built the LLM from the live override (`model`/`temperature`/`retrieval_k`) but stamped `recipe.resolved_attributes()` from the recipe *defaults* — so an overridden run logged the wrong model/temp and never logged k. `Recipe.resolved_attributes` now takes effective `model`/`temperature`/`retrieval_k`; `build_recipe` passes the resolved values (and `effective_k = profile.retrieval.k`). (2) **CRAG grader:** I'd made `build_agent` forward the agent LLM to every tool, which silently routed `corrective_search`'s grading/rewriting onto the expensive agent model (Opus) and made `grader_role` dead. Reverted the forwarding (`build_tools(cfg.tools, **tool_kwargs)`); `corrective_search` builds the cheap `grader_role` **lazily on first call** (no model creds needed just to build the tool/agent). (3) **Double trace:** the inline judge ran *after* the `traced("chat.turn")` span closed, so autolog opened a 2nd trace per `agentic_judged` turn. Moved the judge inside the span (its LLM call nests); feedback is still logged after close via the flushed trace id. (4) **Judge context:** the judge graded against truncated citation quotes, not the retrieved passages. `capture_search_nodes` now *reuses an active sink* (nested scopes share), so the adapter wraps the agent run, captures the full nodes, and surfaces `context_passages`; app prefers them. (5) Added a built-in recipe that exercises few-shot (`regulatory_fewshot`, `fewshot.enabled: true`) — nothing demonstrated the revived loop. (6) Model-override dropdown was hardcoded; now reads `harness.models.list_model_names()` from models.yaml. (7) Cached-answer reuse now offers 👍/👎 (cache-only rating via a `cache_only` payload flag — no stale-trace fallback); `set_chat_profiles` parses recipes once. Left documented: the process-global `EMA_INDEX_PROFILE` env mutation races across concurrent UI users (pre-existing, needs session-scoped threading). Tests: +3 (effective-override stamping, few-shot recipe, nested-capture sink reuse); updated builtin-recipe sets + build_recipe fakes (now need `profile.retrieval.k`). Full suite **420 passed / 2 skipped**, ruff clean on changed files; offline build smoke confirms all 7 recipes stamp the effective model/k and `crag_agentic` builds with no API key. | `harness/recipes/{config,build}.py`, `harness/agents/{registry,workflow_adapter}.py`, `harness/tools/{corrective_search,search}.py`, `harness/models.py`, `app.py`, `harness/configs/recipes/regulatory_fewshot.yaml`, `tests/test_{recipes,tools_corrective,chat_settings}.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-06-25 | **Doc sweep: removed stale references to the retired Workflow engine across the active docs.** After deleting `harness/workflows/*`, the prose docs still described the deleted engine (`get_workflow`, `WorkflowRunner`, the 7 workflows, `prompt_strategy`, "Agentic RAG mode alongside the workflows"). Rewrote the orchestration narrative to the single-engine recipe model in `README.md` (file-tree, status, Stack, data-flow diagram), `docs/ARCHITECTURE.md` (merged §4 "Workflow layer" + §4a "agentic layer" into one recipe-engine section; fixed §5 UI, §6 judge, §7 git layout, diagram), `docs/ONBOARDING.md` (file-map, the 7-strategy table → built-in recipe table, mental-model line, diagram), `docs/RETRIEVAL.md` (3 refs), `docs/TARGET_ARCHITECTURE.md` (§4.1 heading + the WorkflowRunner trace note), `chainlit.md` (recipe dropdown), `docs/SETUP.md` (trace-filter how-to → `ema.recipe`). For `DECISIONS.md` (a decision *log*) added a new superseding entry "Single-engine agentic RAG (recipe engine) supersedes the Workflow engine" + `> Superseded 2026-06-25` annotations on the three affected decisions (LlamaIndex-Workflows-for-orchestration, native-ReAct-workflow, prompt_strategy-as-YAML) rather than rewriting history; Phoenix-era entries left under the existing MLflow-replaces-Phoenix supersession. `docs/WORKFLOWS.md` + `docs/RETRIEVAL_TRACKS.md` keep their existing SUPERSEDED/UNIMPLEMENTED banners (bodies preserved as history); `docs/RUNTIME_VERIFICATION.md` T6 step annotated as superseded (dated runbook, not rewritten). Code unchanged → full suite still 420 passed / 2 skipped. | `README.md`, `chainlit.md`, `DECISIONS.md`, `docs/{ARCHITECTURE,ONBOARDING,RETRIEVAL,TARGET_ARCHITECTURE,SETUP,RUNTIME_VERIFICATION}.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-02 | **Created `docs/REQUIREMENTS_REVIEW.md` — living requirements↔codebase working doc.** Synthesizes the 2026-07-02 full codebase audit (3 parallel deep reviews + test run: 413 passed / 2 skipped offline; only live-Mongo tests fail) against the owner's 7 restated requirements (R1 configurable agentic setups, R2 graph evolution, R3 EMA-graph-only retrieval, R4 gated external tools/MCP, R5 inspectability+judges, R6 reproducible experiments, R7 config-selected Pydantic outputs). Contains a findings register F1–F20 (top: F1 acronym expansion silently dead everywhere + dishonest trace stamping; F3 offline mlflow.genai faithfulness judge never sees context; F4 cross-session lost updates in the rated-trajectory QueryCache; F5 eval layer cannot consume recipes — `build_predict_fn` incompatible with `AgentWorkflowAdapter`, only the recipe-bypassing `build_session` works; F2 output_schema is config-selectable in name only — one registered schema, silent fallback, downstream hardcoded to RegulatoryAnswer; F18 reviewer-in-the-loop regressed from threshold+passed gate to score-only JudgePolicy), per-requirement open questions for the owner, and a proposed 6-step sequencing. Analysis only — no code changed. | `docs/REQUIREMENTS_REVIEW.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-04 | **First tranche of `docs/REQUIREMENTS_REVIEW.md` findings fixes (F1, F2, F13, F14, F15 done; F3 half-done — session interrupted, handed off for machine switch).** F1: acronym query-expansion revived — `ablations/` machinery moved into the package (`harness/retrieval/acronyms.py` `QueryExpander` + shipped `harness/configs/retrieval/acronyms.yaml`); the `acronym` transform now context-aware by default and raises if the dictionary is missing; `ablations/` + `harness/ablations/` deleted. F2: strict output-schema registry (`get/register/list_output_schemas`, no silent RegulatoryAnswer fallback; `Substance` registered). F13: adapter no longer stamps `ema.index.profile` from the process-global env. F14: `tag_current_trace()` stamps `ema.recipe` as a searchable trace-level tag. F15: shared `default_experiment()` resolver (`EMA_MLFLOW_EXPERIMENT`) used by app/demo/eval/session. F3 HALF: judges `_VAR_MAP` `context→outputs` landed, but `harness/eval/predict.py` does NOT yet return `context_passages` — next step, couples with F5; see handoff `.claude/transitions/2026-07-04/123352.md`. Suite: 416 passed / 2 skipped; ruff clean on changed files. | `harness/retrieval/{acronyms,transforms}.py`, `harness/configs/retrieval/acronyms.yaml`, `harness/agents/{registry,session,workflow_adapter,__init__}.py`, `harness/obs/{runs,tracing,__init__}.py`, `harness/eval/{judges,evaluate}.py`, `app.py`, `scripts/run_agent_demo.py`, `tests/test_retrieval_acronyms.py`, `tests/test_eval_judges.py`, `.claude/transitions/2026-07-04/` | Phase 1 (refactor) | — |
| 2026-07-04 | **Second tranche of `docs/REQUIREMENTS_REVIEW.md` findings fixes — F3 finished + F4, F5, F6, F7, F17, F19, F20 done; R6 eval vehicle landed.** F3+F5 (one unit): `build_predict_fn` now prefers the `AgentWorkflowAdapter.invoke` contract and returns `context_passages` on every branch (adapter passthrough; `capture_search_nodes` around bare `.run`/callables) so the offline faithfulness judge finally grades real retrieval; shared `passages_from_nodes` helper in `harness/tools/search.py`. F6: `build_session` + `load_agent_config` + `configs/agent/regulatory.yaml` deleted — `build_recipe` is the single composition path (`AgentConfig` slimmed, `build_agent(config=...)` required). F4: process-wide `get_query_cache()` + `RLock` + atomic tmp/rename writes (cross-session lost updates of entries/ratings fixed; multi-process out of scope, documented). F7: `FewshotPolicy.min_examples` (default 1) plumbed app→injector and stamped on traces. F17: `corrective_search` keeps best-so-far across rewrite cycles (`grade_key`, ties→later), grader prompt keys on `[i]` doc index (not phantom `qa_id`s), parse-error sentinel filtered out of rewrite prompts. F19: cache toggle now disables READS only (writes/ratings persist + UI notice), 👎-rated entries excluded from reuse offers, resume restores the thread's chat-profile recipe + says so, failed profile switch snaps the settings panel back; turn spans stamp the SESSION's index profile (F13 completion). **R6**: `harness/eval/runner.py` + `scripts/run_eval.py` — recipe × `benchmark/benchmark.jsonl` (45 Qs) → one MLflow run per question type (T1–T4) tagged `ema.recipe`/`ema.benchmark`/`ema.question_type`; `judge_model_uri()` resolves the judge from models.yaml roles. F20: deleted `generate_comparison_report.py`, GitHub-issue scripts, empty `main.py`, `HARNESS_REFACTORS.md`, orphaned `relevance_rubric_sme.md`; dropped unused deps (`llama-index-vector-stores-faiss`, `llama-index-retrievers-bm25`, `rank-bm25`); fixed stale `WorkflowRunner`/ablations comments; committed `uv.lock`. Runtime verification of the eval runner pending (GPU host). Suite: 432 passed / 2 skipped; ruff clean on changed files. | `harness/eval/{predict,judges,runner,__init__}.py`, `harness/agents/{session,registry,config,workflow_adapter,__init__}.py`, `harness/recipes/{build,config}.py`, `harness/{query_cache,fewshot_inject,export_traces}.py`, `harness/retrieval/corrective.py`, `harness/tools/{search,corrective_search}.py`, `app.py`, `scripts/{run_eval.py,…}`, `pyproject.toml`, `uv.lock`, `docs/{REQUIREMENTS_REVIEW,RECIPES,AGENTIC_GUIDE,TARGET_ARCHITECTURE}.md`, `tests/` | Phase 1 (refactor) | — |
| 2026-07-05 | **Full markdown documentation sweep — brought all living docs in line with the 2026-07-04 code state (3 parallel audit agents over 40+ md files, every claim verified against code).** The recurring falsehoods fixed everywhere they appeared: (1) "the eval/judge/benchmark-runner suite is archived off-branch and must be rebuilt" → it is rebuilt (`harness/eval/runner.py`, `scripts/run_eval.py`, MLflow = system of record; only lift/closed-book/ablations remain archived) — fixed in README, CLAUDE.md, ONBOARDING, ARCHITECTURE §6, SETUP §7, ROADMAP (banner + Phase 2/3 status + Phase 5 tree), ABLATIONS, OPEN_QUESTIONS, README_OUTLINE, CLAUDE_CODE_SETUP, benchmark/STATS; (2) "workflows untouched / 7 workflows / 9 profiles / Workflow selector" → single recipe-configured FunctionAgent + Recipe dropdown (7 recipes) — fixed in CLAUDE.md banners, AGENTIC_GUIDE (intro + Task 5), ONBOARDING (UI, mermaid, roles agent/grader/judge), TARGET_ARCHITECTURE (banner, §2 react row, §5 config layout), README; (3) moved/deleted paths — acronym dict → `harness/configs/retrieval/acronyms.yaml` (ABLATIONS A1 note, ROADMAP), `ablations/B_process_rewards/SANITY_CHECK.md` ref (OPEN_QUESTIONS), `harness.indexing.enrich_ontology` → `python -m harness.ontology.enrich` (TARGET_ARCHITECTURE §4.5), broken `docs/LEAKAGE|ABLATIONS.md` links in ROADMAP. DECISIONS.md: supersession notes added to 3 orphaned entries (orchestration-block schema, Phoenix span stamping, build_retrieve_fn), few-shot entry updated (min_examples default 1, FewshotPolicy-gated, MLflow), LlamaIndex entry drift-annotated, dead links to deleted files de-linked. REQUIREMENTS_REVIEW: §0 test count 413→432, F1/F2/F13/F14/F15 marked ✅ with fix details, R5/R6/R7 current-state sections updated to post-fix reality, F20 note updated (pycache dirs physically removed 2026-07-05). GLOSSARY (EPAR + Graph RAG entries), BLOG_OUTLINE (3×3 grid, v1 non-goals), RECIPES (regulatory_fewshot added to built-ins table, min_examples knob), chainlit.md verified current. All relative md links now resolve (checker run). No code changed. | `README.md`, `CLAUDE.md`, `DECISIONS.md`, `OPEN_QUESTIONS.md`, `docs/{AGENTIC_GUIDE,ARCHITECTURE,ONBOARDING,RECIPES,REQUIREMENTS_REVIEW,SETUP,TARGET_ARCHITECTURE,RETRIEVAL_TRACKS}.md`, `project_roadmap/{ROADMAP,ABLATIONS,GLOSSARY,BLOG_OUTLINE,README_OUTLINE,CLAUDE_CODE_SETUP}.md`, `benchmark/STATS.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-05 | **Final tranche of `docs/REQUIREMENTS_REVIEW.md` §9 findings — F8, F9, F10, F11, F12, F16, F18 done: the findings register is now fully closed (F1–F20 ✅).** F9: `load_index_profile` honors `$EMA_CONFIG_DIR` via the shared `find_config` search path (external `index/` profiles shadow built-ins). F11: shipped `neo4j_hier.yaml` now `scope.limit: null` (full corpus, matching the live 79,882-doc graph) — the 50-doc rebuild footgun is gone; regression test asserts it. F8: dead `sub_retrievers`/`graph_mode`/`k` fields deleted from `RetrievalPipelineConfig` + `native.yaml` (per R1(d): delete or build, not half-way); retrieval `k` lives solely in the index profile. F10: `run_inline_judges`/`Judge` honor `model_role` (recipes can bind the `reviewer` role); unknown `fewshot.source` is a hard config error. F12 (R3-Q1 yes): the index profile is the single source of truth for the embedding model — `profile.index.embed_model` drives `configure_embed_model` on build/open/retriever paths (env fallback only); the query-cache sidecar gained embed-model provenance (`{"embed_model", "entries"}` format, legacy list accepted) — on a model switch old cache files are backed up (`*.bak-<model>`) and the cache starts fresh instead of mixing embedding spaces. F16: unjudged exemplars carry `score=None`, `judge_filter` refuses them loudly (no more silently-emptied trainset), new `faithfulness_judge()` adapter composes `harness.judge` with `build_predict_fn`'s output (incl. `context_passages`). F18 (owner R1-Q3: soft reviewer): `JudgePolicy.{threshold, on_fail: annotate}` + `review_verdict()` — below-threshold or unscorable answers ship with a visible ⚠️ recommendation (never blocked), verdict stamped `ema.judge.passed`/`ema.judge.threshold`, model confidence shown in the final message; `agentic_judged` recipe sets `threshold: 3`; decision recorded in DECISIONS.md. Docs updated (RECIPES judge block, AGENTIC_GUIDE pipeline row, REQUIREMENTS_REVIEW §1/§3/§9 + decision log). Suite: 445 passed / 2 skipped; ruff clean on changed files. | `harness/indexing/{profiles,property_graph,build}.py`, `harness/retrieval/config.py`, `harness/configs/{index/neo4j_hier,retrieval/native,recipes/agentic_judged,models}.yaml`, `harness/{judge,query_cache}.py`, `harness/eval/{inline_judge,bootstrap,__init__}.py`, `harness/recipes/config.py`, `app.py`, `DECISIONS.md`, `docs/{RECIPES,AGENTIC_GUIDE,REQUIREMENTS_REVIEW}.md`, `tests/` | Phase 1 (refactor) | — |
| 2026-07-05 | **Fixed the source-references bug: references could not be re-opened after closing the side panel, and vanished entirely on chat resume.** Two root causes found by reading the Chainlit 2.11.1 internals: (1) `SQLAlchemyDataLayer` **silently drops all elements when no storage client is configured** (`create_element` early-returns with only a log warning) — so the per-answer source cards were never persisted and resumed threads had nothing to open; (2) the frontend renders a `display="side"` element as a clickable reference **only where its `name` appears in the message content** — our answer messages never contained the names, so the panel only auto-opened once per new element set (memoized by element ids) and, once closed, had no affordance to re-open. Fix: new `_LocalStorageClient` in `app.py` (implements Chainlit's `BaseStorageClient`: writes under `public/elements/` — served by Chainlit at `/public/...` — URL-quoted keys, path-escape guarded, gitignored) wired into `SQLAlchemyDataLayer(storage_provider=...)`; and the final answer message now appends a `**Sources:** Q<n> · Src 1 \| …` line so every element name is a persistent clickable reference (works live and after resume; reference matching is per-message `forId`-scoped so no cross-turn collisions). Verified end-to-end: headless Chainlit boot → element file served at its storage URL (200, correct body), path traversal rejected (400); regression tests for the storage-client roundtrip/escape-guard and a `get_data_layer().storage_provider is not None` guard (the silent-drop trap). Suite: 448 passed / 2 skipped; ruff clean. | `app.py`, `.gitignore`, `tests/test_chat_settings.py` | Phase 1 (refactor) | — |
| 2026-07-07 | **Citation attribution + SME review + configurable export landed (approved plan `PLAN_citations_export_sme_review.md`, all 6 stages; the "chat UI vs dedicated tool" fork was settled by a 103-agent deep-research pass — MLflow Review App is Databricks-gated, Argilla in maintenance mode, Label Studio/Langfuse = second service without two-sided span highlighting → build in-chat, feedback straight to MLflow).** (1) Attribution foundation: `HierarchicalPGRetriever._QUERY` projects Document provenance (title/topic_path/committee/reference_number/source_type) + real `chunk_id`; new `harness/retrieval/doc_categories.py` classifies scientific_guideline/qa/epar/medicine_page/other from URL/topic path; `Citation` enriched with those fields and EVERY schema field gained a steering `Field(description=…)`; `Claim.text` is contractually a verbatim answer span (all 4 agent prompts updated); new `harness/attribution.py` (exact→fuzzy span matching with offset maps, overlap resolution, first-use reference numbering, quote-in-passage location, `[n]` marker injection, graceful zero-claims degradation); adapter joins citations to full captured passages and returns `attribution`+`references`. (2) Chat: source cards renamed `[1]..[n]` so injected inline markers double as clickable citation pills; cards show title/category/committee/ref/score. (3) Export: `harness/export/` (registry mirroring tools, `Exporter` ABC, `ExportBundle` = tool-neutral interchange dict, Markdown + self-contained HTML with two-way span↔reference highlight sync + embedded bundle JSON), `configs/export/default.yaml` via `$EMA_CONFIG_DIR` search path with hard errors, per-turn ⬇ Export action → persisted `cl.File` downloads, `export_turn()` API. (4) SME review: `public/elements/CitationReview.jsx` (persistent CustomElement — collapsed row → side-by-side view, highlight sync, per-reference supports/partial/no + prefer-category + note, verdicts persisted via updateElement), `cite_feedback` callback → new `harness.obs.log_citation_feedback` (unique `citation_<rank>_<chunk8>` names, span_id support added to `_log_feedback`), `export_traces.py` exports `citation_ratings`. (5) `doc_type_priority` postprocessor (deterministic category reorder, config-validated `doc_type_priority` list in pipeline YAML, plumbed via `build_postprocessors`). (6) Docs: `docs/CITATIONS.md`, CLAUDE.md 📎 banner, 3 DECISIONS entries, REQUIREMENTS_REVIEW decision-log row, RECIPES rerank note. Verified: full suite 491 passed / 2 skipped (was 448); ruff clean; headless Chainlit serves `CitationReview.jsx` (200). Runtime UI walk on GPU host pending (docs/CITATIONS.md §5). | `harness/{attribution.py,export/*,export_traces.py}`, `harness/retrieval/{doc_categories,postprocessors,config}.py`, `harness/indexing/property_graph.py`, `harness/schemas/answer.py`, `harness/prompts/agent_*.md`, `harness/agents/{workflow_adapter,session}.py`, `harness/obs/{tracing,__init__}.py`, `harness/configs/{export/default.yaml,retrieval/native.yaml}`, `public/elements/CitationReview.jsx`, `app.py`, `docs/{CITATIONS,RECIPES,REQUIREMENTS_REVIEW}.md`, `CLAUDE.md`, `DECISIONS.md`, `tests/` | Phase 1 (refactor) | — |
| 2026-07-07 | **Documentation overhaul for re-onboarding — big-picture-first rewrite reflecting the July refactors, plus one code fix it surfaced.** `docs/ONBOARDING.md` fully rewritten as THE entry point for a reader who lost the mental model: what the project is (3 sentences), a 6-point "60-second mental model", an updated big-picture mermaid (data → recipe engine → surfaces → feedback), a new **"Life of a question"** sequence diagram + step-by-step walkthrough naming the code behind each step (cache → build_recipe → tool loop + node capture → RegulatoryAnswer → attribution → rendering → MLflow), the three config surfaces with a 7-line recipe example, the feedback loops (👍/👎, per-citation verdicts, judge), file map, commands, benchmark status, and the GPU-walk pointer. `docs/RECIPES.md` gained a **"Worked examples"** section: (1) simple RAG from scratch, (2) **reproducing the CRAG paper** — full YAML + a paper→repo mapping table (retrieval evaluator→0/1/2 grader, web-search fallback→corpus-only rewrite+retry with rationale, max_cycles/best-so-far, no decompose-recompose) with every knob's location, (3) kitchen-sink recipe + pipeline YAML (acronym expansion, doc_type_priority + cross_encoder rerank, judge threshold, few-shot). `docs/RUNTIME_VERIFICATION.md` gained **§8 "The 2026-07-07 walk"** — the ordered GPU-host checklist (W1 retriever provenance, W2 eval-runner smoke, W3 six-step citations UI walk incl. verdict→assessment + resume, W4 doc_type_priority live, W5 embed-provenance) with expected results and failure notes; CITATIONS §5 now points there. README quick-links restructured into a reading order starting at Onboarding + a citations/export status paragraph; ARCHITECTURE module table + diagram extended with attribution/export/doc_categories/obs-feedback rows; AGENTIC_GUIDE links refreshed. **Code fix:** the RECIPES example exposed that `load_pipeline_config` still hardcoded the built-in dir — it now honors `$EMA_CONFIG_DIR/retrieval/` via the shared `find_config` search path (same as recipes/index/export; test added). Suite: 492 passed / 2 skipped; all doc links verified. | `docs/{ONBOARDING,RECIPES,ARCHITECTURE,RUNTIME_VERIFICATION,CITATIONS,AGENTIC_GUIDE}.md`, `README.md`, `harness/retrieval/config.py`, `tests/test_retrieval_pipeline.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-07 | **Ran the §8 GPU walk (first attempt) — W1 + W4-retrieval PASS live; W2/W3 blocked by an LLM-gateway infrastructure regression (not code).** Baseline: services healthy, 492 passed / 2 skipped on the GPU host. **W1 ✅**: the enriched `HierarchicalPGRetriever._QUERY` returns real `title`/`source_type`/`chunk_id`/derived `category` from the live 79,882-doc graph (committee/reference_number empty where absent in the source — expected). **W4 (retrieval half) ✅**: `doc_type_priority` reorders a live mixed result set (epar > other), stable within category. **W2/W3 ⛔ blocked**: the agent runs tool-less ("I don't have access to the ema_search tool") and structured output fails ("Expected at least one tool call, but got 0"). Root cause isolated with raw `anthropic` SDK probes: the `ANTHROPIC_BASE_URL` gateway (`gw.claudeapi.com`) **strips client-supplied tools and injects its own** (a one-tool request came back calling an injected `WebSearch`); the configured key is gateway-issued (401 against `api.anthropic.com`), so the fix is gateway config or a direct key — recorded in RUNTIME_VERIFICATION §8 "Results" with the follow-up list (re-run W2/W3/W5; investigate the non-fatal `MockValSer` span-close warning). The attribution degradation path behaved exactly as designed under the failure (plain answer, zero fabricated references, no crash). **W5 ⏳**: live cache sidecar still legacy-list (26 entries); adopts provenance on the next real turn. No code changes required by the walk so far. | `docs/RUNTIME_VERIFICATION.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-08 | **GPU walk complete — W1–W5 all green; three real defects found and fixed (which is the walk's purpose).** After the owner switched to a direct Anthropic key (the original gateway stripped client tools — infra issue recorded in §8), two library bugs surfaced and were fixed in `harness/llms.py::_Anthropic`: (1) llama-index 0.14.22's `generate_structured_response` forwards `tool_choice=None`, overriding the wrapper's correct tool_choice object → Anthropic HTTP 400 "Input should be an object" → every structured answer silently degraded to raw text; fixed by dropping the explicit None (exact-signature override so llama-index's `tool_required` capability probe still passes). (2) `AnthropicChatResponse.raw` holds lazily-built anthropic-SDK pydantic objects (`MockValSer`); MLflow autolog's span-close `model_dump()` raised, the span never ended, and `mlflow.genai.evaluate` lost the row's trace (`eval_item.trace is None` crash — the W2 failure); fixed by sanitizing `raw` to plain JSON-able data on every chat/stream response. (3) `harness/eval/runner.py` now aggregates per-judge mean scores from trace assessments onto the eval run (`mean_scores` + `_aggregate_run_metrics`) — this mlflow version returns no aggregate metrics, so `summarize()` previously printed "(no metrics)". **Live results:** W1 provenance ✅; W2 `T1: correctness_mean=4.000, faithfulness_mean=5.000` with tagged run + per-row assessments ✅; W3 programmatic ✅ (verbatim-claim spans + inline `[1][2]` markers on a live answer, complete tagged trace, `citation_1_*` feedback assessment logged and read back, MD+HTML exports rendered); W4 ✅ (live doc_type_priority reorder); W5 ✅ (cache sidecar adopted `{"embed_model": "BAAI/bge-large-en-v1.5"}`, 26 legacy entries preserved). Cosmetic leftover: one MockValSer warning per run from mlflow's own stream-accumulator span (harmless — documented). Browser click-through of the UI remains for the owner. Suite: 493 passed / 2 skipped; ruff clean. | `harness/llms.py`, `harness/eval/runner.py`, `docs/RUNTIME_VERIFICATION.md`, `tests/test_eval_runner.py`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-08 | **Browser walk confirmed by owner (GPU walk fully closed) + created `docs/next/` for detailed forward plans, starting with the closed-book baseline / lift metric.** RUNTIME_VERIFICATION §8 marked complete incl. the manual UI click-through (markers, review panel + verdict→assessment, export downloads, resume persistence all confirmed live). New `docs/next/README.md` (index + convention: plans live here in detail, decisions graduate to DECISIONS.md when landed) and `docs/next/closed_book_lift.md` — the full design for the benchmark's headline number: why lift (contamination story, worked example table), what exists to build on (the live-verified runner/judges/aggregation), the design (closed-book as a `closed_book` recipe with `tools: []` through the single composition path; runner `judges=`/`ema.model`-tag extensions; `harness/eval/lift.py` pairing runs by model×type with accuracy@4 binarization + mean-score delta, one MLflow "lift" run with per-type metrics + markdown artifact; `zero_shot_known` backfill script making the closed-book run double as the Phase 2.5 contamination screen), 6 implementation steps (~1–1.5 days), open decisions (zero-tool FunctionAgent verify, threshold, lazy index, n=45 stats, slot-guessing out of scope, OLMo tier), and verification. ONBOARDING §9/§10 updated to point there. | `docs/next/{README,closed_book_lift}.md`, `docs/{ONBOARDING,RUNTIME_VERIFICATION}.md`, `.claude/HISTORY.md` | Phase 1 (refactor) | — |
| 2026-07-12 | **Designed retrieval-miss detection via exact-span search (OLMoTrace-style infini-gram probe) — new gated plan in `docs/next/`.** Investigation trail: OlmoTrace itself is a category mismatch for RAG citation attribution (traces outputs to *training data*, needs an open corpus, explicitly does not verify support; the linked GitHub "OLMOTrace-Agent" is a third-party simulation) — but its mechanism pointed at *our* corpus answers a question attribution.py is blind to: answer spans that exist in-corpus but were never retrieved. Verified the `infini-gram` package (v2.6.0) ships only the search primitive (`find()`/`count()`/`get_doc_by_*`) — the maximal-span algorithm + filters live in allenai/infinigram-api (Apache 2.0, portable). `docs/next/retrieval_miss_detection.md`: three-bucket design (grounded / unretrieved / novel) for eval triage, memorization signature next to lift, and later live citation repair; leaf-chunk index keyed by `chunk_id` (no corpus offset map needed), shared normalizer extracted from attribution.py to survive pymupdf4llm markdown, unigram-prob + doc-frequency filtering for EMA boilerplate, injected-engine seam for offline tests, `--span-probe` runner flag; ~2–3 days; explicitly **gated on §5 trigger conditions** per the complexity rule (Lucene-on-Neo4j recorded as the fallback prototype). README table + ONBOARDING §10 "Possible improvements" updated. | `docs/next/{README,retrieval_miss_detection}.md`, `docs/ONBOARDING.md`, `.claude/HISTORY.md` | Phase 3 (partial) | — |
| 2026-07-12 | **Implemented source-category retrieval steering (Options A+B+C from the EPAR-dominance gap analysis) — composable, config-driven, no hardcoded topics in code.** **(A)** `:Document.category` persisted (ingest stamp in `_entity_for` + idempotent `scripts/backfill_doc_categories.py`); `HierarchicalPGRetriever` gained per-call category filters (`with_categories` → oversample k×`retrieval.oversample` + Cypher filter), per-profile `category_quota` stratification (`harness/retrieval/steering.py`, pure), and `ema_search(source_category=...)` with category-tagged output + honest steering notes + unfiltered fallback. **(B)** the long-declared-never-built `LINKS_TO` walk now exists: `retrieval.graph.expand` follows link edges from vector hits (edge `link_context`/`document_type` + target-category filtered, additive, `retrieval_origin="link_expansion"` + `linked_from` provenance). **(C)** query→category routing tables (`harness/retrieval/routing.py` + `harness/configs/routing/default.yaml`, recipe key `retrieval.routing`, prefer/filter modes, stamped `ema.retrieval.routing`). Precedence: explicit agent arg > routing > profile defaults; expansion always additive. All off by default; new `neo4j_steered` profile + `steered_agent` recipe enable the stack; `agent_regulatory.md` teaches the lever. 554 tests pass (+~45 new, all offline); ruff/mypy at baseline. Docs: RETRIEVAL.md §7 (new), RECIPES.md, ONBOARDING.md, CLAUDE.md banner. | `harness/retrieval/{steering,routing,postprocessors,__init__}.py`, `harness/indexing/{profiles,property_graph}.py`, `harness/tools/search.py`, `harness/recipes/{config,build}.py`, `harness/agents/session.py`, `harness/configs/{routing/,index/,recipes/}`, `harness/prompts/agent_regulatory.md`, `scripts/backfill_doc_categories.py`, `tests/`, `docs/{RETRIEVAL,RECIPES,ONBOARDING}.md`, `CLAUDE.md` | Phase 3 | — |
| 2026-07-12 | **Added headless worked examples for the steering stack as Jupyter notebooks under `docs/examples/`** (no chat UI): `01_source_categories` (vocabulary + classifier + `:Document.category` backfill), `02_steered_retrieval` (Option A filters/quotas + Option B `LINKS_TO` expansion at the retriever level, incl. programmatic knob construction), `03_routing_and_full_agent` (Option C routing tables, `ema_search` standalone with steering notes, and the full `steered_agent` recipe via `build_recipe` → `AgentWorkflowAdapter.ainvoke` with structured-answer inspection + honest-stamping demo), plus `README.md` (prerequisites/run instructions). Notebooks are nbformat-validated, every code cell compile-checked (incl. top-level await), and the offline cells (classifier, router, routing decisions) smoke-ran with asserted outputs; checked in without outputs. Cross-linked from RETRIEVAL.md §7 + ONBOARDING.md file map. | `docs/examples/` (new), `docs/{RETRIEVAL,ONBOARDING}.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-12 | **Added Neo4j graph-inspection tooling + ran the LINKS_TO boilerplate audit — verdict: main-content scoping is working.** New read-only CLI `scripts/inspect_graph.py` (plain `neo4j` driver, no LlamaIndex import): `overview` (label/reltype census via `apoc.meta.stats`, category/source_type histograms, indexes), `links` (the boilerplate audit: kind/link_context/document_type histograms, in-degree concentration vs. the 94.4%-pre-scoping baseline, top in/out-degree, repeated anchors, random samples), `doc <id|substring>` (props + chunk counts + in/out edges), `cypher` (raw passthrough). Paste-ready Browser query collection `deploy/neo4j/inspect_queries.cypher` (census, audit, visual graph views, drill-downs) + README "Inspecting the graph" section (viewer = bundled Neo4j Browser at :7474). **Audit results (live graph, 99,520 edges):** top-10 targets absorb 3.3% / top-100 9.1% (vs 94.4% into 74 chrome targets pre-scoping), median in-degree 1; 54.6% `file_component` (doc cards→PDFs) + 35.8% `inline`; top anchors are topical hubs (committees, COVID) each linked once per source doc, no nav anchors; random samples substantive — the ema_scraper-ported scoped extractor was applied. **Side-finding: `:Document.category` is `(unset)` on all 79,882 docs — `scripts/backfill_doc_categories.py` has not been run, so category steering (filters/quota/routing) currently matches nothing on this graph.** | `scripts/inspect_graph.py` (new), `deploy/neo4j/{inspect_queries.cypher (new),README.md}` | Phase 3 | — |
| 2026-07-12 | **Expanded the source-category vocabulary from 5 → 13 categories after a sitemap/corpus audit, then ran the category backfill — `:Document.category` is now stamped on all 79,882 docs.** Audit first (per user instruction): EMA's live sitemap.xml is a flat Drupal URL list whose only taxonomy is the URL path families, which the corpus mirrors; a cross-tab showed the old 5-category rules left 63% of docs (50,059) in `other`, decomposing cleanly along EMA's own `/en/documents/<type>/` segments. New categories in `harness/retrieval/doc_categories.py`: `regulatory_overview` (topic pages), `regulatory_procedure` (PIP/orphan/PSUSA/referral/SMOP/withdrawal/DHPC/PRAC outputs, 20,227 docs), `herbal` (HMPC family), `glossary`, `meeting_doc` (agendas/minutes/committee reports), `news` (news/events/press/newsletters/statements), `presentation`, `veterinary` (checked FIRST so vet content stays filterable for the human-only benchmark; includes `/documents/mrl-`). `epar` gained `chmp-annex` + non-`-epar-` `scientific-conclusion`. `other` is now 5,939 (7.4%; residue = documents/other, generic reports, templates, SOPs, procurement). Backfill run + verified in Neo4j (histogram above; random spot-checks correct). Vocabulary echoes synced: routing/default.yaml comment, agent_regulatory.md steering paragraph (teaches the new category semantics), RETRIEVAL.md §7, CITATIONS.md; quota test fixed to scale with `len(CATEGORIES)`. Tests: 565 passed; only pre-existing `test_mongo_source.py` failures (Mongo container not running on this host). | `harness/retrieval/doc_categories.py`, `harness/prompts/agent_regulatory.md`, `harness/configs/routing/default.yaml`, `tests/{test_doc_categories,test_retrieval_steering}.py`, `docs/{RETRIEVAL,CITATIONS}.md` | Phase 3 | — |
| 2026-07-12 | **Enriched `:Document` nodes with three authoritative EMA-published labels the URL heuristics couldn't provide — extraction + ingest wiring + backfills, all verified live.** **(1) Page badges (HTML):** confirmed the `ema-bg-category`/`ema-bg-topic` header badges are present in our scraped `web_items.html_raw` (93% of pages) — `harness/indexing/badges.py` extracts `audience` (Human/Veterinary/Corporate/Herbal) + `site_topic` (EMA's curated subject taxonomy, NOT URL-derivable), scoped to `main-content-wrapper` like `links.py`; wired into `ingest.build_ingested_doc` + `property_graph._entity_for`; `scripts/backfill_doc_badges.py` stamped 20,386 audiences (93%) + 5,947 site_topics (27% — the page's OWN header badge, not listing-card badges) across 21,957 HTML nodes. **(2) Document type (PDFs):** investigated EMA's website-data JSON export (download-website-data page → `documents-output-json-report_en.json`, 37 MB, 69,989 records); it ships malformed (array elements space-separated not comma-separated + unescaped quotes in `name`), so `harness/indexing/doc_types.py` brace-splits + regexes the two quote-free fields (`document_url`,`type`) instead of json.loads. Hashing `document_url`→`doc_id` joins EMA's authoritative `type` (85 values) to **96.6% of PDF nodes** (55,930/57,925) — the doc-type signal PDFs lacked (`LINKS_TO` edge `document_type` only reached card-linked PDFs). `scripts/backfill_doc_types.py` (downloads to `.tmp/`, not committed) stamped `d.doc_type` on 55,930 PDFs. Complementary split verified: PDFs=category+doc_type, HTML=category+audience+site_topic. `scripts/inspect_graph.py overview` + `deploy/neo4j/inspect_queries.cypher` now surface all three. All ADDITIVE metadata — not yet wired into a steering knob (gated on a benchmark failure per the complexity rule). New offline tests (badges 6, doc_types 5) pass; ingest/category suites green; ruff clean; mypy clean on new files (baseline errors elsewhere unchanged). Docs: RETRIEVAL.md §7 (new subsection). | `harness/indexing/{badges,doc_types}.py` (new), `harness/indexing/{ingest,property_graph}.py`, `scripts/{backfill_doc_badges,backfill_doc_types,inspect_graph}.py`, `tests/test_{badges,doc_types}.py` (new), `deploy/neo4j/inspect_queries.cypher`, `docs/RETRIEVAL.md` | Phase 3 | — |
| 2026-07-12 | **Documented the deferred half of the metadata-enrichment work — the steering uses I deliberately did NOT wire in are now a visible, execution-ready plan.** New `docs/next/metadata_steering.md` (matching the folder's Why/What-exists/Design/Steps/Triggers format): four independent, cheapest-first uses of the just-landed `doc_type`/`audience`/`site_topic` properties — (A) ground `category` on `doc_type` instead of URL substrings, (B) replace the URL-based `veterinary` rule with the `audience` badge, (C) route on `site_topic` (a topical axis `category` can't express), (D) doc-type-granular `doc_type_priority` — each gated on a concrete benchmark failure per the complexity rule, with the coverage-asymmetry / sparsity / snapshot-drift risks spelled out. Registered in `docs/next/README.md` table + `docs/ONBOARDING.md` §10 "Possible improvements"; the RETRIEVAL.md §7 parenthetical now links the plan instead of vaguely gesturing at it. Docs-only. | `docs/next/metadata_steering.md` (new), `docs/next/README.md`, `docs/ONBOARDING.md`, `docs/RETRIEVAL.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-12 | **Wrote a design plan for a GraphRAG × MCP comparison-and-complementarity benchmark** (from a discussion of the openpharma-org MCP servers + BioClaw). New `docs/next/graphrag_vs_mcp_benchmark.md` (matching the folder's Why/What-exists/Design/Architecture/Steps/Open-decisions format): four system arms as recipes — Vector RAG (exists), GraphRAG (`ontology_query` over `build_cypher_template_retriever` + the deferred Layer-2 ontology extraction), MCP (`ema_info` from openpharma's EMA server), Hybrid — evaluated over two benchmark slices (existing process/narrative `benchmark.jsonl` + a new MCP-auto-generated product/safety `benchmark_mcp.jsonl`) on a metric frontier (correctness/faithfulness/completeness/freshness/provenance/cost/lift). Surfaces the honest coverage-mismatch (our benchmark is procedure-text, `ema_info` serves product/safety records → MCP arm ≈0 on Slice P is a *designed finding*), the Slice-S circularity guard, MCP-as-partial-oracle for an ontology-quality sub-study (answers "can MCP benchmark the ontology"), and a record/replay cache (`harness/eval/mcp_cache.py`) for reproducible offline eval. Thesis: Hybrid ≥ max(arms) + lowest cross-type variance. Phased + gated per the complexity rule (Phase 2 baseline map justifies building B/C/D); flags the missing `llama-index-tools-mcp` dependency. Registered in `docs/next/README.md` table (and retired the R4 "MCP policy surface" candidate note). Docs-only — no implementation. | `docs/next/graphrag_vs_mcp_benchmark.md` (new), `docs/next/README.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-13 | **Made the authoritative EMA labels canonical in Mongo + added the one scraper→Neo4j pipeline entry point — closing the loose end where `doc_type` lived only on the graph (a rebuild silently lost it) and badges were re-derived at every ingest.** New Mongo collection `ema_scraper.document_metadata` (one row per URL: `doc_type`/`audience`/`site_topic` + per-label-group provenance timestamps; `harness/indexing/document_metadata.py` — writers, bootstrap, lookup). `scripts/enrich_document_metadata.py` is the post-scrape enrichment step (badges from `web_items.html_raw`; `doc_type` from the 36.8 MB EMA JSON export, parse now url-keyed `parse_document_types_by_url`); ingest **joins** the row per URL (live-badge fallback + miss-count warning when unenriched), `property_graph._entity_for` now stamps `doc_type` too (previously backfill-only — new builds LOST it), and `_DOC_PROJECTION`/`_node_from_row` carry all three labels on retrieved nodes. `scripts/propagate_metadata_to_graph.py` patches an existing graph from the collection (replaces the deleted `backfill_doc_{types,badges}.py`, which re-derived from raw sources). **`scripts/update_graph.py`** = the centralized pipeline (parse-html/parse-pdfs → enrich → build [→ propagate], each stage its own re-runnable CLI, `--dry-run` prints commands). **Ran live:** enrichment wrote 92,630 rows (22,743 badge + 69,887 doc_type); propagation reproduced the 2026-07-12 coverage exactly through the canonical path (doc_type 55,930/57,925 PDFs; audience 20,386 + site_topic 5,947 / 21,957 HTML). Tests: new `tests/test_document_metadata.py` (7) + ingest-join/fallback tests + url-keyed doc_types tests + shared `tests/conftest.py` (mongomock↔pymongo4.7 bulk-`sort` shim); 592 passed; ruff clean on touched files; mypy baseline unchanged. Docs: RETRIEVAL.md §2 (new flow diagram + 4-collection table) / §5 / §6 (retitled "Build, update + retrieve" — update_graph runbook + when-to-run-what) / §7 (canonical-collection rewrite); metadata_steering.md §2 corrected (its "stamped at ingest" claim was wrong for doc_type); CLAUDE.md data-sources bullet. | `harness/indexing/{document_metadata.py (new),ingest,doc_types,property_graph}.py`, `scripts/{enrich_document_metadata,propagate_metadata_to_graph,update_graph}.py` (new), `scripts/backfill_doc_{badges,types}.py` (deleted), `tests/{test_document_metadata.py,conftest.py} (new), tests/{test_doc_types,test_indexing_ingest}.py`, `docs/RETRIEVAL.md`, `docs/next/metadata_steering.md`, `CLAUDE.md` | Phase 3 | — |
| 2026-07-13 | **Ran the T2 reachability check live (PASSED) and wrote the topic-subgraphs work item — the hub-seeded, metadata-qualified subgraph design is execution-ready for a fresh session.** Live evidence against the newly-labeled graph: the 3 gold docs behind all 10 T2 benchmark items share a **1-hop** `LINKS_TO` ancestor of exactly the expected identity ("Referral procedures: human medicines", `category=regulatory_overview`); its qualified 2-hop subgraph is a coherent ~100-doc bundle; the GVP hub proved 2 hops are required (22 docs @1 hop → 143 @2); the `regulatory_overview` qualifier is what isolates true hubs from news-page noise; raw fanout is a trap (GVP *archive* out-fans the current page 126:22). New plan `docs/next/topic_subgraphs.md`: config seed list (`configs/hubs/*.yaml`, proposed/confirmed statuses, per-hub walk params), explainable qualified-fanout hub detection in plain Cypher (HITS/Louvain via GDS explicitly deferred — plugin not installed, verified), membership stamped as `topic_hubs` through the SAME canonical rails as the labels (document_metadata → propagate → ingest join), query-time = indexed lookup + pageable `topic_context` tool (topic map of identities, count + truncated flag) + optional budgeted best-chunk text context; LLM node summaries deferred (EMA titles are descriptive); Neo4j Browser (already click/hover) + inspect_queries.cypher for curation — bespoke viz UI ruled overengineering, NeoDash the 80/20 if ever wanted. Registered in `docs/next/README.md` (retires the R1-Q2 graph-navigation candidate note). | `docs/next/{topic_subgraphs.md (new),README.md}`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-13 | **Documented how to connect to the Neo4j Browser locally and via SSH tunnel** (from a live debugging session: UI is HTTP :7474 but the database connection is bolt :7687 *from the client machine* — tunneling only 7474 loads the page and then fails with misleading SSO-discovery console noise). deploy/neo4j/README.md gains a "Connecting to the Neo4j Browser" section (both-port tunnel command, bolt:// over neo4j:// through tunnels since routing discovery can bypass them, which console lines are harmless); RETRIEVAL.md §10 gains the matching troubleshooting entry. | `deploy/neo4j/README.md`, `docs/RETRIEVAL.md` | Phase 3 | — |
| 2026-07-13 | **Implemented the topic-subgraphs work item, steps 1–4 (offline) — precomputed hub-seeded topic context is now code, not plan; live build + T2 eval (steps 5–6) remain for the GPU host.** (1) *Seed list + loader*: `harness/configs/hubs/default.yaml` (referral_procedures confirmed, per-hub walk params) + `harness/retrieval/hubs.py` (strict schema/vocab/qualifier validation — categories-OR-doc_types enforced, unqualified walks rejected; `$EMA_CONFIG_DIR/hubs/` shadowing; build-relevant `config_hash`; comment-preserving textual `confirm`/proposal-append helpers). (2) *Membership build*: `harness/indexing/subgraphs.py` (bounded qualified-walk Cypher — EVERY path node qualified, audience-excluded; explainable propose scoring: curated-context links ×2, archive-title penalty ×0.1 strong enough to invert the §2 GVP-archive trap) + `document_metadata.upsert_topic_hubs` (third field group; `$pull`-then-`$addToSet` so single-hub rebuilds clear lost membership without wiping other hubs' stamps) + `scripts/manage_topic_hubs.py` (propose|confirm|report|build) + propagate script carries `topic_hubs` + `update_graph.py` step `subgraphs` + Browser curation queries (inspect_queries.cypher §5). (3) *Node metadata*: `_DOC_PROJECTION`/`_node_from_row` (`topic_hubs`, [] when unstamped), `_entity_for` + ingest join persist membership (+ `revision`) on new builds; retriever exposes public `store`/`embed_model`. (4) *Tool + recipe surface*: `harness/retrieval/subgraphs.py` (`TopicSubgraphReader` — members/memberships/scores/best-chunks Cypher, `SubgraphPolicy`) + `harness/tools/topic_context.py` (pageable query-ranked topic map, honest total+truncated header, PDF revisions grouped under detail pages, hub-key > document-membership resolution with embedding pick on multi-membership, budgeted `chunks` mode feeding the shared capture sink with `retrieval_origin="topic_subgraph"`) + `retrieval.subgraph` recipe keys (stamped iff the tool is in the toolset; keys-without-tool is a config error) + `topic_agent` recipe + `agent_topic.md` prompt. Tests: `test_retrieval_hubs` (16), `test_indexing_subgraphs` (12, mongomock), `test_tools_topic_context` (17, fake reader) + extensions to property_graph/recipes/tools tests; **642 passed**, ruff + mypy clean on new modules. Docs: RETRIEVAL.md §7.1 (status-honest), topic_subgraphs.md status flip. | `harness/retrieval/{hubs,subgraphs}.py` (new), `harness/indexing/{subgraphs.py (new),document_metadata,property_graph,ingest}.py`, `harness/tools/{topic_context.py (new),search,__init__}.py`, `harness/recipes/{config,build}.py`, `harness/agents/session.py`, `harness/configs/{hubs/default.yaml,recipes/topic_agent.yaml}` (new), `harness/prompts/agent_topic.md` (new), `scripts/{manage_topic_hubs.py (new),propagate_metadata_to_graph,update_graph}.py`, `deploy/neo4j/inspect_queries.cypher`, `tests/` (3 new files + 4 extended), `docs/RETRIEVAL.md`, `docs/next/topic_subgraphs.md` | Phase 3 | — |
| 2026-07-13 | **Ran topic-subgraphs steps 5–6 live on the GPU host — the mechanism works and wins on T2; the cross-type no-regression sweep is blocked on API credits.** Step 5: `manage_topic_hubs.py report` walked the confirmed `referral_procedures` hub to 49 members (28 qa / 14 regulatory_overview / 6 scientific_guideline); the §2 reachability check re-run through the shipped code **PASSED** (all 3 T2 gold docs are members); `build` stamped 49 membership rows (config_hash `bd29a77fd0b6`), propagate patched `:Document.topic_hubs` on the live graph (verified by Cypher); live `topic_context` smoke test renders the query-ranked, detail-page-grouped map with honest pagination. Step 6 (`scripts/run_eval.py`, 10 T2 items each, claude_opus + mlflow.genai judges): **`topic_agent` 5.000 correctness / 5.000 faithfulness vs `steered_agent` 4.700 / 4.900** — trace inspection shows the agent called `topic_context` on 6/10 questions, and both baseline drops (correctness 3 and 4) were exactly the predicted cross-sibling completeness failures (fee questions answered from one Article-3x sibling while the gold answer needs the others). MLflow runs `30ceab33…` (topic_agent) / `218c45c3…` (steered_agent), tagged ema.recipe/ema.question_type. **T1/T3/T4 runs for both recipes aborted at question 1 — Anthropic API credit balance exhausted**; rerun after top-up. Implementation committed as `199f995`; plan status updated. | `docs/next/topic_subgraphs.md`, `.claude/HISTORY.md`, Mongo `document_metadata` (49 rows), Neo4j `:Document.topic_hubs` | Phase 3 | — |
| 2026-07-13 | **Completed the topic-subgraphs eval runs (as far as budget allowed) and wrote the detailed results report.** Sonnet-5 support required two `harness/llms.py` shims (models.yaml `claude_sonnet` entry; drop the Claude-5-deprecated `temperature` field; metadata fallback when llama-index's static model table doesn't know the id — without it the context lookup raises and `is_function_calling_model` silently disables tools; 4 new offline tests in `tests/test_llms.py`). Results: **T2 head-to-head (Opus): topic_agent 5.000/5.000 vs steered_agent 4.700/4.900** — both baseline drops are the predicted cross-sibling completeness misses on the two fee questions, and topic_agent used `topic_context` on exactly those (6/10 T2 overall). Cross-type topic_agent sweep (Sonnet 5, Opus judges): T1 4.26/4.53 (tc on 9/20), T3 4.60/4.60 (5/10), T4 3.80/5.00 (1/5) — no collapse; T1/T3 drops concentrate in worksharing questions where no subgraph exists yet. Operational: one T4 attempt hung ~70 min (stuck HTTP call, no timeout — clean rerun fine; recommend a client-side timeout in llms.py); the first sweep died on API credit exhaustion mid-judging (partial run `5ba51e5c` kept, 17/20 judged). Full background + per-item tables + honesty section: `docs/eval/2026-07-13_topic_subgraphs.md`. **Open:** steered_agent T1/T3/T4 baseline for the formal no-regression verdict. | `docs/eval/2026-07-13_topic_subgraphs.md` (new), `harness/llms.py`, `harness/configs/models.yaml`, `tests/test_llms.py` (new), `docs/next/topic_subgraphs.md` | Phase 3 | — |
| 2026-07-13 | **Wrote the topic-subgraphs follow-ups plan** — the eval's four next steps as an execution-ready `docs/next` entry: (1) `steered_agent` T1/T3/T4 baseline for the formal no-regression verdict (one command, ±0.2 neutrality band question flagged); (2) more hubs, worksharing first — justified by the observed failure concentration (29-search T1 flail, T3 3–4s, the correctness-1 T4 item), not anticipation; (3) 2–3 cross-family T2 benchmark items (write-then-never-tune rule); (4) client-side LLM request timeout in `harness/llms.py` (the observed 70-min silent hang; per-request not per-turn). README table updated: topic_subgraphs flipped to "landed (steps 1–6; eval partial)". Docs-only. | `docs/next/{topic_subgraphs_followups.md (new),README.md}`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-13 | **Made the topic-subgraphs work discoverable: a runnable walkthrough notebook + roadmap reconciliation** (user feedback: "hard to understand what you did from the docs alone"). New `docs/examples/04_topic_subgraphs_eval.ipynb` (executed headless end-to-end, outputs committed): the 45 benchmark items + the exact 10 T2 questions with gold answers; the live hub walk re-checking the 3-gold-docs gate (PASS, 49 members); the `topic_context` catalog as the agent sees it; how `run_eval.py` asks + judges (predict_fn, per-type MLflow runs, Opus judges); and the reported result tables reproduced from `mlflow.db` per-item (scores + tool usage), with the stray-run exclusions documented (partial `5ba51e5c`, smoke `c57fb04a`). Optional live-agent cell behind a RUN_LLM=False flag. Registered in `docs/examples/README.md`. `project_roadmap/ROADMAP.md` reconciled to 2026-07-13: Phase 3 eval runner flipped to runtime-verified with the first recorded recipe comparison linked; bottom line now points to `docs/next/` + `docs/eval/`. | `docs/examples/{04_topic_subgraphs_eval.ipynb (new),README.md}`, `project_roadmap/ROADMAP.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-13 | **Readability pass over the core reader docs** (user request: make the docs easier to understand — especially the current project stage — with simpler language, and surface the notebooks more). Restructured each doc current-state-first and moved dated history + work-unit IDs (LIR/MIGR) into Markdown footnotes: README's five status paragraphs collapsed to a one-line summary + a phase table; ROADMAP's two overlapping top banners merged into one banner + phase table; RETRIEVAL/ARCHITECTURE/CITATIONS/SETUP/AGENTIC_GUIDE intro banners slimmed; `docs/next/README` table one-liners shortened + reordered by how-live; `topic_subgraphs` status turned into a scannable built/evaluated/remaining block. Added "Try it live" notebook cross-links (notebooks 02/03) in RECIPES + RAG_TECHNIQUES, a top-of-file notebook callout in RETRIEVAL + ONBOARDING, and a `docs/examples` entry in the README Quick links. Corrected one stale status: RETRIEVAL §7.1 said topic-subgraphs was "NOT yet built or verified live" — changed to "built and evaluated live" to match `topic_subgraphs.md`/ROADMAP/notebook 04. Docs-only; all footnotes balanced and new links verified. | `README.md`, `docs/{ONBOARDING,RECIPES,RAG_TECHNIQUES,RETRIEVAL,ARCHITECTURE,CITATIONS,AGENTIC_GUIDE,SETUP}.md`, `docs/next/{README,topic_subgraphs}.md`, `project_roadmap/ROADMAP.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-16 | **Removed cleartext credentials from code and docs** (user: no passwords in code/documentation). Made secret reads fail-closed: `neo4j_store_from_env` (property_graph.py), `inspect_graph.py` `_driver`, and `app.py` `auth_callback` now raise if `NEO4J_PASSWORD`/`UI_PASSWORD` is unset instead of falling back to the dev-default `ema_nlp_dev_pw` / `"dev"`. Neo4j compose switched the `${NEO4J_PASSWORD:-ema_nlp_dev_pw}` defaults to `${NEO4J_PASSWORD:?...}` (fails to start if unset). Scrubbed the literal from `deploy/neo4j/README.md`, `docs/SETUP.md`, `deploy/neo4j/inspect_queries.cypher`, and the historical Postgres DSN in `DECISIONS.md` → placeholders. No tracked `.env`; remaining `ema_nlp_dev_pw` hits are only in untracked `.claude/settings.local.json` (local permission allowlist, never committed). | `app.py`, `harness/indexing/property_graph.py`, `scripts/inspect_graph.py`, `deploy/neo4j/{docker-compose.yml,README.md,inspect_queries.cypher}`, `docs/SETUP.md`, `DECISIONS.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **Critique of the topic-subgraphs eval documented + embed-model cache moved to EMA_DATA_DIR** (user challenged the benchmark's generalizability and the MockLLM/HF-Hub messages). Added report §8 + notebook §7: the T2 head-to-head is circular by construction (hub built and gated on the test set's gold docs), has no statistical power (2/10 discordant items, judge ceiling, Claude-judging-Claude), and leaves walk-transfer + membership-precision unproven — read it as mechanism validation, not generalization. Verified the MockLLM/HF messages did not affect the eval (real Anthropic LLMs via `get_llm_for_model`; eval recipes run `pipeline: none`; `llm_rewrite` raises rather than degrading) and documented that in both files. `configure_embed_model` now passes `cache_folder=$EMA_DATA_DIR/models/hf` (override `EMA_HF_CACHE_DIR`); existing 1.3 GB bge-large cache migrated from `~/.cache/llama_index` and offline load verified (`HF_HUB_OFFLINE=1`). Added a one-time log line explaining `Settings.llm = None` before LlamaIndex's "Using MockLLM" message. | `harness/providers.py`, `docs/eval/2026-07-13_topic_subgraphs.md`, `docs/examples/04_topic_subgraphs_eval.ipynb`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **Knowledge-base visualization + retrieval-chain HTML exports** (user: KB is hard to see, chains hard to follow; LightRAG-style map + readable chain HTML requested, scope confirmed via plan mode). Track A: `scripts/build_graph_map.py` builds a fully self-contained WebGL map of all `:Document` nodes + `LINKS_TO` edges (igraph DrL/FR layout offline, new `viz` extra; vendored sigma.js 2.4.0 + graphology 0.25.4 in `scripts/lib/graph_map/vendor/`; gzip+base64 payload; category legend/filters/search/detail panel; edges hidden at far zoom); NeoDash 2.4 added to the Neo4j compose (`:5005`) with a committed 4-page dashboard (`deploy/neo4j/neodash_dashboard.json`) + `scripts/seed_neodash.py` round-trip. Track B: `harness/tools/events.py` (`ChainStep`/`NodeRef`, ContextVar capture mirroring `capture_search_nodes`) instruments `ema_search`/`corrective_search`/`topic_context`; the adapter returns `chain_steps` and `ExportBundle.chain` carries them; new `chain_html` exporter (timeline + origin badges + new/cited flags + context-evolution table + inline-SVG turn subgraph behind `include_chain_graph`; raw outputs behind `include_chain_output`) is in the default export formats; `harness/export/chain_from_trace.py` + `scripts/render_trace.py` rebuild the same view from any MLflow trace or `--run-id` (span contract verified empirically on mlflow 3.14 and pinned by fake-span tests). 705 tests pass (30 new); live full-map build + NeoDash walkthrough pending `NEO4J_PASSWORD` (absent from env since the 2026-07-16 credential scrub). Docs: `docs/VISUALIZATION.md`. | `harness/tools/{events.py,search.py,corrective_search.py,topic_context.py}`, `harness/agents/workflow_adapter.py`, `harness/export/{bundle.py,base.py,chain_html.py,chain_from_trace.py,__init__.py}`, `harness/configs/export/default.yaml`, `app.py`, `scripts/{build_graph_map.py,render_trace.py,seed_neodash.py,lib/graph_map/}`, `deploy/neo4j/{docker-compose.yml,neodash_dashboard.json,README.md}`, `pyproject.toml`, `docs/VISUALIZATION.md`, `tests/` | Phase 3 | — |
| 2026-07-19 | **Env-file location moved to the synced dataset folder** (user moved `ema_nlp.env` to `~/Nextcloud/Datasets/ema_nlp/` on marvin-gpu). `config.py` now resolves the file through a search chain — `$EMA_ENV_FILE` → `~/Nextcloud/Datasets/ema_nlp/ema_nlp.env` (canonical) → `~/.myenvs/ema_nlp.env` (legacy fallback until all machines sync) — and exports `ENV_FILE`/`ENV_FILE_CANDIDATES`/`ENV_FILE_HINT`. `app.py` dropped its own hardcoded `load_dotenv` in favour of `import config`; all error messages/comments/docs pointing at `~/.myenvs/ema_nlp.env` updated to the new canonical path (harness llms/models/property_graph, scripts, compose, cypher comments, CLAUDE.md, DECISIONS.md, docs/SETUP.md + deploy READMEs + guide docs). `setup.sh` creates new env files at the canonical path (legacy detected and reused). The four example notebooks now `import config` after resolving the repo root instead of hardcoding the path — with the `NEO4J_URI` setdefault moved *after* the env load so an env-file URI keeps winning. Verified: search-chain resolution, ruff on all touched files, full suite 675 passed. | `config.py`, `app.py`, `harness/{llms,models,providers}.py`, `harness/indexing/property_graph.py`, `harness/configs/{models.yaml,index/neo4j_hier.yaml}`, `scripts/{setup.sh,inspect_graph,build_graph_map,seed_neodash,run_eval,run_agent_demo,update_graph}.py`, `deploy/neo4j/{docker-compose.yml,inspect_queries.cypher,README.md}`, `deploy/mongo/README.md`, `docs/{SETUP,AGENTIC_GUIDE,ARCHITECTURE,RETRIEVAL,RUNTIME_VERIFICATION}.md`, `docs/examples/` (4 notebooks + README), `CLAUDE.md`, `DECISIONS.md` | Phase 3 | — |
| 2026-07-19 | **Live verification of the visualization stack after the env file synced** (ema_nlp.env now at the canonical Nextcloud path; search chain resolves it). Found the project Neo4j container on this host maps alt ports (bolt 7688 / http 7475; the default 7687 is a native instance — earlier auth failures were against the wrong server). `build_graph_map.py` ran live against the container (this host holds the 8-doc verify subset; payload decodes with real data — the full 80k build belongs on marvin-gpu). NeoDash image tag fixed `2.4` → `2.4.11` (`2.4` has no manifest on Docker Hub); container up on :5005 (HTTP 200), `seed_neodash.py` seeded the `EMA KB` dashboard node (verified via cypher: title/version/content). Remaining manual check: open :5005, connect to `localhost:7688`, Load → Neo4j → "EMA KB". | `deploy/neo4j/docker-compose.yml`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **Visualization stack verified live on marvin-gpu — the steps the laptop couldn't run** (full graph + real MLflow traces live here; Neo4j on default ports). Full KB map built: 79,882 docs / 99,520 links → `results/graph_map/ema_kb_map.html` (5.1 MB self-contained, layout 88 s; embedded gzip payload decodes: 13 categories, 84 doc_types). NeoDash 2.4.11 up on :5005 (HTTP 200) + `EMA KB` dashboard seeded. `render_trace.py` verified against real traces and **two real bugs fixed** that fake-span tests couldn't catch: (1) `--run-id` failed on mlflow 3.14 because `search_traces` only looks in experiment 0 — now resolves the run's experiment and passes `locations=[experiment_id]`; (2) pre-chain-capture traces rendered with an empty question — `_find_answer` now falls back to autolog's `FunctionAgent.run` root-span `user_msg` input (new fake-span test pins it). All 5 traces of eval run `casual-lark-306` re-render with questions + 9–21 steps each. Full suite green on this host (669 passed + 7 mongo tests after starting `ema_mongo`; ruff/mypy clean on changed files). | `scripts/render_trace.py`, `harness/export/chain_from_trace.py`, `tests/test_chain_from_trace.py`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **NeoDash dashboard resized for laptop screens** (user: over `ssh -L 5005` the dashboard "does not fit" the laptop — root-caused via NeoDash 2.4.11 source: react-grid-layout is width-responsive (24 cols ≥1200 px) but `rowHeight` is a fixed 100 px/unit, so big-monitor card heights overflow laptop viewports; the tunnel is irrelevant). Card heights in `neodash_dashboard.json` reduced (bars/tables h6→4, drill-down graph h8→6, y-positions restacked): Census 9 rows ≈900 px, Link audit / Topic hubs 8 ≈800 px, drill-down cards each ≤600 px; widths untouched (viewport-responsive). Reseeded live. Docs §5 gains the fit note (fixed row height, zoom + per-report fullscreen fallback, keep relayouts via `--dump`). | `deploy/neo4j/neodash_dashboard.json`, `docs/VISUALIZATION.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **NeoDash load fixed: seeded node was missing its `uuid`** (user hit "A dashboard with UUID 'null' does not exist" — the seeder MERGEd by title and never set `uuid`, but NeoDash 2.4.11 (DashboardThunks.ts) MERGEs by `uuid` and the load dialog passes `n.uuid`, so the dashboard listed but couldn't load). `seed_neodash.py` now MERGEs on the JSON's `uuid` (falls back to a stable uuid5 of the title), sets it on the node, and deletes stale same-title uuid-less nodes; the committed JSON's existing `"uuid": "ema-kb-dashboard"` moved to the top and is the merge key. Reseeded + verified via cypher (one node, uuid set). Along the way: user's bolt auth failure diagnosed as the laptop's native Neo4j answering on the collided local 7687 — use `ssh -L 17687:localhost:7687` per docs §5. | `scripts/seed_neodash.py`, `deploy/neo4j/neodash_dashboard.json`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **Result artifacts moved to the Nextcloud-synced results dir via `config.RESULTS_DIR`** (user: laptop results irrelevant; wants results synced between machines; explicitly no symlinks — config-driven path). `config.py` adds `RESULTS_DIR` (`$EMA_RESULTS_DIR` → default `EMA_DATA_DIR/results` = `~/Nextcloud/Datasets/ema_nlp/results/`); `build_graph_map.py` + `render_trace.py` default their outputs there (explicit `--out` still wins); the repo-local `results/` dir is gone. Existing Nextcloud `results/` (May-era pre-refactor eval outputs, same content as the laptop's) archived to `results/archive_pre_refactor/`; marvin-gpu's `chains/` + `graph_map/` moved in; full KB map rebuilt at the new location. Docs updated (VISUALIZATION §1/§5 — "no tunnel/scp needed, Nextcloud syncs artifacts"; CLAUDE.md data conventions — RESULTS_DIR, MLflow stays system of record for metrics). Verified: both scripts write to the new default; related tests + ruff green. | `config.py`, `scripts/{build_graph_map.py,render_trace.py}`, `docs/VISUALIZATION.md`, `CLAUDE.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **KB-map viewer: boot failures now surface on-page** (user's laptop stuck on "Decompressing graph data…" — the async boot IIFE had no error handling, so any throw (truncated Nextcloud sync mid-flight, missing `DecompressionStream`, WebGL failure) left the loading message up forever with the error only in the console). Template adds `error` + `unhandledrejection` listeners writing into the loading box, an explicit DecompressionStream feature check (Chrome 80+/Firefox 113+/Safari 16.4+ message), and try/catch around atob + decompress/parse with a "file is likely truncated — re-sync and reload" hint. Full map rebuilt with the hardened loader (5,081,484 bytes — the reference size for checking sync completeness). | `scripts/lib/graph_map/template.html`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-19 | **KB-map render crash fixed: `renderer` TDZ in the sigma edgeReducer** (the hardened loader surfaced the real laptop error: "Cannot access 'renderer' before initialization" — sigma invokes the reducers during its *constructor's* initial render, and `edgeReducer` referenced the `const renderer` being constructed; only reached when the graph has edges, so the laptop's 8-doc verify build and unopened smoke builds never hit it — the full 99,520-edge map was the first real browser render). Fix: `let renderer = null` pre-declared + null guard in `edgeReducer` (null ⇒ initial zoomed-out frame ⇒ edges hidden, the intended first-frame behaviour anyway). **Verified headlessly this time**: 3,000-node/1,238-edge build rendered in `google-chrome --headless=new` — status line "3,000 documents · 1,238 links" present, loading div removed. Full map rebuilt. | `scripts/lib/graph_map/template.html`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-20 | **Live single-question T5 smoke — chain tree view verified on real data, and the seeding conclusion corrected** (WI-8 remainder; user chose the small half over the full 5-item run). `run_eval.py --recipe tree_agent --benchmark showcase.jsonl --types T5 --limit 1` ran clean (MLflow run `ade67c22`, correctness 5.000 / faithfulness 5.000); `render_trace.py --run-id` produced `chain_b17b4d2f.html` (219 KB) — **the layered site-tree SVG rendered real data for the first time**: viewBox 720×1360, 70 circles (54 retrieved docs + 16 grey section nodes), 69 tree edges, 26 dashed provenance edges, real ancestor labels (`ema.europa.eu → medicines → human → EPAR`, plus `documents/<type>` buckets), all three origin badges, 5 citations, headless-Chrome clean with no JS errors. **The run also falsified the step-1 seeding conclusion:** the chain's `args.query` per step shows the agent never searches the raw benchmark question — it reformulated into 5 targeted queries, the first (`Comirnaty COVID-19 vaccine authorisation`) landing the anchor at **vector rank 1**, with the anchor also arriving via `link_expansion` (step 4) and `tree_ancestor` (step 3). The probe had measured raw-question retrieval = the `naive_rag` path, not the configured recipe. Corrections written where the claims live: eval report §6 (what survives — the ANN artifact and the length-bias mechanism — vs what does not — the "seeding is the weak link" impact claim), plan §1b (re-scope: measure the agent path + `naive_rag` across all 5 anchors *before* building any fix), RETRIEVAL.md §7.2 banner, and the backlog (new #1 = the re-scope; ungating `oversample` demoted to #2 but kept as independently justified; #4/#5 marked gated on #1). Methodological note added for future probes: measure through the recipe's invoke path or state which path is being measured. | `docs/eval/2026-07-20_tree_seeding.md`, `docs/next/tree_retrieval_followups.md`, `docs/RETRIEVAL.md`, `BACKLOG.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-20 | **`BACKLOG.md` — one ranked queue replaces seven competing trackers** (user: too many open tasks, loses track of previous ones while planning new; asked whether GitHub issues would help — recommended against for now: `gh` not installed and push blocked on marvin-gpu, solo project, and issues can't close atomically in the same commit as the code; revisit if a second contributor or mobile capture appears). Surveyed the actual state: 36 open-ish items scattered across ROADMAP / `docs/next` / OPEN_QUESTIONS / REQUIREMENTS_REVIEW / `.claude/work` — and `ACTIVE_WORK` still pointed at a 2026-06-04 work unit six weeks later, the exact failure mode described. New root `BACKLOG.md` is the single ranked queue: **`Now` hard-capped at 3** (the mechanism that forces "what about the other thing?" at planning time), then `Next` (ordered), `Later/Parked` **with the reason each is parked**, `Blocked` (the 8 unpushed commits / missing marvin-gpu credentials), and the live `OPEN_QUESTIONS` entries as a decide-don't-build table. Four rules: a row is a pointer never a copy; nothing is open without a row; closing = delete row + append HISTORY in the same commit; phase status stays in ROADMAP, item status here. Seeded with the 4 tree-retrieval follow-up steps, closed-book lift, topic-subgraph follow-ups, and the parked designs (metadata steering, miss detection, GraphRAG/MCP, Phase 4 ablations blocked on lift, DSPy bootstrap needing ≥50 rated examples). CLAUDE.md gained a **session-start rule** to read the backlog before planning and to maintain it as work happens (this is what makes it enter context automatically). De-duplicated the competing trackers: ROADMAP banner now defers to the backlog for item status, REQUIREMENTS_REVIEW banner marks F1–F20 historical/not-a-queue, OPEN_QUESTIONS notes the backlog owns liveness, `ACTIVE_WORK` retired with an explanatory stub, `docs/next/README` relabelled a catalogue rather than a queue, README gained a "what's open right now" entry point. All links verified. | `BACKLOG.md` (new), `CLAUDE.md`, `README.md`, `OPEN_QUESTIONS.md`, `project_roadmap/ROADMAP.md`, `docs/{REQUIREMENTS_REVIEW.md,next/README.md}`, `.claude/work/ACTIVE_WORK`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-20 | **T5 anchor-seeding measured (follow-up plan step 1) — the miss is three problems, not one** (user: run step 1). Read-only probe over all 5 showcase anchors on the live graph: anchor rank in the final result, in a k=500 candidate pool, branch coverage, origin mix, plus an ANN sweep at k=10/40/100/200/500. Findings: **(a)** T5-005's anchor is the *true* rank-5 chunk yet invisible at k≤100 — Neo4j's vector index is approximate, so a doc can be near-top-ranked and unreachable at production k; compounded because `oversample` currently applies **only** when a category filter/quota is active, which `neo4j_tree` deliberately disables (so it runs at raw k=10 and every measured rank is a lower bound). **(b)** two anchors are genuine scoring failures (rank 58, ~99). **(c)** two are genuine recall failures (absent from the top 500) — the length-bias hypothesis confirmed but only for 2/5, not the blanket cause claimed earlier. **(d)** the ancestor pass already partially compensates: T5-002/T5-003 got their anchor in at rank 11 **via `tree_ancestor` rather than vector search** (T5-003's hub is absent from the top 500 yet still reached the agent, walked up from its retrieved PDF children), with 11/12 and 10/13 final nodes on the anchor branch; it fails only where nothing on the branch is retrieved. Deliverable `docs/eval/2026-07-20_tree_seeding.md` (method, both tables, consequences, plus honesty limits: n=5, anchor-reach ≠ answer quality, k=500 is itself an ANN bound). Plan updated: step 1 ✅, step 2 re-ordered with a NEW cheapest step in front — ungate oversampling and re-baseline the sweep — then title/short-doc boost, depth signal (noted: re-orders the pool only, can't fix (c) alone), anchor-then-expand last. RETRIEVAL.md §7.2 corrected (it had attributed the whole miss to length bias). | `docs/eval/2026-07-20_tree_seeding.md`, `docs/next/{tree_retrieval_followups.md,README.md}`, `docs/RETRIEVAL.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-20 | **Per-component reference docs (`docs/tools/`) + tree-retrieval follow-up plan** (user: detailed documentation for all new features, one file per tool with a central index; plus next steps in the project's own documentation convention). New `docs/tools/` (8 files, ~870 lines): `README.md` index (tool table + retrieval-component table + the wiring path + the conventions each doc follows); one file per agent tool — `ema_search.md` (signature, the description the LLM actually reads, output tags incl. `via=`/`path=`, steering precedence, unfiltered-retry + invalid-category failure modes), `corrective_search.md` (the bounded grade→rewrite loop with best-so-far and the tie rule, the separate cheap grader model, cost note), `topic_context.md` (hubs vs subgraphs, map/chunks modes, resolution order, curation gate, honest post-hoc-lossiness note, the 2026-07-13 eval with its critique), `resolve_substance.md` (incl. the "AI = Acceptable Intake" acronym defence); plus the components below the tools — `retriever.md` (all four passes with the Cypher shapes, the full node-metadata contract, every config key + the three shipped profiles and why `neo4j_tree` drops the category filter, prerequisites table, graceful degradation), `site_tree.md` (the generic parent-signal algorithm, `TreeRecord` API, backfill + staleness rule, live shape 79,882/depth-7/55,911 doc-parented, hand-verification Cypher), `chain_events.md` (ChainStep/NodeRef fields, the origin vocabulary, the site-tree view, post-hoc rebuild limits, and the two-sink contract for adding a tool). Follow-up plan `docs/next/tree_retrieval_followups.md` in the established docs/next shape (why → what exists → steps → open decisions → success criteria): step 1 measure the anchor-rank miss before changing anything, step 2 hub-aware seeding (title/short-doc boost → depth-aware scoring → anchor-then-expand, cheapest first), step 3 the deferred `tree_context` tool, step 4 freeze the traversal shape as a structural regression fixture; open decision flags that hub-likeness must stay graph-derived, never a category literal. Indexes updated: `docs/next/README.md` table row, `docs/RETRIEVAL.md` banner and `README.md` doc list now point at `docs/tools/`. All 32 internal links in the new docs verified to resolve. | `docs/tools/` (8 new files), `docs/next/{tree_retrieval_followups.md,README.md}`, `docs/RETRIEVAL.md`, `README.md`, `.claude/HISTORY.md` | Phase 3 | — |
| 2026-07-20 | **Tree-aware retrieval + tree-view chain export + T5 link-traversal showcase** (user: the radial tree is the right model for retrieval — "a recursive retriever should traverse this tree up to the root with awareness of each level"; links between documents represent organizational knowledge, generically). Plan-mode design (3 explore agents + plan agent); user chose retriever-level traversal (no new agent tool yet) + layered left-to-right chain SVG. **WI-1** `harness/indexing/site_tree.py`: the tree derivation factored out of `build_graph_map.py` (re-exported; `tests/test_graph_map.py` passes unchanged = behavior-neutral) + `derive_tree_records` (parent/depth/path/ancestors) + `layered_positions`; docstring states the generic algorithm (parent-signal priority chain: breadcrumb → first structural linker → flat bucket; BFS-layering degradation for link-only corpora; levels = emergent sibling sets, never hardcoded categories). **WI-2** `scripts/backfill_site_tree.py` stamps `tree_parent_id/tree_depth/tree_path/tree_ancestor_ids` on `:Document` (precompute-then-lookup, like topic_hubs); new `tree` step in `update_graph.py` DEFAULT_STEPS. **WI-3** retriever: tree fields in `_DOC_PROJECTION`/`_node_from_row`, new `_ANCESTOR_QUERY` + `_expand_ancestors` (nearest-first, capped, additive, `retrieval_origin="tree_ancestor"` + `linked_from` seeds; pure lookup, no traversal query), `graph.ancestors`/`max_ancestors` config, `format_nodes` shows `path=/<tree_path>` so the agent sees each hit's level. **WI-4** `NodeRef` += `topic_path`/`source_type` (+ topic_context/`_MEMBER_FIELDS` stamping, post-hoc recovery in `chain_from_trace`). **WI-5** `_subgraph_svg` (igraph FR/circle) → `_tree_svg`: retrieved docs placed in the site tree, greyed section path to root, nothing else drawn; dashed provenance overlay; `tree_ancestor` badge/stroke; exporter no longer needs igraph. **WI-6** `neo4j_tree` profile (expand any-category max 6 + ancestors 3; steered's category filter would drop EPAR/news) + `tree_agent` recipe. **WI-7** `benchmark/showcase.jsonl` (5 T5 items, hub anchors picked by documented fan-out query; all 8 gold URLs verified present in the graph), `QUESTION_TYPES += T5`, SCHEMA.md T5 section, RETRIEVAL.md §7.2, VISUALIZATION.md. 698 tests pass (22 new; the 2 old SVG tests deliberately replaced). **Live (marvin-gpu):** backfill 79,882 records, max depth 7, 55,911 doc-parented; comirnaty page at `medicines/human/EPAR/comirnaty` depth 4 with 51 adopted PDF children; live retrieve returns vector 10 / link_expansion 5 / tree_ancestor 3. **Honest first T5 failure documented** (RETRIEVAL.md §7.2): broad "summarize Comirnaty" seeds on CHMP/COMP minutes instead of the hub (short navigational hub pages lose leaf-chunk cosine to long minutes) — seeding, not traversal, is the weak link; this is the benchmark failure that would justify hub-aware seeding or the deferred `tree_context` tool. | `harness/indexing/{site_tree.py,property_graph.py,profiles.py}`, `harness/export/{chain_html.py,chain_from_trace.py}`, `harness/tools/{events.py,search.py,topic_context.py}`, `harness/retrieval/subgraphs.py`, `harness/eval/runner.py`, `harness/configs/{index/neo4j_tree.yaml,recipes/tree_agent.yaml}`, `scripts/{backfill_site_tree.py,update_graph.py,build_graph_map.py}`, `benchmark/{showcase.jsonl,SCHEMA.md}`, `docs/{RETRIEVAL,VISUALIZATION}.md`, `tests/` (7 files) | Phase 3 | — |
| 2026-07-20 | **KB map `--tree` mode: the whole corpus as one radial tree rooted at ema.europa.eu** (user proposal). Data check first: `topic_path` is a real breadcrumb for the 21,957 HTML docs but only a flat `documents/<type>` bucket for the 57,925 PDFs — so the tree uses breadcrumbs for pages (a page whose path *is* a section occupies that node) and hangs each PDF under the first HTML page that `LINKS_TO` it (deterministic; unlinked → bucket fallback), making the link graph literally the tree signal. `build_tree` + `tree_layout` (radial: depth→radius, span ∝ subtree docs, leaf fans arc-packed; pure Python, no igraph) + payload extensions (`tree_edges` uncounted for in-degree, `section_sizes` = child count); template gets a "site section" color and filter exemption for skeleton nodes. Test-caught bug: PDF bucket paths must not be stripped to `segs[:-1]` like page breadcrumbs. 3 new tests (8 pass); full 80,029-node/179,548-edge build renders headlessly; `ema_kb_tree.html` (5.4 MB) alongside the force-layout map. | `scripts/build_graph_map.py`, `scripts/lib/graph_map/template.html`, `tests/test_graph_map.py`, `docs/VISUALIZATION.md`, `.claude/HISTORY.md` | Phase 3 | — |
