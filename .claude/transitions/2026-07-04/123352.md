# Handoff: 2026-07-04 12:33 UTC — REQUIREMENTS_REVIEW findings refactor (machine switch)

**Branch:** `claude/agentic-rag-foundation` — everything below is committed & pushed.
**Driving document:** `docs/REQUIREMENTS_REVIEW.md` §9 findings table (F1–F20). The owner
answered the open questions in commit `55eac9f`; this session started working down the
findings. **Full offline suite green: 416 passed / 2 skipped** (only `tests/test_mongo_source.py`
needs the live MongoDB container).

## Done this session (in the commit accompanying this handoff)

- **F1 fixed — acronym expansion was silently dead, now live.**
  - `ablations/A_evidence_filter/acronym_dict.yaml` → `harness/configs/retrieval/acronyms.yaml`
    (packaged, no CWD-relative path); `harness/ablations/a1_query_expansion.py` →
    `harness/retrieval/acronyms.py` (`QueryExpander`, context-aware, handles the
    list-shaped YAML that the old dict-parse swallowed). `harness/ablations/` and
    `ablations/` deleted.
  - `harness/retrieval/transforms.py`: the `acronym` transform with `acronyms=None`
    (the config-driven default) now uses `QueryExpander` over the shipped dictionary
    and **raises if the dictionary is missing** (no silent no-op); an explicit mapping
    (tests) still does plain word-boundary substitution.
  - `harness/agents/session.py`: dropped `_load_default_acronyms()`; passes
    `acronyms=None` through so the transform owns the default load.
  - Tests: `tests/test_ablations.py` → `tests/test_retrieval_acronyms.py` (expanded).
- **F2 fixed** — `harness/agents/registry.py`: strict `get_output_schema()` (unknown name
  = hard `KeyError`, no silent `RegulatoryAnswer` fallback), plus
  `register_output_schema()` / `list_output_schemas()`; `Substance` registered as a second
  schema. Exported from `harness.agents`.
- **F3 HALF fixed — ⚠️ this is the exact stopping point, see Next steps.**
  `harness/eval/judges.py` `_VAR_MAP`: `{{context}}` now maps to `outputs` (was `inputs`).
  The comment there claims `build_predict_fn` returns `context_passages` — **that half is
  NOT done yet**: `harness/eval/predict.py` still returns only
  `{answer, citations, num_claims, confidence}`.
- **F13 fixed** — `harness/agents/workflow_adapter.py` no longer stamps
  `ema.index.profile` from the process-global env; the recipe's resolved attributes carry
  the honest `ema.retrieval.index_profile`.
- **F14 fixed** — new `harness/obs/tracing.py:tag_current_trace()`; the adapter stamps
  `ema.recipe` as a **trace-level tag** (searchable via `mlflow.search_traces`), not just
  a child-span attribute.
- **F15 fixed** — new `harness/obs/runs.py:default_experiment()`
  (`EMA_MLFLOW_EXPERIMENT` env, else `ema_nlp`); consumed by `app.py`,
  `scripts/run_agent_demo.py`, `harness/eval/evaluate.py`, `AgentSession.arun`. No more
  experiment-name drift between app / demo / eval.

## Next steps (in order)

1. **Finish F3** in `harness/eval/predict.py`: `predict_fn` must return the retrieved
   passages as `context_passages` in the prediction dict so the offline faithfulness
   judge grades against real retrieval. The passages aren't on `RegulatoryAnswer` —
   they come from the retrieval capture (`harness/tools/search.py:capture_search_nodes`,
   which `AgentWorkflowAdapter.ainvoke` already uses to build its `context_passages`
   result key). This couples naturally with:
2. **F5** — eval can't consume recipes: `build_predict_fn` expects `.run`/callable but
   `AgentWorkflowAdapter` exposes `invoke/ainvoke` returning a dict that *already
   contains* `answer_text/docs/answer/context_passages`. Teaching `build_predict_fn`
   to accept the adapter solves F3's passage plumbing for free. Consider doing F3+F5
   as one unit.
3. Then continue down the findings table (`docs/REQUIREMENTS_REVIEW.md` §9):
   F4 (query-cache lost updates), F7 (few-shot `min_examples` unreachable),
   F17 (corrective_search best-so-far), F19 (UI feedback edge cases),
   F20 cleanup (note: `scripts/create_github_issues.py` still references the old
   `ablations/` paths — it's on F20's dead-artifact list anyway).

## Gotchas for the next session

- `docs/REQUIREMENTS_REVIEW.md` is the working document — owner-edited; check its §9
  table and inline owner answers before re-deciding anything.
- The `_VAR_MAP` flip in judges.py is inert-but-harmless until step 1 lands (the judge
  sees the outputs dict either way; it just doesn't have passages in it yet).
- Verification commands: `python -m pytest -q --ignore=tests/test_mongo_source.py`
  (should stay at 416+ passed), `ruff check .`.
